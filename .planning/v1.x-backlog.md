---
status: active
created: 2026-05-26
updated: 2026-05-27
scope: Items deferred to a future v1.1+ milestone. Not in scope for v1 GA close-out (Phase 11). See REQUIREMENTS.md for the one-line index pointing here.
---

# v1.x Backlog

Items surfaced during v1.0 development that are **not blocking GA** but should be addressed in a subsequent milestone (v1.1 or later). Each item has source context, a size estimate, and dependencies so that future milestone planning (`/gsd:new-milestone`) has the information it needs.

---

## ADMIN-search: Admin user-list search/filter

**Source:** Phase 10 live UAT acceptance note (2026-05-26 — `.planning/phase-10/10-HUMAN-UAT.md` §Acceptance note)
**Estimate:** S
**Status:** Captured

Today `/app/admin/users` is paginated only. For fleets with many users, finding a specific account requires clicking through pages. Add a search / filter input on `AdminUsers.vue` that filters on `username` and `email`.

**Sketch:**
- Backend: extend `GET /api/v2/admin/users` in `lib/router.admin.js` with a `q=` query param. CouchDB Mango query is the cleanest given the docs are already in `managed_users`.
- Frontend: debounced search input in `AdminUsers.vue` toolbar; on input, refetch with `q=` param and reset pagination to page 1.
- Decision needed: case-insensitive substring (better UX) vs prefix-only (cheaper query). Substring probably, bounded by pagination so we don't ship 10k matches.

**Dependencies:** None. Self-contained.

---

## AUTH-04: Vue console signup form

**Source:** Phase 9 gap G11 (`.planning/phase-9/09-UAT-SUMMARY.md` #G11)
**Estimate:** M
**Status:** Captured

Vue console exposes no signup form — new account creation only works in the legacy AngularJS console. This forces existing-user-only Vue console usage and breaks any "evaluate the platform" flow.

**Sketch:**
- New route `/signup` + page `Signup.vue` (mirror `PasswordReset.vue` layout from Phase 8)
- Vuex action in `auth` store: `POST /api/v2/register`
- Activation-email round-trip ties into the existing PasswordReset.vue's `?activation=...` query path (same component handles both `reset_key` and `activation` per Phase 8 design)
- Login.vue: add "Create account" link below the "Forgot password?" link

**Dependencies:** Backend confirmation that `POST /api/v2/register` works as expected for new owners (G11 didn't dig into backend, only noted absence of UI).

---

## ADMIN-devcount: Real device_count per user

**Source:** Phase 10 locked OQ-B (2026-05-24) — `device_count: 0` was a v1 placeholder
**Estimate:** S–M
**Status:** Captured

The admin user list shows `device_count: 0` for every row. The intent at Phase 10 design time was to ship the placeholder for v1 and aggregate the real count from `managed_devices` for v1.1.

**Sketch:**
- Add a CouchDB view on `managed_devices` keyed by `owner`, reducing to count (`_count`)
- In `lib/router.admin.js` GET /admin/users handler, query the view per owner (or batch with `keys=[...]`) and merge into the response
- Avoid N+1 — use the batch endpoint
- Consider caching if list pages get slow

**Dependencies:** None. Self-contained.

---

## HIST-flags: Native admin/impersonation flag chips on History page

**Source:** Phase 10 research §A9 footnote (`.planning/phase-10/10-RESEARCH.md`)
**Estimate:** XS
**Status:** Captured (polish)

History page's `flagFilterOptions` array (from Phase 7) doesn't natively include `admin` and `impersonation` chips. Audit rows under impersonation can still be found via free-text search, but a dedicated chip would surface them faster.

**Sketch:**
- One-line array extension in the History page component
- Optional: visual differentiation (color) for admin-flagged rows

**Dependencies:** None.

---

## OPS-swarmpull: Diagnose swarm auto-pull failure

**Source:** 2026-05-25 / 2026-05-26 incident — parent CI pipelines 5213 + 5214 + 5215 each succeeded but the swarm did not pull the new `thinx/console:vue` image. Manual `./scripts/stack-deploy` on swarm manager fixed it. Auto-pull worked previously (parent commit `96e8e144` at 14:44 CET on 2026-05-25 deployed automatically within minutes).
**Estimate:** Unknown — could be small if it's a stopped Watchtower container; could be large if it's a registry / network / image-tag / credential config issue.
**Status:** Captured (operational)

Until this is fixed, every Vue console deploy requires manual `stack-deploy` on the swarm manager — error-prone and adds latency to the dev loop.

**Investigation hints:**
- `docker ps -a | grep -i watch` on each swarm node — is Watchtower (or equivalent) container alive?
- `docker service inspect thinx_vue-console` — is the service tracking the `:vue` tag, or has it been pinned to a specific digest?
- `docker service logs thinx_vue-console` — any pull errors?
- Did `registry.thinx.cloud:5000` credentials rotate? Check `--with-registry-auth` token freshness.
- Cross-reference with the `chore: deploy commit <SHA>` style auto-redeploy commits in parent git history — if those stopped being authored automatically, that's the mechanism that broke.

**Dependencies:** SSH access to swarm manager.

---

## AUTH-bearer-null: Vue console sends `Bearer null` on logged-out requests

**Source:** Frontend half of the parent monorepo's Phase 1 / AUTH-API-01 root-cause investigation (2026-05-26). See parent `.planning/milestones/v1.0-phases/01-auth-api-password-reset/01-SUMMARY.md` §"Root cause" + §"Fix philosophy".
**Estimate:** XS
**Status:** Captured (harmless after parent backend fix; hygiene cleanup only)

`vue/src/core/api.js:53-57` unconditionally serializes `Authorization: 'Bearer ' + this.refreshToken` on every outbound request. When the user is logged out, `this.refreshToken === null`, so the literal string `"Bearer null"` ships as the auth header on every public-route call (e.g. `POST /api/v2/password/reset`).

This was the frontend half of the G8 403 regression. The parent monorepo's Phase 1 shipped a class-fix Bearer-null guard at `lib/router.js:103` (parent commit `622aa01`) so the malformed header now falls through to the cookie/no-auth path — the Vue side is no longer load-bearing for correctness. The cleanup is the right hygiene fix: don't send an `Authorization` header at all when there's no token to send.

**Sketch:**
- In `vue/src/core/api.js#composeOptions` (or wherever the header is currently set), gate the `Authorization` line on `this.refreshToken != null` (covers both `null` and `undefined`)
- Consider the symmetric check on the access-token branch
- Behavior unchanged for authenticated requests; logged-out requests should ship with NO `Authorization` header (verify in Network tab on a logged-out `/password/reset` POST)

**Dependencies:** None — purely client-side cleanup. Safe under the parent's backend guard.

---

## CY-loginargs: `cy.login(user, pass)` ignores its arguments

**Source:** Surfaced by background agent during Phase 10 Wave 3 `cy.loginAsAdmin()` work — `vue/cypress/support/commands.ts:43`
**Estimate:** XS
**Status:** Captured (latent bug)

`cy.login` is declared with `(user, password)` parameters but always reads `fixtures.username` / `fixtures.password`. Harmless today because all 7 non-admin specs rely on the current fixture-only behavior — but any future test that calls `cy.login('alice', 'secret')` would silently log in as the leaked test fixture instead.

**Fix options:**
- Honor the params when provided, default to fixture when absent (semantic-preserving for existing 7 specs; small TS change)
- Rename the parameterized variant to `cy.loginAs(user, pass)`, keep `cy.login()` as the fixture-only shortcut (cleaner separation of intent; matches the new `cy.loginAsAdmin()` pattern)

The second option matches the existing `cy.loginAsAdmin()` naming and makes intent unambiguous at every call site.

**Dependencies:** None — purely test-side cleanup.

---

*Backlog file created: 2026-05-26 during Phase 10 close-out.*
*Items graduate from here to a v1.1+ milestone via `/gsd:new-milestone` when scoped.*
