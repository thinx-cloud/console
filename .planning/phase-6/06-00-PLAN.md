---
phase: 06-user-profile
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - vue/cypress/integration/profile.spec.js
  - vue/src/components/Notifications/Notifications.vue
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04
  - PROF-05
  - PROF-06
tags:
  - cypress
  - e2e
  - test-stub
  - vue
  - bugfix
  - g1

must_haves:
  truths:
    - "vue/cypress/integration/profile.spec.js exists and uses the same describe/beforeEach/cy.login()/cy.visit pattern as devices.spec.js"
    - "profile.spec.js has exactly 7 it(...) stubs covering PROF-01 through PROF-06 plus G1, each with a TODO PROF-XX or TODO G1 comment body"
    - "No it.only is used — the spec does not lock the Cypress runner to a single test"
    - "Notifications.vue line 57 no longer has mapGetters in computed: — it is now in methods: alongside the existing mapActions spread"
    - "Notifications.vue still calls this.getBuildLog() as a function at line 75 and this continues to work"
    - "The recentBuilds computed property is undisturbed — it still references this.buildItems (a data property)"
  artifacts:
    - path: "vue/cypress/integration/profile.spec.js"
      provides: "Wave 0 e2e stub for the Profile page (PROF-01–06 + G1 coverage)"
      contains: "Profile feature"
    - path: "vue/src/components/Notifications/Notifications.vue"
      provides: "G1 bugfix — mapGetters moved from computed: to methods: so getBuildLog() works as a function"
      contains: "getBuildLog"
  key_links:
    - from: "profile.spec.js beforeEach"
      to: "cy.login() + cy.visit('http://localhost:3000/#/app/profile')"
      via: "Cypress custom command defined in vue/cypress/support/commands.ts"
      pattern: "cy\\.login\\(\\)"
    - from: "Notifications.vue methods:"
      to: "mapGetters({ getBuildLog: 'buildlog/getItems' })"
      via: "spread into methods: not computed:"
      pattern: "getBuildLog.*buildlog"
---

<objective>
Create the Wave 0 Cypress spec stub for the Profile page and fix the G1 bug in Notifications.vue.

Purpose: The Cypress stub gives Wave 1 and Wave 2 tasks a non-MISSING automated verify target (Nyquist compliance). The G1 fix (moving mapGetters from computed: to methods: in Notifications.vue) is a one-line change that can ship independently — it unblocks the TypeError that fires on every authenticated page. Both tasks are independent and can run in parallel.

Output: One new Cypress spec file at vue/cypress/integration/profile.spec.js plus a one-line edit to Notifications.vue:57. No store changes, no route changes, no other production code touched.
</objective>

<execution_context>
@/Users/igraczech/Repositories/thinx-device-api/services/console/.claude/get-shit-done/workflows/execute-plan.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/ROADMAP.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-6/06-RESEARCH.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/integration/devices.spec.js
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/support/commands.ts
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/components/Notifications/Notifications.vue

<interfaces>
From vue/cypress/integration/devices.spec.js (reference structure — copy describe/beforeEach pattern exactly):

  describe('Devices feature', function() {
    beforeEach(() => {
      cy.viewport(1536, 754);
      cy.login();
      cy.visit('http://localhost:3000/#/app/devices');
    });
    it('Should display category filter pills (DEVI-01)', function() { /* TODO DEVI-01: ... */ });
  });

Notes:
- cy.login() is a custom command in vue/cypress/support/commands.ts — no args needed.
- Viewport 1536x754 is the project convention.
- Cypress 9.5.4 mocha-style syntax.
- Do NOT use it.only.
- Profile route: /#/app/profile (registered in Routes.js:104).

From vue/src/components/Notifications/Notifications.vue (current broken state at lines 56-57):

  computed: {
    ...mapGetters({ getBuildLog: 'buildlog/getItems' }),  // LINE 57 — wrong placement
    recentBuilds() { ... },
  },
  methods: {
    ...mapActions({ fetchBuildLog: 'buildlog/fetchBuildLog' }),
    async loadNotifications() {
      ...
      this.buildItems = this.getBuildLog() || [];  // line 75 — called as function, breaks
    },
  }

Fix: remove ...mapGetters({ getBuildLog }) from computed: and add it to methods: alongside the existing ...mapActions spread.
The recentBuilds computed property only reads this.buildItems (a data() property) — it is unaffected by this move.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Cypress spec stub for the Profile page (PROF-01–06 + G1)</name>
  <files>vue/cypress/integration/profile.spec.js</files>
  <read_first>
    - vue/cypress/integration/devices.spec.js — reference describe/beforeEach/cy.login()/cy.visit pattern, viewport convention 1536x754
    - vue/cypress/support/commands.ts — confirm cy.login() custom command signature (no required args)
    - vue/src/Routes.js — confirm the profile route path resolves to /#/app/profile
  </read_first>
  <action>
    Create vue/cypress/integration/profile.spec.js with describe('Profile feature', function() { ... }). In beforeEach set cy.viewport(1536, 754), call cy.login(), then cy.visit('http://localhost:3000/#/app/profile'). Add the following 7 it(...) blocks, each with a single-line TODO comment as the body — no assertions (Waves 1 and 2 fill them in):

    1. it('Should load the profile page without JS errors (PROF-01)', function() { /* TODO PROF-01: verify page title "My Profile" visible, no console errors */ })
    2. it('Should display and save profile fields: first name, last name, phone, timezone (PROF-01)', function() { /* TODO PROF-01: fill first_name/last_name/mobile_phone/timezone fields, click Save Profile, verify success alert */ })
    3. it('Should display avatar upload picker and preview (PROF-02)', function() { /* TODO PROF-02: verify avatar tab visible, file input present, preview <img> updates after upload */ })
    4. it('Should save notification preferences without overwriting profile info (PROF-03)', function() { /* TODO PROF-03: check "All notifications" checkbox, click Save Notifications, verify success alert, reload and confirm profile first_name unchanged */ })
    5. it('Should show admin tab for admin users and hide it for non-admin (PROF-04)', function() { /* TODO PROF-04: verify admin tab present when profile.admin === true, absent when false */ })
    6. it('Should require confirmation before deleting account and redirect to /login (PROF-05)', function() { /* TODO PROF-05: click Delete My Account, confirm modal appears, cancel does not redirect, confirm does redirect to /login */ })
    7. it('Should be reachable via header My Account dropdown link (PROF-06)', function() { /* TODO PROF-06: click header settings dropdown, click My Account, verify URL is /#/app/profile */ })

    Do NOT use it.only. Do NOT add any cy.get, cy.intercept, or assertion calls — those go in Waves 1 and 2. Ensure the file ends with a single trailing newline and is valid ES2017 JavaScript.
  </action>
  <verify>
    <automated>node --check /Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/integration/profile.spec.js && grep -c "^  it(" /Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/integration/profile.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - File vue/cypress/integration/profile.spec.js exists.
    - node --check exits 0 (valid JavaScript).
    - grep -c "^  it(" returns 7.
    - grep -v "^#" vue/cypress/integration/profile.spec.js | grep -c "it.only" returns 0.
    - grep -c "cy.login()" returns 1 — called exactly once in beforeEach.
    - grep -c "cy.visit('http://localhost:3000/#/app/profile')" returns 1.
    - grep -c "TODO PROF-" returns at least 6 — one TODO per PROF-XX requirement.
    - grep -c "TODO G1" returns 0 — G1 regression is covered by the Notifications component fix in Task 2, not a separate stub.
    - grep -c "describe('Profile feature'" returns 1.
    - At least one it description references each of PROF-01, PROF-02, PROF-03, PROF-04, PROF-05, PROF-06.
  </acceptance_criteria>
  <done>profile.spec.js exists, parses with node --check, contains 7 it stubs covering PROF-01–06, uses no .only, and is recognised by yarn cy:open as a runnable no-assert spec.</done>
</task>

<task type="auto">
  <name>Task 2: Fix G1 — move mapGetters from computed: to methods: in Notifications.vue</name>
  <files>vue/src/components/Notifications/Notifications.vue</files>
  <read_first>
    - vue/src/components/Notifications/Notifications.vue — read in full; the bug is at line 57 (mapGetters spread in computed:); the call site is line 75 (this.getBuildLog() in loadNotifications)
    - .planning/codebase/CONVENTIONS.md — mapGetters in methods: convention documented at line 94
  </read_first>
  <action>
    Open vue/src/components/Notifications/Notifications.vue and make two changes:

    Step (a) — Remove ...mapGetters({ getBuildLog: 'buildlog/getItems' }) from the computed: block (currently line 57). The computed: block should then contain only the recentBuilds() computed property.

    Step (b) — Add ...mapGetters({ getBuildLog: 'buildlog/getItems' }) into the methods: block as the FIRST spread, immediately before the existing ...mapActions({ fetchBuildLog: 'buildlog/fetchBuildLog' }) spread. The methods: block already imports mapActions from vuex at line 45 — do not duplicate the import. Ensure mapGetters is also present in the existing import { mapActions, mapGetters } from 'vuex' at line 45 (it already is — do not modify the import line).

    Do NOT change any other line — loadNotifications() at line 75 already calls this.getBuildLog() as a function and will work correctly once getBuildLog is in methods:. The recentBuilds computed property at lines 58-63 reads only this.buildItems (a data() property) and is unaffected.
  </action>
  <verify>
    <automated>cd /Users/igraczech/Repositories/thinx-device-api/services/console && grep -n "getBuildLog" vue/src/components/Notifications/Notifications.vue</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "getBuildLog" Notifications.vue shows getBuildLog appearing ONLY inside the methods: block — not inside computed:.
    - grep -c "computed:" Notifications.vue returns 1 — the computed: block is still present (for recentBuilds).
    - grep -c "recentBuilds" Notifications.vue returns at least 2 — the computed property definition is preserved.
    - The computed: block does NOT contain ...mapGetters.
    - The methods: block DOES contain ...mapGetters({ getBuildLog: 'buildlog/getItems' }).
    - grep -c "mapGetters" Notifications.vue returns 2 — one in the import at line 45, one in the methods: spread.
    - yarn --cwd vue serve compiles without errors.
  </acceptance_criteria>
  <done>Notifications.vue has mapGetters spread in methods: only. this.getBuildLog() at line 75 now resolves as a function call without TypeError. The recentBuilds computed property is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cypress runner → dev server | Test spec runs against http://localhost:3000 — same origin as real app |
| cy.login() → /login form | Custom command authenticates with static test fixture credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-00-01 | Information Disclosure | static test credentials in cy.login() | accept | Test creds are dev-only in cypress/fixtures/thinx.json (pre-existing). No change. |
| T-06-00-02 | Tampering | spec file committed to repo | mitigate | Stub contains no secrets, no eval, no arbitrary require. Only describe/it/cy.* calls. |
| T-06-00-03 | Elevation of Privilege | G1 fix exposing getBuildLog to methods: namespace | accept | getBuildLog is a read-only Vuex getter; moving it to methods: changes call convention only, not access level. No new capabilities are exposed. |
| T-06-00-SC | Tampering | npm supply chain | accept | This plan installs NO new npm packages. Cypress 9.5.4 already pinned. |
</threat_model>

<verification>
1. yarn --cwd vue cy:open opens the Cypress runner sidebar and lists profile.spec.js alongside the existing login, devices, device-detail, and dashboard specs.
2. Selecting profile.spec.js shows 7 pending it blocks — all pass vacuously (empty bodies).
3. yarn --cwd vue serve compiles Notifications.vue without errors.
4. In the running app, navigating to any authenticated page that renders the Notifications widget no longer produces TypeError: getBuildLog is not a function in the browser console.
</verification>

<success_criteria>
- profile.spec.js exists, parses with node --check, and passes all grep gates from Task 1 acceptance_criteria.
- Notifications.vue has mapGetters in methods: only — grep confirms it is absent from computed:.
- Both files compile and the Cypress runner accepts the new spec without parse errors.
</success_criteria>

<output>
Create /Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-6/06-00-SUMMARY.md when done
</output>
