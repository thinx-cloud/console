---
phase: 05-real-dashboard
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - vue/cypress/integration/dashboard.spec.js
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
tags:
  - cypress
  - e2e
  - test-stub
  - vue
  - dashboard

must_haves:
  truths:
    - "vue/cypress/integration/dashboard.spec.js exists and is loaded by `yarn cy:open` alongside login.spec.js, devices.spec.js, device-detail.spec.js"
    - "The spec uses the same describe/beforeEach/cy.login()/cy.visit pattern as devices.spec.js"
    - "dashboard.spec.js has at least 8 `it(...)` blocks, each tagged with a DASH-XX TODO comment, covering DASH-01 through DASH-05"
    - "Spec does not block — empty TODO-only bodies pass under `yarn cy:open` without throwing"
    - "No `it.only` is used (would lock the runner to a single test)"
  artifacts:
    - path: "vue/cypress/integration/dashboard.spec.js"
      provides: "Wave 0 e2e stub for the Dashboard page (DASH-01–05 coverage)"
      contains: "Dashboard feature"
  key_links:
    - from: "dashboard.spec.js beforeEach"
      to: "cy.login() + cy.visit('http://localhost:3000/#/app/dashboard')"
      via: "Cypress custom command (defined in vue/cypress/support/commands.ts)"
      pattern: "cy\\.login\\(\\)"
---

<objective>
Create the Wave 0 Cypress spec stub for the Dashboard page so every Wave 1 and Wave 2 implementation task has an automated verification target (Nyquist compliance).

Purpose: Phase 4 established the pattern of a Wave 0 Cypress stub plan that produces a runnable but assertion-free spec, letting downstream implementation tasks declare a non-MISSING `<automated>` verify referencing the spec. Phase 5 follows the same pattern with a single new spec file for the Dashboard. The stub is minimal — `describe`/`beforeEach`/`cy.login()`/`cy.visit()` plus one `it(...)` per requirement with a `// TODO DASH-XX` body. Waves 1 and 2 fill in the real assertions.

Output: One new Cypress spec file registered with the existing Cypress runner. No production code is touched.
</objective>

<execution_context>
@/Users/igraczech/Repositories/thinx-device-api/services/console/.claude/get-shit-done/workflows/execute-plan.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/ROADMAP.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/REQUIREMENTS.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/integration/devices.spec.js
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/support/commands.ts

<interfaces>
From vue/cypress/integration/devices.spec.js (reference structure — copy describe/beforeEach pattern exactly):

```
describe('Devices feature', function() {
  beforeEach(() => {
    cy.viewport(1536, 754);
    cy.login();
    cy.visit('http://localhost:3000/#/app/devices');
  });
  it('Should display category filter pills (DEVI-01)', function() { /* TODO DEVI-01: ... */ });
});
```

Notes:
- `cy.login()` is a custom command defined in vue/cypress/support/commands.ts — it visits `/`, types credentials from `cypress/fixtures/thinx.json`, clicks login, and `cy.wait(2000)`. Call it in `beforeEach`.
- Viewport size 1536×754 is the project convention — use the same value.
- Cypress 9.5.4 syntax (mocha-style `describe`/`it`).
- DO NOT use `it.only` in stubs — that locks the runner to a single test. Use plain `it(...)`.
- The Dashboard route is registered in vue/src/Routes.js with `name: 'Dashboard'` and component `@/pages/Visits/Visits`. The route path resolves to `/#/app/dashboard`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Cypress stub for the Dashboard page</name>
  <files>vue/cypress/integration/dashboard.spec.js</files>
  <read_first>
    - vue/cypress/integration/devices.spec.js — reference describe/beforeEach/cy.login()/cy.visit pattern, viewport convention 1536×754
    - vue/cypress/support/commands.ts — confirm `cy.login()` custom command exists and takes no required args
    - vue/src/Routes.js — confirm the Dashboard route path so `cy.visit` targets the correct URL (route `name: 'Dashboard'`, child path resolving under `/app`)
    - .planning/ROADMAP.md — Phase 5 section, DASH-01 through DASH-05 UAT criteria
  </read_first>
  <action>
    Create vue/cypress/integration/dashboard.spec.js with `describe('Dashboard feature', function() { ... })`. In `beforeEach` set `cy.viewport(1536, 754)`, call `cy.login()`, then `cy.visit('http://localhost:3000/#/app/dashboard')`. Add the following `it(...)` blocks, each with a single-line TODO comment as the body (no assertions yet — Waves 1 and 2 fill them in):
    - `it('Should load the dashboard without JS errors (DASH-01)', function() { /* TODO DASH-01: verify page-title Dashboard visible, no console errors */ })`
    - `it('Should fetch real stats from GET /stats (DASH-01)', function() { /* TODO DASH-01: intercept /stats, assert request fired and cards bound to response */ })`
    - `it('Should display six metric cards (DASH-02)', function() { /* TODO DASH-02: verify 6 metric card elements rendered */ })`
    - `it('Should show today/week/month breakdowns on metric cards (DASH-02)', function() { /* TODO DASH-02: verify each card shows three period sub-values */ })`
    - `it('Should render the check-ins timeline chart (DASH-03)', function() { /* TODO DASH-03: verify canvas/chart element present */ })`
    - `it('Should update the timeline chart when the range selector changes (DASH-03)', function() { /* TODO DASH-03: click 31-day option, verify chart re-renders */ })`
    - `it('Should display the recent builds widget with download links (DASH-04)', function() { /* TODO DASH-04: verify last-10 builds list and download link per row */ })`
    - `it('Should display the recent audit events widget (DASH-05)', function() { /* TODO DASH-05: verify audit events card and rows */ })`
    Do NOT use `it.only`. Do NOT add any `cy.get`, `cy.intercept`, or assertion calls — Wave 1 and Wave 2 tasks add real bodies. Ensure the file ends with a single trailing newline and is valid ES2017 JavaScript so Cypress can load it.
  </action>
  <verify>
    <automated>node --check /Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/integration/dashboard.spec.js && grep -c "^  it(" /Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/integration/dashboard.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - File vue/cypress/integration/dashboard.spec.js exists.
    - `node --check` on the file exits 0 (valid JavaScript).
    - `grep -c "^  it("` returns 8.
    - `grep -c "it.only"` returns 0 — no `.only` lock-ins.
    - `grep -c "cy.login()"` returns 1 — login command called exactly once (in beforeEach).
    - `grep -c "cy.visit('http://localhost:3000/#/app/dashboard')"` returns 1.
    - `grep -c "TODO DASH-"` returns at least 8 (one TODO per `it`).
    - At least one `it` description mentions each of DASH-01, DASH-02, DASH-03, DASH-04, DASH-05.
    - `grep -c "describe('Dashboard feature'" vue/cypress/integration/dashboard.spec.js` returns 1.
  </acceptance_criteria>
  <done>dashboard.spec.js exists, parses with `node --check`, contains 8 `it` stubs covering DASH-01–05, uses no `.only`, and is recognised by `yarn cy:open` as a runnable but currently no-assert spec file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cypress runner → dev server | Test spec dispatches HTTP requests against http://localhost:3000 — same origin as real app, no cross-origin concerns |
| cy.login() → /login form | Custom command performs real auth flow against the dev backend with a static test account from cypress/fixtures/thinx.json |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-00-01 | Information Disclosure | static test account credentials in cy.login() | accept | Test creds live in `cypress/fixtures/thinx.json` (pre-existing), are dev-only, never reach production. No change here. |
| T-05-00-02 | Tampering | spec file committed to repo | mitigate | Stub contains no secrets, no eval, no shell-out — only describe/it/cy.* calls. Verified by grep that no `eval`, `Function(`, or arbitrary `require(` appears. |
| T-05-00-SC | Tampering | npm install (supply chain) | accept | This plan installs NO new npm packages. Cypress 9.5.4 already pinned in package.json. No package-manager install task exists in this plan. |
</threat_model>

<verification>
1. `yarn --cwd vue cy:open` opens the Cypress runner without parse errors.
2. The runner sidebar lists `dashboard.spec.js` alongside the existing `login.spec.js`, `devices.spec.js`, `device-detail.spec.js`.
3. Selecting `dashboard.spec.js` shows 8 pending `it` blocks.
4. Running them all passes (empty bodies are vacuous true) — confirms Wave 1/2 tasks can reference the spec with `<automated>` verifies.
</verification>

<success_criteria>
- dashboard.spec.js exists, parses with `node --check`, and matches the grep gates in acceptance_criteria.
- No `it.only` used.
- Each requirement DASH-01 through DASH-05 has at least one corresponding `it` stub.
- Wave 1 and Wave 2 implementation tasks can now declare a non-MISSING `<automated>` verify referencing this spec.
</success_criteria>

<output>
Create /Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-5/05-00-SUMMARY.md when done
</output>
