---
phase: 08-auth-extras
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - vue/cypress/integration/auth-extras.spec.js
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
tags:
  - cypress
  - e2e
  - test-stub
  - vue
  - auth

must_haves:
  truths:
    - "vue/cypress/integration/auth-extras.spec.js exists and uses describe('Auth Extras feature', function() { ... })"
    - "beforeEach sets cy.viewport(1536, 754) only — no cy.login() in beforeEach because AUTH-01/02 specs target an unauthenticated route"
    - "The spec contains exactly 4 it() blocks: one for AUTH-01, two for AUTH-02 (initiate + confirm form states), and one for AUTH-03"
    - "cy.login() appears exactly once — inside the AUTH-03 it() block, not in beforeEach"
    - "Each it() body is a single TODO comment, no assertions, no cy.get / cy.intercept"
    - "No it.only anywhere"
    - "cy.visit URLs include /#/password-reset (no query), /#/password-reset?reset_key=abc&owner=test, and the AUTH-03 spec does not visit a new page after cy.login()"
  artifacts:
    - path: "vue/cypress/integration/auth-extras.spec.js"
      provides: "Wave 0 e2e stub for the Password Reset page + session-expiry redirect (AUTH-01..03)"
      contains: "Auth Extras feature"
  key_links:
    - from: "auth-extras.spec.js AUTH-03 it() body"
      to: "cy.login()"
      via: "Cypress custom command defined in vue/cypress/support/commands.ts"
      pattern: "cy\\.login\\(\\)"
    - from: "auth-extras.spec.js AUTH-01/02 it() bodies"
      to: "/#/password-reset (unauthenticated route)"
      via: "cy.visit on hash-mode router"
      pattern: "cy\\.visit\\('http://localhost:3000/#/password-reset"
---

<objective>
Create the Wave 0 Cypress spec stub for the Auth Extras feature (Password Reset page + session-expiry redirect).

Purpose: The Cypress stub gives later Wave-N tasks a non-MISSING automated verify target (Nyquist compliance) — Wave 1+ will fill in assertions for AUTH-01 (page renders unauthenticated), AUTH-02 (initiate vs. confirm form rendering based on reset_key query param), and AUTH-03 (session-expiry redirect to /login). No production code is touched in this wave; the only artifact is one new file.

Critical structural difference from prior Wave-0 stubs (Phase 7 history.spec.js, Phase 6 profile.spec.js, etc.): the password-reset page is reachable UNAUTHENTICATED, so cy.login() MUST NOT live in beforeEach. Viewport setup goes in beforeEach; cy.login() is called only inside the single AUTH-03 it() block (where session-expiry redirect testing requires an authenticated starting state).

Output: One new Cypress spec file at vue/cypress/integration/auth-extras.spec.js. No store changes, no route changes, no component changes.
</objective>

<execution_context>
@/Users/igraczech/Repositories/thinx-device-api/services/console/.claude/get-shit-done/workflows/execute-plan.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/ROADMAP.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-8/08-RESEARCH.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-8/08-CONTEXT.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/integration/history.spec.js
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/integration/profile.spec.js
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/support/commands.ts
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/Routes.js

<interfaces>
From vue/cypress/integration/history.spec.js (reference structure — same describe/beforeEach pattern, but cy.login() WILL MOVE out of beforeEach for this phase):

  describe('History feature', function() {
    beforeEach(() => {
      cy.viewport(1536, 754);
      cy.login();
      cy.visit('http://localhost:3000/#/app/history');
    });
    it('Should ... (HIST-01)', function() { /* TODO HIST-01: ... */ });
    // ... single-line TODO body per it(), no assertions
  });

Auth Extras adaptation (this plan):

  describe('Auth Extras feature', function() {
    beforeEach(() => {
      cy.viewport(1536, 754);
      // No cy.login() here — most of these specs hit an unauthenticated route.
    });
    it('... (AUTH-01)', function() { cy.visit('http://localhost:3000/#/password-reset'); /* TODO AUTH-01: ... */ });
    it('... (AUTH-02)', function() { cy.visit('http://localhost:3000/#/password-reset'); /* TODO AUTH-02: ... */ });
    it('... (AUTH-02)', function() { cy.visit('http://localhost:3000/#/password-reset?reset_key=abc&owner=test'); /* TODO AUTH-02: ... */ });
    it('... (AUTH-03)', function() { cy.login(); /* TODO AUTH-03: ... */ });
  });

Notes:
- cy.login() is a custom command in vue/cypress/support/commands.ts (lines 43-50). Parameters are declared but unused — it reads credentials from cypress/fixtures/thinx.json. Call with NO arguments.
- Viewport 1536x754 is the project convention (used in profile.spec.js, history.spec.js, and dashboard.spec.js beforeEach).
- Cypress 9.5.4 mocha-style syntax (pinned in vue/package.json).
- Do NOT use it.only.
- The /password-reset route does not yet exist in Routes.js — it will be added in a later wave as a sibling of /login (see vue/src/Routes.js:30-33 for the /login pattern). The Wave 0 stub references the URL the route WILL resolve to; specs will be pending until the route lands.
- This stub must NOT call cy.get, cy.intercept, or any assertion — those land in Waves 1+.
- The AUTH-03 it() body calls cy.login() and adds a TODO referencing the planned approach (forge a JWT with exp 1s ahead, wait 2s, assert window.location.hash === '#/login'). It does NOT call cy.visit after cy.login (cy.login itself lands the runner on an authenticated page).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Cypress spec stub for the Auth Extras feature (AUTH-01..03)</name>
  <files>vue/cypress/integration/auth-extras.spec.js</files>
  <read_first>
    - vue/cypress/integration/history.spec.js — reference describe/beforeEach/it() formatting (blank line after describe header, blank line between it() blocks, trailing semicolon on each it(), single trailing newline at EOF)
    - vue/cypress/integration/profile.spec.js — second reference for the same stub pattern at a different route
    - vue/cypress/support/commands.ts — confirm cy.login() takes no required args (lines 43-50)
    - vue/src/Routes.js — confirm /login route shape at lines 30-33 (sibling pattern for the /password-reset route to be added in later waves)
    - .planning/phase-8/08-RESEARCH.md §1.7 and §4 Wave 0 — canonical it() skeletons for this plan
  </read_first>
  <action>
    Create vue/cypress/integration/auth-extras.spec.js with top-level describe('Auth Extras feature', function() { ... }). In beforeEach call ONLY cy.viewport(1536, 754) — do NOT call cy.login() in beforeEach, because three of the four specs target the unauthenticated /#/password-reset route. Add a single-line comment in beforeEach explaining why cy.login() is omitted ("No cy.login() here — most of these specs hit an unauthenticated route.").

    Add the following 4 it(...) blocks in this order:

    1. it('Should render the password-reset page unauthenticated (AUTH-01)', function() { ... })
       Body: cy.visit('http://localhost:3000/#/password-reset'); followed by the TODO comment: /* TODO AUTH-01: assert page title visible, no console errors, no redirect to /login */

    2. it('Should show the initiate (email) form when no token is in the query (AUTH-02)', function() { ... })
       Body: cy.visit('http://localhost:3000/#/password-reset'); followed by the TODO comment: /* TODO AUTH-02: assert email input visible, password fields not visible */

    3. it('Should show the confirm (password) form when reset_key is in the query (AUTH-02)', function() { ... })
       Body: cy.visit('http://localhost:3000/#/password-reset?reset_key=abc&owner=test'); followed by the TODO comment: /* TODO AUTH-02: assert two password inputs visible, email input not visible */

    4. it('Should redirect to /login when the access token expires (AUTH-03)', function() { ... })
       Body: cy.login(); followed by the TODO comment: /* TODO AUTH-03: forge a JWT with exp 1 second ahead, wait 2s, assert window.location.hash is '#/login' */
       Do NOT call cy.visit after cy.login in this spec — cy.login lands the runner on an authenticated page already.

    Mirror the formatting of history.spec.js exactly:
    - Leading blank line at the top of the file
    - describe with a 2-space indent for its body
    - Blank line between describe header and beforeEach
    - Blank line between beforeEach and the first it()
    - Blank line between each it() block
    - Each it() ends with a semicolon
    - Single trailing newline at end of file
    - Valid ES2017 JavaScript

    Do NOT use it.only. Do NOT add any cy.get, cy.intercept, or assertion calls. Do NOT add console.log statements. Do NOT add fixtures loads. cy.login() MUST appear exactly once — only inside the AUTH-03 it() body.
  </action>
  <verify>
    <automated>node --check /Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/integration/auth-extras.spec.js && grep -c "^  it(" /Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/integration/auth-extras.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - File vue/cypress/integration/auth-extras.spec.js exists.
    - node --check exits 0 (valid JavaScript).
    - grep -c "^  it(" returns 4.
    - grep -c "it.only" returns 0 — the spec does not lock the Cypress runner to a single test.
    - grep -c "cy.login()" returns 1 — called exactly once, inside the AUTH-03 it() body, NOT in beforeEach.
    - grep -c "cy.visit('http://localhost:3000/#/password-reset" returns at least 3 — three visits to /#/password-reset variations (no-query, no-query, with reset_key query).
    - grep -c "TODO AUTH-" returns at least 4 — one TODO per it().
    - grep -c "describe('Auth Extras feature'" returns 1.
    - Each of AUTH-01, AUTH-02, AUTH-03 appears at least once in it() descriptions (AUTH-02 appears twice).
    - cy.login() does NOT appear inside beforeEach — verified by reading the file and confirming the only occurrence is inside the AUTH-03 it() body.
    - beforeEach contains exactly one Cypress call (cy.viewport) plus an explanatory comment.
  </acceptance_criteria>
  <done>auth-extras.spec.js exists, parses with node --check, contains 4 it() stubs covering AUTH-01, AUTH-02 (x2), and AUTH-03, uses no .only, calls cy.login() exactly once (inside the AUTH-03 it() body), and is recognised by yarn cy:open as a runnable no-assert spec alongside the existing login, devices, device-detail, dashboard, profile, and history specs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cypress runner → dev server | Test spec runs against http://localhost:3000 — same origin as the real app |
| cy.login() → /login form | Custom command authenticates with static test fixture credentials from cypress/fixtures/thinx.json (used only in the AUTH-03 spec) |
| Unauthenticated cy.visit → /#/password-reset | Three of the four specs deliberately exercise the unauthenticated path; ensures the password-reset route does NOT require a session cookie |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-00-01 | Information Disclosure | static test credentials in cy.login() | accept | Test creds are dev-only in cypress/fixtures/thinx.json (pre-existing across Phases 4–7). No change in this plan. cy.login() invoked only once, inside the AUTH-03 it() body. |
| T-08-00-02 | Tampering | spec file committed to repo | mitigate | Stub contains no secrets, no eval, no arbitrary require, no network calls beyond cy.login() + cy.visit(). Only describe/it/cy.* calls. |
| T-08-00-03 | Elevation of Privilege | unauthenticated route exposure | accept | The /#/password-reset route is INTENDED to be reachable without a session — that is requirement AUTH-01. The stub merely visits it; later waves will assert behavior. No privilege boundary is crossed in this Wave 0 task. |
| T-08-00-SC | Tampering | npm supply chain | accept | This plan installs NO new npm packages. Cypress 9.5.4 already pinned in vue/package.json. |
</threat_model>

<verification>
1. yarn --cwd vue cy:open opens the Cypress runner sidebar and lists auth-extras.spec.js alongside the existing login, devices, device-detail, dashboard, profile, and history specs.
2. Selecting auth-extras.spec.js shows 4 pending it() blocks — all pass vacuously (TODO-only bodies, no assertions).
3. node --check vue/cypress/integration/auth-extras.spec.js exits 0.
4. None of the existing specs (login, devices, device-detail, dashboard, profile, history) are altered.
5. Manual file inspection confirms cy.login() appears only inside the AUTH-03 it() body, never in beforeEach.
</verification>

<success_criteria>
- auth-extras.spec.js exists, parses with node --check, and passes all grep gates from Task 1 acceptance_criteria.
- The Cypress runner accepts the new spec without parse errors.
- No production code is modified — files_modified contains exactly one entry, the new spec file.
- The structural invariant "cy.login() not in beforeEach" is upheld and is verifiable by reading the file.
</success_criteria>

<output>
Create /Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-8/08-00-SUMMARY.md when done
</output>
