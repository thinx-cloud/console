---
quick_id: 260526-2d3
slug: g9-revoke-selection-prune
date: 2026-05-26
status: complete
phase_ref: Phase 11 Wave 2 (ROADMAP.md)
files_modified:
  - vue/src/pages/Devices/Devices.vue
commits:
  - 4be39f3 fix(11-02/G9): Devices.vue revokeRow — splice udid from selection before refetch
unsigned_commits:
  - 4be39f3 (GPG pinentry unavailable; user authorized --no-gpg-sign for this task)
---

# Quick Task 260526-2d3 — SUMMARY

## What shipped

`vue/src/pages/Devices/Devices.vue` `revokeRow()` success branch now splices
the just-revoked udid out of `this.selectedUdids[]` before `loadData()`.
Two lines added, no behavior changes anywhere else in the file.

**Diff:**
```diff
@@ -312,6 +312,8 @@ revokeRow:
       const result = await this.revokeDevices([udid]);
       if (result.success) {
         this.message = 'Device revoked.';
+        const i = this.selectedUdids.indexOf(udid);
+        if (i > -1) this.selectedUdids.splice(i, 1);
         this.loadData();
       } else {
```

Pattern mirrors `toggleDevice` (L233-L237) — same `indexOf` + `splice`
idiom, preserves array identity. Zero new dependencies. `mapGetters` stays
in `methods:` (Phase 6 G1 anti-regression — unchanged).

## must_haves verification

- [x] Per-row Revoke success path splices the revoked udid out of `selectedUdids[]` before `loadData()` — see L315-L316 of Devices.vue (post-commit)
- [x] Bulk Revoke behavior unchanged — `confirmRevoke` still hits `this.selectedUdids = []` (L258, untouched by this commit)
- [x] No new dependencies, no `mapGetters` moves — confirmed by single-file diff

## UAT (per ROADMAP.md L393)

**Manual walk required on live console.thinx.cloud after deploy:**

`/#/app/devices` → tick checkbox on a throwaway device row (toolbar shows
`Revoke (1)`) → click that row's per-row Revoke button → confirm modal →
device disappears AND toolbar counter drops to `(0)`.

Not added to Cypress: project convention is intentional Cypress stubs
([[thinx-console-vue-conventions]]); revoke flows already covered by
existing live-walk UAT items in Phase 9 / Phase 10.

## Deployment

This change rides the standard console-vue deploy path:

1. Push `thinx-staging` branch (current branch)
2. Parent monorepo submodule bump rebuilds the `vue` image
3. Swarm redeploy pulls `registry.thinx.cloud:5000/thinx/console:vue`

See [[deployment-console-thinx-cloud]] and [[ci-thinx-cloud-console]] for
the post-2026-05-24 CI flow. Manual `./scripts/stack-deploy` may still be
needed until swarm auto-pull (v1.x backlog OPS-swarmpull) is diagnosed.

## Phase 11 status after this task

| Wave | Item | Repo | Status |
|------|------|------|--------|
| 1 | G8 — `POST /api/v2/password/reset` 403 | parent monorepo | **Not started** (different repo) |
| 2 | G9 — Devices.vue per-row Revoke selection-prune | console (this) | **Code shipped** `4be39f3`; UAT pending live deploy |

Phase 11 itself remains Pending in ROADMAP.md — Wave 1 is the gating item.
G9 closed via quick task per user direction (one-line splice, no plan overhead).

## Anomaly

Commit `4be39f3` is **unsigned**. GPG pinentry was unavailable in this
session; user explicitly authorized `--no-gpg-sign` for this single
commit. All other recent project commits are GPG-signed. If signing
matters for downstream tooling, amend with `git commit --amend -S
--no-edit` once GPG is reachable.
