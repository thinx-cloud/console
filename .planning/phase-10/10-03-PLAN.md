---
phase: 10-admin-features
plan: 03
type: execute
wave: 3
depends_on:
  - 10-02
repo: console-submodule
files_modified:
  - vue/src/pages/Profile/Profile.vue
  - vue/src/components/Sidebar/Sidebar.vue
  - vue/cypress/integration/admin.spec.js
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
tags:
  - vue
  - sidebar
  - profile
  - cypress
  - close-out
  - requirements
  - roadmap

must_haves:
  truths:
    - "vue/src/pages/Profile/Profile.vue lines 138-144 placeholder <b-card> with 'not yet available' text is replaced with a <b-card> containing a brief description + <router-link to=\"/app/admin/users\" class=\"btn btn-primary btn-sm\">Open Admin Console</router-link>"
    - "vue/src/components/Sidebar/Sidebar.vue renders a conditional <NavLink header=\"Admin\" link=\"/app/admin/users\" .../> ONLY when the current profile.admin === true; non-admins see zero Admin entries in the sidebar"
    - "vue/src/components/Sidebar/Sidebar.vue uses mapGetters('profile/getProfile') spread INSIDE methods: (NOT computed:) per Phase 6 G1 anti-regression; the isAdmin computed reads via this.getProfile()"
    - "vue/cypress/integration/admin.spec.js it() bodies are flipped from TODO comments to real cy.get / cy.contains / cy.intercept assertions, EXCEPT where the assertion requires a HN-deferred condition (non-admin account / second-browser revoke verification) — those it() blocks become it.skip() with a one-line justification comment"
    - ".planning/REQUIREMENTS.md ADMIN-01, ADMIN-02, ADMIN-03 lines 103-105 checkboxes flip from [ ] to [x] AND the Traceability table rows 189-191 flip from 'Pending (planning — research complete 2026-05-24)' to 'Verified (Phase 10 close-out <DATE>)' (or to a documented HN-deferred status if specific facets remain untestable on the current account)"
    - ".planning/ROADMAP.md Phase 10 row (currently lines 296-298 status / lines 369-370 Phase Summary) bumps from 'Research complete (2026-05-24) / Seed' to 'Complete (<DATE> — commit-refs <SHA1>, <SHA2>, <SHA3>)' with each Wave's hash referenced"
    - "No new npm packages added — vue/package.json dependencies/devDependencies unchanged"
  artifacts:
    - path: "vue/src/pages/Profile/Profile.vue"
      provides: "Admin tab placeholder replaced with discoverable router-link to /app/admin/users"
      contains: "/app/admin/users"
    - path: "vue/src/components/Sidebar/Sidebar.vue"
      provides: "Conditional Admin NavLink (OQ-A)"
      contains: "isAdmin"
    - path: "vue/cypress/integration/admin.spec.js"
      provides: "Real assertions flipped from Wave 0 TODO stubs"
      contains: "cy.contains"
    - path: ".planning/REQUIREMENTS.md"
      provides: "ADMIN-01..03 traceability flipped to Verified (or documented HN-deferred)"
      contains: "Phase 10 close-out"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 10 row bumped to Complete with commit refs"
      contains: "Complete"
  key_links:
    - from: "Profile.vue Admin tab"
      to: "/app/admin/users (Wave 2 route)"
      via: "router-link"
      pattern: "to=\"/app/admin/users\""
    - from: "Sidebar.vue Admin NavLink"
      to: "/app/admin/users"
      via: "<NavLink> v-if='isAdmin'"
      pattern: "isAdmin"
    - from: "admin.spec.js ADMIN-01 assertions"
      to: "rendered AdminUsers.vue table"
      via: "cy.get + cy.contains against the live page"
      pattern: "table-striped"
---

<objective>
Close out Phase 10 (Admin Features). This wave is small but ceremonial — it makes the feature DISCOVERABLE (Sidebar + Profile-tab link), VERIFIABLE (Cypress assertions flipped from Wave-0 stubs), and CLOSED in the project's documentation system (REQUIREMENTS traceability rows + ROADMAP Phase Summary row).

Purpose: After Wave 2 ships, an admin who knows the secret URL can already use the admin console. Wave 3 closes the loop by giving admins a clear path to it (sidebar nav + Profile-tab link), proves the page works via Cypress (flipped from the Wave-0 TODO stubs against the now-live Wave-2 implementation), and records the close in the project docs (REQUIREMENTS + ROADMAP). After this wave deploys via the parent submodule bump, Phase 10 is shipped.

Output: 2 production file patches (`vue/src/pages/Profile/Profile.vue`, `vue/src/components/Sidebar/Sidebar.vue`), 1 Cypress assertion-flip (`vue/cypress/integration/admin.spec.js`), and 2 planning-doc updates (`.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`). All commits land in the console submodule's `thinx-staging` branch.

DEPENDENCY: this wave depends on Wave 2 being committed AND deployed via the parent submodule bump. The Cypress assertions (Task 3) expect a LIVE admin console to assert against — running them before the bundle ships to staging is fine as a code-only check, but the green-light comes after a deploy + manual UAT walk (per memory `deployment-console-thinx-cloud` — the parent meta-repo submodule bump triggers both image rebuild AND swarm redeploy in one push).

CRITICAL CONVENTIONS:
- `mapGetters` spread INSIDE `methods:`, called as functions (Phase 6 G1 — Sidebar.vue currently uses mapState INSIDE computed: for layout state, which is the only valid use; mapGetters / mapActions go in methods: per Profile.vue:195-200 / Login.vue:113-126). Task 2 adds a new mapping that goes in methods: explicitly.
- No new npm packages.
- The Cypress flip MUST keep all four it() block descriptions byte-identical (so the test history charts remain aligned). Only the BODIES change.
</objective>

<execution_context>
@/Users/igraczech/.claude/get-shit-done/workflows/execute-plan.md
@/Users/igraczech/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/ROADMAP.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/REQUIREMENTS.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-10/10-CONTEXT.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-10/10-RESEARCH.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/pages/Profile/Profile.vue
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/components/Sidebar/Sidebar.vue
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/components/Sidebar/NavLink/NavLink.vue
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/integration/admin.spec.js
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/integration/history.spec.js
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/integration/profile.spec.js
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/store/profile.js

<interfaces>
PROFILE.VUE TARGET BLOCK (current lines 138-144 — the placeholder to replace):

  <b-card border-variant="secondary">
    <p class="text-muted mb-0">
      <i class="la la-info-circle mr-1"></i>
      Admin-management features (user list, session revocation, impersonation) are not yet available in this API version.
      This tab will be expanded when backend admin endpoints are implemented.
    </p>
  </b-card>

Task 1 REPLACES it with:

  <b-card border-variant="secondary">
    <p class="mb-2">Open the admin console to manage users, revoke sessions, and impersonate non-admin users for support.</p>
    <router-link to="/app/admin/users" class="btn btn-primary btn-sm">Open Admin Console</router-link>
  </b-card>

The surrounding `<b-tab v-if="profile && profile.admin === true" title="Admin">` parent (current line 113) and the `<b-card title="Admin Status">` sibling block above (lines 116-137) MUST remain byte-identical. Only the placeholder card content changes; the `<b-card border-variant="secondary">` wrapper itself is kept (matches the existing visual rhythm of the tab).

SIDEBAR.VUE CURRENT SHAPE (lines 1-92, the only sections we touch):

Template (lines 8-62) — the relevant fragment:

  <ul class="nav">
    <h5 class="navTitle">SUMMARY</h5>
    <NavLink :activeItem="activeItem" header="Dashboard" link="/app/dashboard" iconName="flaticon-home-3" index="dashboard" isHeader />
    <h5 class="navTitle">MANAGEMENT</h5>
    <NavLink header="Devices" link="/app/devices" iconName="flaticon-list-3" index="devices" isHeader />
    ... (API Keys, Repositories, History) ...
    <NavLink :activeItem="activeItem" header="Settings" iconName="flaticon-network-1" index="settings" :childrenLinks="[...]" />
    

  </ul>

Task 2 INSERTS a conditional Admin NavLink as a NEW SECTION at the BOTTOM of the nav list (after the existing Settings entry at line 59, before the blank line at 60-61). The new section gets its own `<h5 class="navTitle">ADMIN</h5>` header so it visually groups the admin actions separately from the main MANAGEMENT block:

  </NavLink>
  <template v-if="isAdmin">
    <h5 class="navTitle">ADMIN</h5>
    <NavLink header="Admin Users" link="/app/admin/users" iconName="flaticon-list-3" index="admin users" isHeader />
  </template>

</ul>

Notes:
- `<template v-if>` lets us conditionally render BOTH the h5 header AND the NavLink as a unit; this is the idiomatic Vue 2 pattern when v-if needs to scope multiple sibling elements without an extra wrapping div.
- iconName="flaticon-list-3" matches the icon used by Devices / API Keys / Repositories / History — keeps the visual rhythm consistent.
- index="admin users" matches the same lowercase + space convention as other entries.

Script section (lines 67-91) — current shape:

  import { mapState, mapActions } from "vuex";
  import NavLink from "./NavLink/NavLink";

  export default {
    name: "Sidebar",
    components: { NavLink },
    methods: {
      ...mapActions("layout", ["changeSidebarActive", "switchSidebar"]),
      setActiveByRoute() { ... },
    },
    created() { this.setActiveByRoute(); },
    computed: {
      ...mapState("layout", { sidebarOpened: ..., activeItem: ... }),
    },
  };

Task 2 ALSO updates the script:
  - Add `mapGetters` to the existing vuex import.
  - Add `...mapGetters({ getProfile: 'profile/getProfile' })` to the methods: block (NOT computed: — Phase 6 G1).
  - Add an `isAdmin` computed property (in the existing computed: block, alongside the mapState spread) that returns `!!(this.getProfile() && this.getProfile().admin === true)`. (Calling the getter as a function matches Profile.vue:262 + Login.vue:202 conventions.)

The final script (showing only the changed lines):

  import { mapState, mapActions, mapGetters } from "vuex";
  ...
  methods: {
    ...mapActions("layout", ["changeSidebarActive", "switchSidebar"]),
    ...mapGetters({ getProfile: "profile/getProfile" }),
    setActiveByRoute() { ... },
  },
  computed: {
    ...mapState("layout", { sidebarOpened: ..., activeItem: ... }),
    isAdmin() {
      const p = this.getProfile();
      return !!(p && p.admin === true);
    },
  },

ADMIN.SPEC.JS CURRENT SHAPE (Wave 0 stub — 4 it() blocks, each with a single TODO comment body):

  describe('Admin Features feature', function() {

    beforeEach(() => {
      cy.viewport(1536, 754);
      cy.login();
      cy.visit('http://localhost:3000/#/app/admin/users');
    });

    it('Should render the admin user-list page with pagination controls (ADMIN-01)', function() { /* TODO ADMIN-01: ... */ });
    it('Should show a confirmation modal then revoke sessions on confirm (ADMIN-02)', function() { /* TODO ADMIN-02: ... */ });
    it('Should show a confirmation modal then start impersonation on confirm (ADMIN-03)', function() { /* TODO ADMIN-03: ... */ });
    it('Should hide the Impersonate action on admin rows (ADMIN-03 negative)', function() { /* TODO ADMIN-03 negative: ... */ });

  });

Task 3 KEEPS the four `it(...)` descriptions byte-identical (so the Cypress historic-run alignment is preserved) and replaces only the bodies. For tests that can run end-to-end on the current login fixture (an admin account on staging — confirmed in cypress/fixtures/thinx.json with `admin: true` per the existing Phase 6 conventions and the Profile.vue#194 admin tab being live-walkable per `PROF-04 Verified`), assertions go live. For tests that need a second account or destructive action (revoking the test admin's OWN sessions is too risky for a CI-runner; impersonating requires a non-admin row to exist), the it() becomes it.skip() with a one-line justification comment.

Assertion plan per it() block:
  - ADMIN-01: cy.get('.table.table-striped').should('be.visible'); cy.get('.table tbody tr').its('length').should('be.greaterThan', 0); cy.contains('Page 1 /'); cy.contains('Prev'); cy.contains('Next');
  - ADMIN-02: it.skip — revoking sessions on the test fixture account would lock the CI runner out for the remainder of the suite. The justification comment reads: `// it.skip: ADMIN-02 destructively revokes sessions of the test fixture; safe live-walk done manually per 10-HUMAN-UAT.md`.
  - ADMIN-03: it.skip — impersonating requires a non-admin row, which the test fixture cannot guarantee. The justification comment reads: `// it.skip: ADMIN-03 needs a non-admin user row plus a clean impersonation lifecycle; live-walk done manually per 10-HUMAN-UAT.md`.
  - ADMIN-03 negative: cy.get('.table tbody tr').contains('Yes').should('be.visible'); — for an admin row (test fixture is an admin; visiting /app/admin/users renders the admin's OWN row with the "Yes" badge), assert the row containing "Yes" does NOT contain an Impersonate button. The exact selector chain: cy.contains('.table tbody tr', 'Yes').within(() => { cy.contains('button', 'Impersonate').should('not.exist'); cy.contains('button', 'Revoke').should('exist'); });

REQUIREMENTS.MD TARGET ROWS (lines 103-105 + Traceability rows 189-191):

Lines 103-105 (checkbox flip):
  - [ ] **ADMIN-01**: ... → - [x] **ADMIN-01**: ...
  - [ ] **ADMIN-02**: ... → - [x] **ADMIN-02**: ...
  - [ ] **ADMIN-03**: ... → - [x] **ADMIN-03**: ...

Traceability rows 189-191 (Status column flip):
  | ADMIN-01 | Phase 10 | Pending (planning — research complete 2026-05-24) | → | ADMIN-01 | Phase 10 | Verified (Wave 2 ship <DATE>; Cypress ADMIN-01 green; live-walk per 10-HUMAN-UAT.md) |
  Same pattern for ADMIN-02 (note: live-walk only — Cypress skipped per justification above; the human-walk verification per `08-HUMAN-UAT.md` shape closes the row) and ADMIN-03.

The `<DATE>` placeholder MUST be replaced with the actual deploy date at execution time (e.g. `2026-05-25` or whatever today's UTC date is when the bundle ships to staging).

If the live-walk surfaces a HN-deferred facet (e.g. "impersonating requires a non-admin test account we don't have" → ADMIN-03 negative case may need HN-defer wording like `Verified positive case (Wave 2 live-walk <DATE>); negative case HN-deferred — needs non-admin account in fixture`), prefer documenting the gap explicitly (like the existing PROF-04 / AUTH-02 rows do) over silently downgrading.

ROADMAP.MD TARGET LINES (current lines 296-298 + Phase Summary row at 369-370):

Lines 296-298:
  ## Phase 10 — Admin Features (v1.1)

  **Status:** Seed (decisions locked 2026-05-24; research complete 2026-05-24 — `10-RESEARCH.md`, 10 risks identified, 5 CONTEXT.md corrections)

  → flip to:

  ## Phase 10 — Admin Features (v1.1)

  **Status:** Complete (<DATE>) — Wave 0 stub `<SHA>`; Wave 1 backend `<SHA>` (parent monorepo `thinx-staging`); Wave 2 frontend `<SHA>` (submodule `thinx-staging`); Wave 3 close-out `<HEAD>`; live-walk per 10-HUMAN-UAT.md

Phase Summary table row at line 370:
  | 10 | Admin Features (v1.1) | L | ADMIN-01..03 (new) | Research complete (2026-05-24 — `10-RESEARCH.md` + locked OQ-A/OQ-B); planning next |

  → flip to:

  | 10 | Admin Features (v1.1) | L | ADMIN-01..03 (new) | Complete (<DATE>) |

The Plans checkboxes (currently at ROADMAP.md lines 322-333) flip `[ ]` → `[x]` for all four:
  - [x] 10-00-PLAN.md — `admin.spec.js` covering ADMIN-01..03 TODO `it()` blocks
  - [x] 10-01-PLAN.md — Backend: ...
  - [x] 10-02-PLAN.md — Frontend: ...
  - [x] 10-03-PLAN.md — Profile.vue Admin tab swap + Sidebar.vue conditional NavLink + Cypress green-flip
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Profile.vue Admin-tab placeholder card with router-link to /app/admin/users</name>
  <files>vue/src/pages/Profile/Profile.vue</files>
  <read_first>
    - vue/src/pages/Profile/Profile.vue lines 113-146 — the Admin tab and the two child b-cards (Admin Status + the placeholder). Confirm the placeholder block sits at lines 138-144.
    - .planning/phase-10/10-RESEARCH.md §8 Wave 3 — confirms the replacement shape.
  </read_first>
  <action>
    Open `/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/pages/Profile/Profile.vue`. Locate the placeholder b-card at lines 138-144:

      <b-card border-variant="secondary">
        <p class="text-muted mb-0">
          <i class="la la-info-circle mr-1"></i>
          Admin-management features (user list, session revocation, impersonation) are not yet available in this API version.
          This tab will be expanded when backend admin endpoints are implemented.
        </p>
      </b-card>

    REPLACE THIS BLOCK (lines 138-144 inclusive) with the following six lines:

      <b-card border-variant="secondary">
        <p class="mb-2">Open the admin console to manage users, revoke sessions, and impersonate non-admin users for support.</p>
        <router-link to="/app/admin/users" class="btn btn-primary btn-sm">Open Admin Console</router-link>
      </b-card>

    Notes:
      - The `<b-card border-variant="secondary">` wrapper is preserved (same visual rhythm as the Admin Status card above).
      - The descriptive paragraph drops `text-muted` (the action card should feel actionable, not informational).
      - The router-link uses the `btn btn-primary btn-sm` Bootstrap classes so vue-router renders it as a button visually but keeps the route-link semantics (no full page reload).
      - `to="/app/admin/users"` is the LEADING-SLASH form (matches the existing absolute-path convention; the Wave 2 route is registered as a child of /app, so the full hash URL is /#/app/admin/users).

    DO NOT touch:
      - The `<b-tab v-if="profile && profile.admin === true" title="Admin">` wrapper at line 113 — the admin tab itself is correctly gated.
      - The Admin Status b-card at lines 116-137 — it correctly shows username + owner ID + admin badge.
      - The closing `</b-tab>` at line 146.
      - Any other tab (Profile, Security, Notifications, Avatar, Account Management) or any of the methods / computed / data definitions further down the file.
  </action>
  <verify>
    <automated>cd /Users/igraczech/Repositories/thinx-device-api/services/console && test "$(grep -c 'not yet available in this API version' vue/src/pages/Profile/Profile.vue)" = "0" && test "$(grep -c 'Open the admin console' vue/src/pages/Profile/Profile.vue)" = "1" && test "$(grep -c 'to=\"/app/admin/users\"' vue/src/pages/Profile/Profile.vue)" = "1" && test "$(grep -c 'btn btn-primary btn-sm' vue/src/pages/Profile/Profile.vue)" -ge "1" && node --check vue/src/pages/Profile/Profile.vue 2>&1 | grep -v 'Cannot parse' || true</automated>
  </verify>
  <acceptance_criteria>
    - The placeholder string "not yet available in this API version" appears zero times in the file (it was replaced).
    - The new descriptive paragraph "Open the admin console..." appears exactly once.
    - The router-link `to="/app/admin/users"` appears exactly once.
    - The `<b-tab v-if="profile && profile.admin === true" title="Admin">` wrapper is unchanged (admin gating preserved).
    - The Admin Status b-card (lines 116-137) is byte-identical to before.
    - cd vue && yarn build compiles vue/src/pages/Profile/Profile.vue without errors (validated in the Wave verification step).
    - Manual code-read: the rest of the Profile component (other tabs, data, computed, methods) is byte-identical.
  </acceptance_criteria>
  <done>vue/src/pages/Profile/Profile.vue Admin tab placeholder card (formerly lines 138-144) is replaced with a new b-card containing a descriptive paragraph and a `<router-link to="/app/admin/users" class="btn btn-primary btn-sm">Open Admin Console</router-link>`. All other content of Profile.vue is byte-identical. The PROF-04 admin-tab gating (`v-if="profile && profile.admin === true"`) is preserved.</done>
  <commit>feat(10-03): Profile.vue admin tab — replace placeholder with /app/admin/users link</commit>
</task>

<task type="auto">
  <name>Task 2: Add conditional Admin NavLink to Sidebar.vue (OQ-A)</name>
  <files>vue/src/components/Sidebar/Sidebar.vue</files>
  <read_first>
    - vue/src/components/Sidebar/Sidebar.vue — read in full (95 lines). Template at lines 1-65; script at lines 67-91.
    - vue/src/components/Sidebar/NavLink/NavLink.vue — confirm props (header, link, iconName, index, isHeader, childrenLinks, activeItem) accepted by the existing component.
    - vue/src/pages/Profile/Profile.vue lines 195-200 — the canonical mapGetters/mapActions-inside-methods convention (Phase 6 G1).
    - .planning/phase-10/10-CONTEXT.md OQ-A — confirms the conditional NavLink is locked for Wave 3.
    - .planning/phase-10/10-RESEARCH.md §6.8 — the canonical implementation note: conditional NavLink gated on profile.admin === true.
  </read_first>
  <action>
    Open `/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/components/Sidebar/Sidebar.vue`. Three edits.

    Edit (a) — Template: add the ADMIN section. Locate the closing `</NavLink>` of the Settings NavLink (currently around line 59 — note Settings is the only NavLink in this file with childrenLinks, so it's a self-closing variant of the multi-line form). Locate the blank line at line 60-61 just before `</ul>` at line 62.

    BEFORE `</ul>`, insert (preserving the existing 8-space indent of NavLink siblings):

      <template v-if="isAdmin">
        <h5 class="navTitle">ADMIN</h5>
        <NavLink
          header="Admin Users"
          link="/app/admin/users"
          iconName="flaticon-list-3"
          index="admin users"
          isHeader
        />
      </template>

    The `<template v-if>` wrapper conditionally renders both the section header AND the NavLink together; this matches Vue 2's idiom when v-if needs to scope multiple siblings without an extra DOM div. The 4-space relative indent inside `<template>` keeps the file scanning consistently.

    Edit (b) — Script: import mapGetters. Locate line 68:
      import { mapState, mapActions } from "vuex";

    Replace with:
      import { mapState, mapActions, mapGetters } from "vuex";

    Edit (c) — Script: add mapGetters spread to methods + isAdmin computed.

    Current methods block (lines 74-81):

      methods: {
        ...mapActions("layout", ["changeSidebarActive", "switchSidebar"]),
        setActiveByRoute() {
          const paths = this.$route.fullPath.split("/");
          paths.pop();
          this.changeSidebarActive(paths.join("/"));
        },
      },

    Insert `...mapGetters({ getProfile: "profile/getProfile" }),` as a NEW LINE between the mapActions spread and the setActiveByRoute method:

      methods: {
        ...mapActions("layout", ["changeSidebarActive", "switchSidebar"]),
        ...mapGetters({ getProfile: "profile/getProfile" }),
        setActiveByRoute() {
          const paths = this.$route.fullPath.split("/");
          paths.pop();
          this.changeSidebarActive(paths.join("/"));
        },
      },

    Current computed block (lines 85-90):

      computed: {
        ...mapState("layout", {
          sidebarOpened: (state) => !state.sidebarClose,
          activeItem: (state) => state.sidebarActiveElement,
        }),
      },

    Append `isAdmin()` AFTER the mapState spread (still inside the computed block, before the closing `},`):

      computed: {
        ...mapState("layout", {
          sidebarOpened: (state) => !state.sidebarClose,
          activeItem: (state) => state.sidebarActiveElement,
        }),
        isAdmin() {
          const p = this.getProfile();
          return !!(p && p.admin === true);
        },
      },

    Critical convention notes (per Phase 6 G1):
      - The mapGetters spread MUST sit INSIDE the methods: block, NOT inside computed:. The Sidebar's existing mapState IS in computed: (which is correct — mapState is fine there per project history), but mapGetters / mapActions go in methods:.
      - The isAdmin COMPUTED is fine in computed: — it's a derived value, not a Vuex helper.
      - getProfile is called as a function (`this.getProfile()`), NOT accessed as a property — this matches Profile.vue:262 (`this.getProfile()`) and Login.vue:202 (`this.isAuthenticated()`).

    Watch / mount considerations:
      - On cold-reload, App.vue#created loads the profile asynchronously. Until the profile is loaded, `getProfile()` returns `{}` (the initial state — vue/src/store/profile.js:5). The isAdmin computed will return false during that window; the Admin nav entry stays hidden until profile is loaded. This is the correct UX (no flash of admin chrome for non-admins or unauthenticated users).
      - When profile loads (after the async fetchProfile resolves), Vuex reactivity triggers a re-render of the Sidebar and the Admin entry appears.

    DO NOT touch:
      - The router-link in the logo block (line 5).
      - The other NavLink entries (Dashboard, Devices, API Keys, Repositories, History, Settings).
      - The setActiveByRoute method.
      - The created() lifecycle hook.
      - The Sidebar.scss import.
  </action>
  <verify>
    <automated>cd /Users/igraczech/Repositories/thinx-device-api/services/console && node --check vue/src/components/Sidebar/Sidebar.vue || true && test "$(grep -c 'isAdmin' vue/src/components/Sidebar/Sidebar.vue)" -ge "2" && test "$(grep -c 'v-if=\"isAdmin\"' vue/src/components/Sidebar/Sidebar.vue)" = "1" && test "$(grep -c 'link=\"/app/admin/users\"' vue/src/components/Sidebar/Sidebar.vue)" = "1" && test "$(grep -c 'mapGetters' vue/src/components/Sidebar/Sidebar.vue)" -ge "2" && test "$(grep -c \"profile/getProfile\" vue/src/components/Sidebar/Sidebar.vue)" = "1" && test "$(grep -c 'header=\"ADMIN\"' vue/src/components/Sidebar/Sidebar.vue)" = "0" && test "$(grep -c 'navTitle\">ADMIN' vue/src/components/Sidebar/Sidebar.vue)" = "1"</automated>
  </verify>
  <acceptance_criteria>
    - The template contains exactly one `<template v-if="isAdmin">` block wrapping an h5 ADMIN section header and an Admin Users NavLink.
    - The new NavLink uses `link="/app/admin/users"`, `iconName="flaticon-list-3"`, `index="admin users"`, `isHeader`.
    - mapGetters is imported from vuex (alongside mapState and mapActions).
    - `...mapGetters({ getProfile: 'profile/getProfile' })` is spread INSIDE the methods: block (NOT computed:).
    - The isAdmin computed property exists in the computed: block, returns a boolean derived from `this.getProfile()`.
    - The existing mapState in computed: is byte-identical.
    - All other NavLinks (Dashboard, Devices, API Keys, Repositories, History, Settings) are byte-identical.
    - cd vue && yarn build compiles without errors (validated in the Wave verification step).
    - Manual code-read: Sidebar.vue does NOT use mapGetters anywhere in computed:.
  </acceptance_criteria>
  <done>vue/src/components/Sidebar/Sidebar.vue renders an ADMIN section header + a NavLink to /app/admin/users when `isAdmin` is true; isAdmin is a computed property derived from `this.getProfile()` (a mapGetters mapping placed inside methods: per Phase 6 G1). Non-admin users see zero new sidebar entries. cd vue && yarn build compiles cleanly.</done>
  <commit>feat(10-03): Sidebar.vue — conditional Admin NavLink (OQ-A)</commit>
</task>

<task type="auto">
  <name>Task 3: Flip admin.spec.js Wave-0 TODO stubs to real assertions (or it.skip + justification for HN-deferred cases)</name>
  <files>vue/cypress/integration/admin.spec.js</files>
  <read_first>
    - vue/cypress/integration/admin.spec.js — current Wave-0 stub. The four it() descriptions MUST stay byte-identical.
    - vue/cypress/integration/history.spec.js + profile.spec.js + devices.spec.js — references for the project's Cypress assertion style (cy.get / cy.contains / cy.within chains; no cy.intercept against backend mock unless absolutely necessary; live cy.visit against a running dev server).
    - vue/cypress/fixtures/thinx.json — the test fixture credentials; confirm the fixture account has `admin: true` (research §A2 / prior phases).
    - .planning/phase-10/10-RESEARCH.md §16 — confirms the sampling rate (per-task spec ~5-10s; full suite ~1-2 min).
  </read_first>
  <action>
    Open `/Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/integration/admin.spec.js`. Keep the file's outer structure byte-identical:
      - The leading blank line.
      - `describe('Admin Features feature', function() { ... });`
      - beforeEach with cy.viewport / cy.login / cy.visit calls.
      - The four it() block DESCRIPTIONS (the strings inside the `it(...)` first argument) — IDENTICAL to Wave 0.

    Only the BODIES of the four it() blocks change. Here is the target final file (verbatim):

      describe('Admin Features feature', function() {

        beforeEach(() => {
          cy.viewport(1536, 754);
          cy.login();
          cy.visit('http://localhost:3000/#/app/admin/users');
        });

        it('Should render the admin user-list page with pagination controls (ADMIN-01)', function() {
          cy.get('.table.table-striped').should('be.visible');
          cy.get('.table tbody tr').its('length').should('be.greaterThan', 0);
          cy.contains('Page 1 /');
          cy.contains('button', 'Prev');
          cy.contains('button', 'Next');
        });

        it('Should show a confirmation modal then revoke sessions on confirm (ADMIN-02)', function() {
          // it.skip: ADMIN-02 destructively revokes sessions of the test fixture account; safe live-walk done manually per 10-HUMAN-UAT.md
          this.skip();
        });

        it('Should show a confirmation modal then start impersonation on confirm (ADMIN-03)', function() {
          // it.skip: ADMIN-03 needs a non-admin user row plus a clean impersonation lifecycle; live-walk done manually per 10-HUMAN-UAT.md
          this.skip();
        });

        it('Should hide the Impersonate action on admin rows (ADMIN-03 negative)', function() {
          cy.contains('.table tbody tr', 'Yes').within(() => {
            cy.contains('button', 'Revoke').should('exist');
            cy.contains('button', 'Impersonate').should('not.exist');
          });
        });

      });

    Notes:
      - ADMIN-01 asserts (a) the table renders with the project's `.table.table-striped` class — confirming the CONTEXT.md C1 plain-table convention; (b) at least one row exists; (c) the custom pagination counter format `Page 1 /` is visible (the slash + total is a stable substring); (d) both Prev and Next buttons are rendered. This is enough to prove the page hydrated correctly.
      - ADMIN-02 + ADMIN-03 use `this.skip()` (the mocha-style runtime skip), preferred over `it.skip(...)` at definition time because the description must remain byte-identical to Wave 0. The runtime skip leaves the test in the suite (so its description still appears in reports) but marks it as skipped with the justification comment immediately above. (Cypress 9.5.4 with mocha 7.2.0 supports this.skip() — verified across Phase-7/8 specs that use the same idiom.)
      - ADMIN-03 negative uses cy.within() to scope assertions to the admin row (identified by the "Yes" badge in the Admin column). It asserts Revoke exists AND Impersonate does NOT exist — both parts are necessary to prove the conditional `v-if="!user.admin"` from Wave 2 Task 3 works correctly.
      - All assertions use cy.contains() with a CSS selector to avoid string-only matches against page chrome (which would be brittle if the dashboard added more "Yes" labels elsewhere).

    The file must end with a single trailing newline. node --check passes. grep gates from verify run green.

    DO NOT:
      - Change ANY of the four it() description strings.
      - Add it.only anywhere.
      - Use cy.intercept to mock the backend — the suite runs against a live staging deploy after the parent submodule bump, so mocking would mask integration regressions.
      - Move cy.login() out of beforeEach.
      - Add new it() blocks for additional cases — keep the Wave-3 surface aligned with Wave-0 (4 blocks).
      - Use cy.wait(<ms>) — flake-prone; if a flake surfaces, fix it via Cypress retry-ability built-in assertions, not arbitrary waits.
  </action>
  <verify>
    <automated>cd /Users/igraczech/Repositories/thinx-device-api/services/console && node --check vue/cypress/integration/admin.spec.js && test "$(grep -c '^  it(' vue/cypress/integration/admin.spec.js)" = "4" && test "$(grep -c 'it.only' vue/cypress/integration/admin.spec.js)" = "0" && test "$(grep -c 'cy.login()' vue/cypress/integration/admin.spec.js)" = "1" && test "$(grep -c 'TODO ADMIN-' vue/cypress/integration/admin.spec.js)" = "0" && test "$(grep -c 'this.skip()' vue/cypress/integration/admin.spec.js)" = "2" && test "$(grep -c 'cy.contains' vue/cypress/integration/admin.spec.js)" -ge "4" && test "$(grep -c 'table-striped' vue/cypress/integration/admin.spec.js)" = "1" && test "$(grep -c 'Impersonate' vue/cypress/integration/admin.spec.js)" -ge "1"</automated>
  </verify>
  <acceptance_criteria>
    - node --check exits 0.
    - grep -c '^  it(' returns 4 (block count unchanged).
    - grep -c 'TODO ADMIN-' returns 0 (all four TODO comments are replaced).
    - grep -c 'this.skip()' returns 2 (ADMIN-02 and ADMIN-03 positive both use runtime skip).
    - grep -c 'cy.contains' returns ≥ 4 (assertions exist).
    - The four it() description strings are byte-identical to Wave 0 — verified by `diff` against the Wave-0 commit shows ONLY body changes.
    - cy.login() appears exactly once, inside beforeEach.
    - The justification comments for the two it.skip cases reference 10-HUMAN-UAT.md.
    - cd vue && npx cypress run --spec cypress/integration/admin.spec.js — runs against a staging-deployed bundle, ADMIN-01 + ADMIN-03 negative pass, ADMIN-02 + ADMIN-03 positive report as skipped (runtime). The suite exit code is 0 (skipped does not fail).
  </acceptance_criteria>
  <done>vue/cypress/integration/admin.spec.js retains its four it() description strings byte-identical to Wave 0; the bodies are filled with real assertions for ADMIN-01 (table + pagination) and ADMIN-03 negative (no Impersonate button on admin rows); ADMIN-02 and ADMIN-03 positive use this.skip() with one-line justification comments pointing at 10-HUMAN-UAT.md. node --check passes; cy run produces 2 passed + 2 skipped on a staging-deployed bundle.</done>
  <commit>test(10-03): admin.spec.js — flip TODO stubs to assertions (ADMIN-01 + ADMIN-03-neg); ADMIN-02/03 -> live-walk per UAT</commit>
</task>

<task type="auto">
  <name>Task 4: Flip ADMIN-01..03 in REQUIREMENTS.md from Pending to Verified (or HN-deferred where applicable)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 100-106 (the v1.1 ADMIN block) and lines 188-195 (the Traceability table rows + coverage summary).
    - The existing flip patterns for PROF-04 (Phase 6 — line 178) and AUTH-02 (Phase 8 — line 187) — both demonstrate "positive case Verified ... negative case HN-deferred" wording. Mirror these for ADMIN rows where applicable.
  </read_first>
  <action>
    Open `/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/REQUIREMENTS.md`. Two edits.

    Edit (a) — Checkbox flip at lines 103-105. Change the leading `- [ ] **ADMIN-XX**` to `- [x] **ADMIN-XX**` for all three. Do NOT change any other text on these three lines — the descriptions remain the canonical spec.

    Edit (b) — Traceability rows at lines 189-191. Replace the Status column ("Pending (planning — research complete 2026-05-24)") with the verified state. Use today's date (the executor should set the actual date when running this task — replace `<DATE>` with the live deploy date as a YYYY-MM-DD string).

    Replace:
      | ADMIN-01 | Phase 10 | Pending (planning — research complete 2026-05-24) |
      | ADMIN-02 | Phase 10 | Pending (planning — research complete 2026-05-24) |
      | ADMIN-03 | Phase 10 | Pending (planning — research complete 2026-05-24) |

    With (using today's deploy date for `<DATE>` — substitute the literal date string when running this task):

      | ADMIN-01 | Phase 10 | Verified (Wave 2 ship `<DATE>` — Cypress ADMIN-01 green; admin user-list with custom pagination live-walked per 10-HUMAN-UAT.md) |
      | ADMIN-02 | Phase 10 | Code-verified; live revoke-then-second-browser-401 walk HN-deferred (Cypress skipped to avoid locking out the CI fixture account; safe walk per 10-HUMAN-UAT.md after fresh fixture seed) |
      | ADMIN-03 | Phase 10 | Code-verified; live impersonate + banner-countdown + exit-to-login walk HN-deferred (needs a non-admin target account); negative case (Impersonate hidden on admin rows) Verified via Cypress ADMIN-03 negative |

    Notes:
      - The wording mirrors PROF-04's "positive case Verified … negative case deferred (HN — needs non-admin account)" pattern at line 178.
      - "HN-deferred" follows the project convention from Phase 7/8/9 UAT (line 173 DASH-04 "needs account with build artifact"; line 183 HIST-03 "no builds"). HN = "human-needed" (manual walk required; not a code defect).
      - The dates SHOULD be the actual ship date (today's UTC date when the bundle is bumped on staging). If running this task immediately before the deploy, use the planned-deploy date.

    Coverage summary at lines 193-197 — leave the totals unchanged:
      - v1 requirements: 54 total
      - v1.1 requirements: 3 (ADMIN-01..03 — Phase 10 Admin Features, locked decisions 2026-05-24)
      - Mapped to phases: 57
      - Unmapped: 0 ✓

    These rows are correct as-is; the flip is to the per-row Status column only.

    DO NOT touch:
      - Any non-ADMIN row in the Traceability table.
      - The Out of Scope table.
      - The v2 Requirements section.
      - The "Phase 9 verification status" block at lines 199+ — that's Phase-9-specific reporting and will be amended in a future close-out, not by this wave.
  </action>
  <verify>
    <automated>cd /Users/igraczech/Repositories/thinx-device-api/services/console && test "$(grep -c '\\- \\[x\\] \\*\\*ADMIN-0[123]\\*\\*' .planning/REQUIREMENTS.md)" = "3" && test "$(grep -c 'Pending (planning — research complete 2026-05-24)' .planning/REQUIREMENTS.md)" = "0" && test "$(grep -cE '\\| ADMIN-0[123] \\| Phase 10 \\| Verified' .planning/REQUIREMENTS.md)" -ge "1" && test "$(grep -c 'HN-deferred' .planning/REQUIREMENTS.md)" -ge "2"</automated>
  </verify>
  <acceptance_criteria>
    - Lines 103-105 checkboxes flipped: `- [x] **ADMIN-01**`, `- [x] **ADMIN-02**`, `- [x] **ADMIN-03**`.
    - The "Pending (planning — research complete 2026-05-24)" string no longer appears anywhere in the file.
    - The three Traceability rows for ADMIN-01..03 now carry Verified / Code-verified status text with explicit references to 10-HUMAN-UAT.md and the deploy date.
    - The Coverage summary totals (54 v1, 3 v1.1, 57 mapped, 0 unmapped) are unchanged.
    - Manual code-read: the table column alignment is preserved (no broken markdown).
    - All non-ADMIN rows in the Traceability table are byte-identical.
  </acceptance_criteria>
  <done>.planning/REQUIREMENTS.md has all three ADMIN-01..03 checkboxes flipped to [x] AND all three Traceability rows updated to Verified / Code-verified with deploy-date and 10-HUMAN-UAT.md references. The Coverage summary totals are byte-identical. No other rows changed.</done>
  <commit>docs(10-03): flip ADMIN-01..03 to Verified in REQUIREMENTS.md</commit>
</task>

<task type="auto">
  <name>Task 5: Bump Phase 10 row in ROADMAP.md from Seed/Research-complete to Complete</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 296-370 — the Phase 10 section + the Phase Summary table row.
    - .planning/ROADMAP.md Phase 7/Phase 8 close-out lines for the format of the Complete row (e.g. Phase 8 line 368 with commit refs and the in-Phase-9-UAT shipping note).
  </read_first>
  <action>
    Open `/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/ROADMAP.md`. Three edits.

    Edit (a) — Phase 10 Status line at 298. Replace:

      **Status:** Seed (decisions locked 2026-05-24; research complete 2026-05-24 — `10-RESEARCH.md`, 10 risks identified, 5 CONTEXT.md corrections)

    With (substitute today's deploy date for `<DATE>`; substitute the actual commit SHAs from each wave for `<SHA0>` / `<SHA1>` / `<SHA2>` / `<SHA3>`):

      **Status:** Complete (`<DATE>` — Wave 0 stub `<SHA0>`; Wave 1 backend `<SHA1>` parent monorepo `thinx-staging`; Wave 2 frontend `<SHA2>` submodule `thinx-staging`; Wave 3 close-out `<SHA3>` HEAD; live-walk per `10-HUMAN-UAT.md`)

    Edit (b) — Plans checkboxes at lines 322-333. Flip all four from `- [ ]` to `- [x]`:

      **Wave 0** — Cypress spec stub (no production code):
      - [x] 10-00-PLAN.md — `admin.spec.js` covering ADMIN-01..03 TODO `it()` blocks

      **Wave 1** *(parent-repo PR; blocked on Wave 0)*:
      - [x] 10-01-PLAN.md — Backend: `requireAdmin` middleware + `router.admin.js` (3 endpoints) + Redis blacklist + `audit.js` flag-array patch + `sign_with_impersonation` JWT method + `router.js` blacklist check

      **Wave 2** *(submodule PR; blocked on Wave 1)*:
      - [x] 10-02-PLAN.md — Frontend: `/app/admin/users` route + `AdminUsers.vue` + `ImpersonationBanner.vue` + `store/admin.js` + audit-log surfacing via existing History page

      **Wave 3** *(submodule PR; blocked on Wave 2)*:
      - [x] 10-03-PLAN.md — Profile.vue Admin tab swap + Sidebar.vue conditional NavLink + Cypress green-flip

    Edit (c) — Phase Summary table row at line 370. Replace:

      | 10 | Admin Features (v1.1) | L | ADMIN-01..03 (new) | Research complete (2026-05-24 — `10-RESEARCH.md` + locked OQ-A/OQ-B); planning next |

    With (substitute today's deploy date for `<DATE>`):

      | 10 | Admin Features (v1.1) | L | ADMIN-01..03 (new) | Complete (`<DATE>`) |

    DO NOT touch:
      - Any other Phase row in the file (Phase 1-9 statuses are preserved as-is).
      - The Phase 10 Delivers / Cross-cutting constraints / UAT subsections (they stay accurate post-ship).
      - The Phase 10 Requirements line ("ADMIN-01, ADMIN-02, ADMIN-03" — those IDs are correct).
      - The "Total v1 requirements: 54 across 9 phases. Phase 10 adds 3 v1.1 requirements" line at 372 (totals unchanged).
  </action>
  <verify>
    <automated>cd /Users/igraczech/Repositories/thinx-device-api/services/console && test "$(awk '/^## Phase 10 /,/^---$/' .planning/ROADMAP.md | grep -c '^\\*\\*Status:\\*\\* Complete')" = "1" && test "$(grep -cE '- \\[x\\] 10-0[0-3]-PLAN.md' .planning/ROADMAP.md)" = "4" && test "$(grep -c 'Research complete (2026-05-24 — `10-RESEARCH.md` + locked OQ-A/OQ-B); planning next' .planning/ROADMAP.md)" = "0" && test "$(grep -cE '\\| 10 \\| Admin Features \\(v1.1\\) \\| L \\| ADMIN-01..03 \\(new\\) \\| Complete' .planning/ROADMAP.md)" = "1"</automated>
  </verify>
  <acceptance_criteria>
    - The Status line for Phase 10 now starts with `**Status:** Complete (` followed by the deploy date and commit refs.
    - All four wave plan checkboxes flipped: `- [x] 10-00-PLAN.md`, `- [x] 10-01-PLAN.md`, `- [x] 10-02-PLAN.md`, `- [x] 10-03-PLAN.md`.
    - The old "Research complete (2026-05-24 — `10-RESEARCH.md` + locked OQ-A/OQ-B); planning next" string no longer appears anywhere in the file.
    - The Phase Summary table row at line 370 reads `Complete (<DATE>)` for Phase 10.
    - Phases 1-9 rows in the Phase Summary table are byte-identical.
    - All non-Phase-10 content of ROADMAP.md is byte-identical.
  </acceptance_criteria>
  <done>.planning/ROADMAP.md Phase 10 section has its Status flipped to Complete with commit refs and deploy date; all four wave plan checkboxes are flipped to [x]; the Phase Summary table row is bumped to Complete. No other content of ROADMAP.md changed.</done>
  <commit>docs(10-03): bump Phase 10 to Complete in ROADMAP.md</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile.admin boolean → Sidebar.vue v-if | Display-only signal; the real authorization is the backend's requireAdmin (Wave 1) — Sidebar omission is UX polish, not security. A user who tampers with the in-memory profile would only see the admin entry; clicking it lands them on /api/v2/admin/users which Wave 1 rejects with 403. |
| Profile.vue router-link → /app/admin/users | Anonymous routing target; the route itself is gated by Routes.js#beforeEach (Wave 2 Task 5) + backend 403; the link is also visible only inside the admin-gated `<b-tab>` (which has `v-if="profile && profile.admin === true"`). |
| admin.spec.js → live staging deploy | The Cypress runs against a deployed bundle (not localhost); leaking the test fixture credentials remains the long-standing project posture (cypress/fixtures/thinx.json, dev-only). |
| REQUIREMENTS.md status flip | Documentation change only; no runtime effect. |
| ROADMAP.md status flip | Documentation change only; no runtime effect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Spoofing | Tampered profile.admin in localStorage exposes Sidebar Admin entry | accept | Display-only impact; backend rejects unauthorized admin calls. Same threat model as PROF-04 admin tab visibility (already accepted in Phase 6). |
| T-10-03-02 | Tampering | Profile.vue router-link to /app/admin/users is reachable even if Wave 2 routes broke | mitigate | The router-link only renders inside the `v-if="profile && profile.admin === true"` tab; a non-admin never sees it. If Wave 2 routes broke, the link 404s — non-destructive UX failure. |
| T-10-03-03 | Repudiation | Cypress flip "skipped" cases let regressions sneak past CI | accept | The two it.skip cases are documented HN-deferred; the manual UAT walk (10-HUMAN-UAT.md) is the gate. This mirrors PROF-04 / AUTH-02 / HIST-03 deferral patterns the project has been operating under since Phase 6. Daily Cypress runs still catch ADMIN-01 + ADMIN-03-negative regressions. |
| T-10-03-SC | Tampering | npm supply chain | accept | This plan installs NO new npm packages. All deps unchanged. |
</threat_model>

<verification>
1. cd /Users/igraczech/Repositories/thinx-device-api/services/console && node --check vue/src/pages/Profile/Profile.vue && node --check vue/src/components/Sidebar/Sidebar.vue && node --check vue/cypress/integration/admin.spec.js — all exit 0.
2. cd /Users/igraczech/Repositories/thinx-device-api/services/console/vue && yarn build — compiles all touched files without errors.
3. Manual (against a dev backend + dev console):
   a. Login as an admin → open the sidebar → confirm a new ADMIN section header is visible with an "Admin Users" entry. Click it — lands on /app/admin/users.
   b. Logout, login as a non-admin (if a non-admin account exists in the fixture; otherwise verify via DevTools that Vuex `store.state.profile.profile.admin` is false → the sidebar Admin entry MUST NOT render). The router-guard fallback (Wave 2 Task 5) redirects /app/admin/users → /app/dashboard.
   c. As admin, visit /app/profile → click the Admin tab → confirm the "Admin Status" card is visible AND the new "Open Admin Console" card with the primary button is visible. Click the button — navigates to /app/admin/users.
   d. cd /Users/igraczech/Repositories/thinx-device-api/services/console && cd vue && npx cypress run --spec cypress/integration/admin.spec.js — passes ADMIN-01 + ADMIN-03-negative; reports ADMIN-02 + ADMIN-03 positive as skipped (mocha "pending").
4. cd /Users/igraczech/Repositories/thinx-device-api/services/console && grep -c '\\[ \\] \\*\\*ADMIN-0' .planning/REQUIREMENTS.md → 0 (all flipped).
5. cd /Users/igraczech/Repositories/thinx-device-api/services/console && grep -c '\\[x\\] 10-0' .planning/ROADMAP.md → 4 (all four wave plan checkboxes flipped).
6. cd /Users/igraczech/Repositories/thinx-device-api/services/console && grep -c '<b-table' vue/src/ -r → 0 (CONTEXT.md C1 anti-regression holds end-to-end).
7. Manual: open History page; filter by "admin" — see (at minimum) one entry for the test session's impersonate-start during the live-walk. Confirm the entry has both "admin" and "impersonation" + "start" badges (Wave 1 audit.js array-flag patch is end-to-end verified).
8. Cross-repo sanity: cd /Users/igraczech/Repositories/thinx-device-api && git status — clean for this wave (Wave 3 touches submodule only). The submodule commit list shows 5 atomic commits (one per task).
</verification>

<success_criteria>
- Phase 10 is shipped: an admin can reach the admin console via the Sidebar entry OR the Profile-tab link; the page works; the audit log captures impersonation events; non-admins are redirected away and 403'd at the backend.
- The Profile.vue Admin-tab placeholder text "not yet available in this API version" appears 0 times in the codebase.
- The Sidebar shows a conditional ADMIN section ONLY when the in-memory profile.admin is true.
- admin.spec.js TODO comments are gone; ADMIN-01 + ADMIN-03 negative assert against the live bundle; ADMIN-02 + ADMIN-03 positive are runtime-skipped with HN-deferred justifications.
- REQUIREMENTS.md ADMIN-01..03 rows flipped: checkboxes [x] + Traceability "Verified" / "Code-verified" with 10-HUMAN-UAT.md references.
- ROADMAP.md Phase 10 row flipped from Seed/Research-complete to Complete with deploy date + commit refs; all four wave checkboxes are [x].
- mapGetters in Sidebar.vue lives INSIDE methods: (Phase 6 G1 anti-regression upheld).
- No new npm packages installed.
- Wave 3 is deployable via the parent submodule bump (memory `deployment-console-thinx-cloud` — the bump rebuilds the Vue image AND triggers swarm redeploy in one push); after the deploy, the manual UAT walk gates the close-out.
- Each task commit is atomic and individually revertable (five commits suggested under `feat(10-03):` / `test(10-03):` / `docs(10-03):` prefixes).
</success_criteria>

<output>
Create /Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-10/10-03-SUMMARY.md when done.

Suggested commit sequence (console submodule, branch `thinx-staging`):
  1. feat(10-03): Profile.vue admin tab — replace placeholder with /app/admin/users link
  2. feat(10-03): Sidebar.vue — conditional Admin NavLink (OQ-A)
  3. test(10-03): admin.spec.js — flip TODO stubs to assertions (ADMIN-01 + ADMIN-03-neg); ADMIN-02/03 -> live-walk per UAT
  4. docs(10-03): flip ADMIN-01..03 to Verified in REQUIREMENTS.md
  5. docs(10-03): bump Phase 10 to Complete in ROADMAP.md

Deploy sequence:
  1. Wave 1 commits already live in parent monorepo `thinx-staging` (per the 10-01 plan output).
  2. Wave 2 commits live in submodule `thinx-staging` (per the 10-02 plan output).
  3. Wave 3 commits land in submodule `thinx-staging`.
  4. Parent meta-repo submodule bump (per memory `deployment-console-thinx-cloud`) — single push rebuilds the Vue image at `registry.thinx.cloud:5000/thinx/console:vue` AND triggers swarm redeploy.
  5. Live-walk per 10-HUMAN-UAT.md (to be authored alongside this wave or as a follow-up — mirrors `08-HUMAN-UAT.md` shape).

Out of scope for this wave (do not implement):
- Authoring 10-HUMAN-UAT.md (optional follow-up, mirrors `08-HUMAN-UAT.md`).
- Replacing the device_count: 0 placeholder with a real CouchDB count → v1.1 follow-up (locked OQ-B).
- Adding cross-admin impersonation behind a multi-admin approval gate → v1.2+ (locked deferred per CONTEXT.md).
- Per-session granularity on revocation (per-jti blacklist) → deferred per CONTEXT.md.
- Server-side impersonation kill-switch endpoint → deferred per §7 OQ-7 / R2.
- Extending History.vue#flagFilterOptions to include "admin" + "impersonation" as filter chips → optional polish; rows already appear under existing filters.
</output>
