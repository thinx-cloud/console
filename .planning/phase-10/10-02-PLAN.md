---
phase: 10-admin-features
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
repo: console-submodule
files_modified:
  - vue/src/store/admin.js
  - vue/src/store/index.js
  - vue/src/pages/AdminUsers/AdminUsers.vue
  - vue/src/components/ImpersonationBanner/ImpersonationBanner.vue
  - vue/src/Routes.js
  - vue/src/components/Layout/Layout.vue
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
tags:
  - vue
  - vuex
  - vue-router
  - frontend
  - admin
  - bootstrap-vue
  - submodule

must_haves:
  truths:
    - "vue/src/store/admin.js exists as a namespaced Vuex module with state.users, mutation setUsers, actions fetchUsers + revokeSession + impersonate, getter getUsers"
    - "vue/src/store/index.js registers the admin module alongside the existing modules"
    - "vue/src/pages/AdminUsers/AdminUsers.vue uses a plain <table class=\"table table-striped\"> with v-for over pagedUsers (NOT <b-table>) and custom Prev/Next pagination (NOT <b-pagination>) — per CONTEXT.md correction C1"
    - "AdminUsers.vue per-row actions: Revoke (always visible) + Impersonate (hidden when row.admin === true) — both invoke $bvModal.msgBoxConfirm before dispatching the Vuex action"
    - "AdminUsers.vue uses mapGetters + mapActions spread INSIDE methods: — NEVER in computed: (Phase 6 G1 anti-regression)"
    - "vue/src/Routes.js adds an /admin/users child under the /app parent with component AdminUsersPage"
    - "vue/src/Routes.js#beforeEach guard extended with ADMIN_PATHS = ['/app/admin'] prefix-match check that next('/app/dashboard') for non-admins (best-effort via in-memory store; backend 403 is the security boundary) — per OQ-4"
    - "vue/src/components/ImpersonationBanner/ImpersonationBanner.vue mounts above <router-view> in Layout.vue, decodes localStorage.accessToken via VueJwtDecode on created() + on $route changes, renders only when payload.impersonator_owner is present, shows 🎭 + target username + MM:SS countdown + Exit button"
    - "ImpersonationBanner.vue setInterval ticker runs at 1000ms cadence (per OQ-5) and is cleared in beforeDestroy (per R5)"
    - "ImpersonationBanner.vue Exit button dispatches auth/clearSession + $router.push('/login') — matches the existing Header.vue logout flow exactly (per OQ-7); no server-side call"
    - "AdminUsers.vue impersonate flow rewrites localStorage.accessToken with the new JWT, removes localStorage.refreshToken (impersonation has no refresh), commits auth/setAccessToken, dispatches auth/scheduleExpiry, and pushes /app/dashboard so the banner is visible"
    - "No new npm packages added — vue/package.json dependencies/devDependencies unchanged"
  artifacts:
    - path: "vue/src/store/admin.js"
      provides: "Vuex module for admin actions (fetchUsers, revokeSession, impersonate)"
      contains: "namespaced: true"
    - path: "vue/src/store/index.js"
      provides: "admin module registration"
      contains: "admin"
    - path: "vue/src/pages/AdminUsers/AdminUsers.vue"
      provides: "User list page with Revoke + Impersonate per-row actions (plain <table>, custom pagination)"
      contains: "pagedUsers"
    - path: "vue/src/components/ImpersonationBanner/ImpersonationBanner.vue"
      provides: "Sticky banner with MM:SS countdown + Exit button when localStorage.accessToken carries impersonator_owner"
      contains: "impersonator_owner"
    - path: "vue/src/Routes.js"
      provides: "/app/admin/users route + ADMIN_PATHS guard extension"
      contains: "AdminUsersPage"
    - path: "vue/src/components/Layout/Layout.vue"
      provides: "ImpersonationBanner mount point above <router-view>"
      contains: "ImpersonationBanner"
  key_links:
    - from: "AdminUsers.vue#created"
      to: "auth/fetchUsers Vuex action"
      via: "mapActions + this.fetchUsers()"
      pattern: "fetchUsers"
    - from: "AdminUsers.vue revoke modal confirm"
      to: "DELETE /api/v2/admin/session/:owner"
      via: "admin/revokeSession Vuex action -> $api.$delete"
      pattern: "revokeSession"
    - from: "AdminUsers.vue impersonate modal confirm"
      to: "POST /api/v2/admin/impersonate"
      via: "admin/impersonate Vuex action -> $api.$post"
      pattern: "impersonate"
    - from: "ImpersonationBanner.vue"
      to: "localStorage.accessToken via VueJwtDecode.decode"
      via: "decode() in created() + watch $route"
      pattern: "VueJwtDecode"
    - from: "ImpersonationBanner.vue Exit"
      to: "auth/clearSession + $router.push('/login')"
      via: "mapActions + this.clearSession() then this.$router.push('/login')"
      pattern: "clearSession"
---

<objective>
Ship the frontend half of Phase 10 (Admin Features) in the console submodule (`/Users/igraczech/Repositories/thinx-device-api/services/console/`). Three new files (Vuex admin module, AdminUsers page, ImpersonationBanner component) + three patched files (store index, Routes.js, Layout.vue) — all wiring against the Wave 1 backend endpoints that are presumed live in parent `thinx-staging` BEFORE this wave deploys.

Purpose: Wave 1 delivers `/api/v2/admin/users`, `/api/v2/admin/session/:owner`, `/api/v2/admin/impersonate`. Without Wave 2 there is no way for an admin to actually USE those endpoints — the Profile.vue Admin tab still shows the placeholder text ("not yet available in this API version"), there is no `/app/admin/users` route, no impersonation banner, no Vuex module. Wave 2 lights up the user-facing surface; Wave 3 (Profile.vue tab swap + Sidebar nav + Cypress flip + REQUIREMENTS / ROADMAP close-out) is the final layer that makes the feature discoverable and verifies it.

Output: 3 new files (`vue/src/store/admin.js`, `vue/src/pages/AdminUsers/AdminUsers.vue`, `vue/src/components/ImpersonationBanner/ImpersonationBanner.vue`) + 3 patched files (`vue/src/store/index.js`, `vue/src/Routes.js`, `vue/src/components/Layout/Layout.vue`) — all in the console submodule. Commits land in the submodule's `thinx-staging` branch. `cd vue && yarn build` succeeds. `cd vue && npx cypress run --spec cypress/integration/admin.spec.js` can run end-to-end against the Wave-0 stub (still TODO-only at this wave; Wave 3 flips assertions).

CRITICAL DEPENDENCY: this wave depends on Wave 1 being LIVE in the parent monorepo's `thinx-staging` branch — `fetchUsers` will 404 against a backend that doesn't yet have `router.admin.js`. Do NOT deploy this wave (via parent submodule bump) until Wave 1 commits are in the parent's deployed image.

CRITICAL CONVENTIONS (do NOT regress):
- `mapGetters` + `mapActions` spread INSIDE `methods:`, called as functions (Phase 6 G1 — confirmed in Profile.vue:195-200, Login.vue:113-126).
- Plain `<table class="table table-striped">` + v-for + custom Prev/Next pagination — NEVER `<b-table>` / `<b-pagination>` (CONTEXT.md C1).
- `$api.$get/$post/$delete` paths are `/admin/users` etc. — NO `/api/v2` prefix (added by the $api client at `core/api.js:12,61` where `apiPath = '/api/v2'` is composed in `composeUrl()`; same convention as profile.js:86-104).
- No new npm packages — Vue 2.6 + BootstrapVue 2.21.2 + Vuex + vue-jwt-decode (already imported at store/auth.js:1).
</objective>

<execution_context>
@/Users/igraczech/.claude/get-shit-done/workflows/execute-plan.md
@/Users/igraczech/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/ROADMAP.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/REQUIREMENTS.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-10/10-CONTEXT.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-10/10-RESEARCH.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/store/index.js
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/store/profile.js
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/store/auth.js
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/Routes.js
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/components/Layout/Layout.vue
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/pages/Profile/Profile.vue
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/pages/Devices/Devices.vue
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/pages/Login/Login.vue
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/components/Header/Header.vue
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/core/api.js

<interfaces>
VUEX MODULE SHAPE (mirror vue/src/store/profile.js verbatim — lines 1-112):

  export default {
    namespaced: true,
    state: { users: [] },
    mutations: {
      setUsers(state, users) { state.users = users; },
    },
    actions: {
      async fetchUsers({ commit }) {
        const result = await this.$api.$get('/admin/users');
        if (result.success) commit('setUsers', result.response);
        return result;
      },
      async revokeSession(_, { owner }) {
        return await this.$api.$delete('/admin/session/' + owner);
      },
      async impersonate(_, { owner }) {
        return await this.$api.$post('/admin/impersonate', JSON.stringify({ owner }));
      },
    },
    getters: {
      getUsers(state) { return state.users; },
    },
  };

Path convention: `/admin/users` (NOT `/api/v2/admin/users`) — the $api client at `vue/src/core/api.js:12,61` (`apiPath = '/api/v2'` composed in `composeUrl()`) adds the `/api/v2` prefix. Same convention as profile.js:86 (`/profile` → `/api/v2/profile`). A duplicated prefix produces a 404.

STORE INDEX PATCH (vue/src/store/index.js — current lines 1-43):

Current top-of-file imports (lines 1-18):
  import profile from './profile';
  ...
  import stats from './stats.js';

Current modules block (lines 22-41):
  modules: {
    layout, auth, repositories, apikeys, rsakeys, enviros, channels,
    profile, transformers, devices, buildlog, auditlog, stats
  }

Task 2 adds:
  import admin from './admin';    // after the profile import; alphabetical-ish neighbours
  ...
  modules: { ..., admin }         // append at the end of the modules block

ROUTES PATCH (vue/src/Routes.js — current shape):

The /app parent has children at lines 51-121 (dashboard, devices, device/:udid, apikeys, repositories, history, rsakeys, transformers, transformer/:utid, enviros, channels, profile). Task 5 INSERTS a new child entry:

  {
    path: 'admin/users',
    name: 'AdminUsers',
    component: AdminUsersPage,
  },

NOTE: child paths under /app DO NOT have a leading slash (see profile child at line 116 — `path: 'profile'`). The full URL becomes /#/app/admin/users via the hash-mode router.

Top-of-file import addition (after the existing ProfilePage import at line 24):
  import AdminUsersPage from '@/pages/AdminUsers/AdminUsers';

Guard extension (Routes.js#beforeEach at current lines 131-144):

Current:
  const PUBLIC_PATHS = ['/login', '/password-reset', '/error'];
  router.beforeEach((to, from, next) => {
    if (PUBLIC_PATHS.includes(to.path)) return next();
    if (!to.path.startsWith('/app')) return next();
    const authed =
      !!(store && store.state && store.state.auth && store.state.auth.accessToken) ||
      !!(typeof window !== 'undefined' && window.localStorage.getItem('accessToken'));
    if (!authed) return next('/login');
    next();
  });

Task 5b inserts an ADMIN_PATHS check BETWEEN the `if (!authed) return next('/login');` line and the final `next();` call (per OQ-4):

  const ADMIN_PATHS = ['/app/admin'];   // hoisted to module-scope alongside PUBLIC_PATHS
  ...
  if (ADMIN_PATHS.some(p => to.path.startsWith(p))) {
    const profile = (store && store.state && store.state.profile && store.state.profile.profile);
    if (!profile || profile.admin !== true) return next('/app/dashboard');
  }
  next();

This is best-effort: the in-memory profile may not be loaded on cold-reload (App.vue#created fetches it async). The 403 from /api/v2/admin/users is the real security boundary; the guard is UX polish (avoid rendering a broken admin page).

LAYOUT.VUE BANNER MOUNT (vue/src/components/Layout/Layout.vue:1-14 current shape):

  <template>
  <div :class="{root: true, sidebarClose}">
    <Header />
    <Sidebar />
    <div ref="content" class="content animated fadeInUp">
      <transition name="router-animation">
        <router-view />
      </transition>
    </div>
    ...

Task 6 INSERTS `<ImpersonationBanner />` between `<Sidebar />` (line 4) and the `<div ref="content" ...>` (line 5):

  <Header />
  <Sidebar />
  <ImpersonationBanner />
  <div ref="content" class="content animated fadeInUp">
    ...

Script section: import ImpersonationBanner and register in components alongside Header and Sidebar.

IMPERSONATIONBANNER SHAPE (full component — research §4.6 + §6.4 + §6.5):

  <template>
    <div v-if="impersonatorOwner" class="impersonation-banner bg-warning text-dark py-2 px-3 d-flex align-items-center">
      <span class="mr-3">🎭 Impersonating <strong>{{ targetUsername || targetOwner }}</strong> — expires in {{ countdown }}</span>
      <b-button size="sm" variant="dark" @click="exit">Exit impersonation</b-button>
    </div>
  </template>

  <script>
  import VueJwtDecode from "vue-jwt-decode";
  import { mapActions } from "vuex";

  export default {
    name: "ImpersonationBanner",
    data() {
      return {
        impersonatorOwner: null,
        targetOwner: null,
        targetUsername: null,
        expSeconds: null,
        countdown: '--:--',
        tickerId: null,
      };
    },
    created() {
      this.decode();
      this.tickerId = setInterval(() => this.updateCountdown(), 1000);
    },
    beforeDestroy() {
      if (this.tickerId) clearInterval(this.tickerId);
    },
    watch: {
      '$route'() { this.decode(); },
    },
    methods: {
      ...mapActions({ clearSession: "auth/clearSession" }),
      decode() {
        try {
          const token = window.localStorage.getItem('accessToken');
          if (!token) { this.impersonatorOwner = null; return; }
          const decoded = VueJwtDecode.decode(token);
          if (decoded && decoded.impersonator_owner) {
            this.impersonatorOwner = decoded.impersonator_owner;
            this.targetOwner = decoded.username;
            this.expSeconds = decoded.exp;
            this.updateCountdown();
          } else {
            this.impersonatorOwner = null;
          }
        } catch (_e) {
          this.impersonatorOwner = null;
        }
      },
      updateCountdown() {
        if (!this.expSeconds) return;
        const secondsLeft = this.expSeconds - Math.floor(Date.now() / 1000);
        if (secondsLeft <= 0) {
          this.countdown = '00:00';
          this.exit();   // belt-and-suspenders; auth/scheduleExpiry should already have fired
          return;
        }
        const m = String(Math.floor(secondsLeft / 60)).padStart(2, '0');
        const s = String(secondsLeft % 60).padStart(2, '0');
        this.countdown = m + ':' + s;
      },
      async exit() {
        await this.clearSession();
        this.$router.push('/login');
      },
    },
  };
  </script>

  <style scoped>
  .impersonation-banner { position: sticky; top: 0; z-index: 1050; border-bottom: 2px solid #856404; }
  </style>

Subscription model: Vuex getter binding rejected in favour of localStorage + watch:$route (OQ-3 (a), recommended). The banner re-reads the JWT on every route change. Tradeoff: same-route token swap requires a route change to refresh — accepted because the AdminUsers.vue impersonate flow ends with $router.push('/app/dashboard'), which DOES trigger the watch.

ADMINUSERS.VUE SHAPE (full page — research §1.6 + §1.7 + §4.5 + §10):

Template (plain <table>, custom pagination, per-row actions):

  <template>
    <div>
      <div class="page-top-line">
        <h1>Admin · Users</h1>
      </div>
      <b-alert variant="danger" :show="!!error" dismissible @dismissed="error = ''">{{ error }}</b-alert>
      <b-alert variant="success" :show="!!message" dismissible @dismissed="message = ''">{{ message }}</b-alert>

      <div v-if="loading">Loading users…</div>
      <div v-else>
        <table class="table table-striped">
          <thead>
            <tr>
              <th>Username</th>
              <th>Email</th>
              <th>Admin</th>
              <th>Last login</th>
              <th>Devices</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            <tr v-for="user in pagedUsers" :key="user.owner">
              <td>{{ user.username }}</td>
              <td>{{ user.email }}</td>
              <td>
                <b-badge v-if="user.admin" variant="success">Yes</b-badge>
                <span v-else class="text-muted">—</span>
              </td>
              <td>{{ formatDate(user.last_login) }}</td>
              <td>{{ user.device_count }}</td>
              <td>
                <b-button size="sm" variant="danger" class="mr-1" @click="confirmRevoke(user)">Revoke</b-button>
                <b-button v-if="!user.admin" size="sm" variant="warning" @click="confirmImpersonate(user)">Impersonate</b-button>
              </td>
            </tr>
            <tr v-if="users.length === 0">
              <td colspan="6" class="text-center text-muted">No users.</td>
            </tr>
          </tbody>
        </table>

        <div class="d-flex align-items-center">
          <b-button size="sm" :disabled="page === 1" @click="page = page - 1">Prev</b-button>
          <span class="mx-2">Page {{ page }} / {{ totalPages }}</span>
          <b-button size="sm" :disabled="page >= totalPages" @click="page = page + 1">Next</b-button>
        </div>
      </div>
    </div>
  </template>

Script (Phase 6 G1 — mapActions + mapGetters in methods:, called as functions):

  <script>
  import { mapActions, mapGetters, mapMutations } from "vuex";

  export default {
    name: "AdminUsers",
    data() {
      return {
        loading: true,
        error: '',
        message: '',
        page: 1,
        pageSize: 20,
      };
    },
    computed: {
      users() { return this.getUsers() || []; },
      totalPages() { return Math.max(1, Math.ceil(this.users.length / this.pageSize)); },
      pagedUsers() {
        const start = (this.page - 1) * this.pageSize;
        return this.users.slice(start, start + this.pageSize);
      },
    },
    async created() {
      try {
        await this.fetchUsers();
      } catch (e) {
        this.error = 'Failed to load users.';
      } finally {
        this.loading = false;
      }
    },
    methods: {
      ...mapGetters({ getUsers: 'admin/getUsers' }),
      ...mapActions({
        fetchUsers: 'admin/fetchUsers',
        revokeSession: 'admin/revokeSession',
        impersonate: 'admin/impersonate',
        scheduleExpiry: 'auth/scheduleExpiry',
      }),
      ...mapMutations({ setAccessToken: 'auth/setAccessToken' }),
      formatDate(ts) {
        if (!ts) return '—';
        try { return new Date(ts).toLocaleString(); } catch (_e) { return String(ts); }
      },
      async confirmRevoke(user) {
        this.error = '';
        this.message = '';
        const ok = await this.$bvModal.msgBoxConfirm(
          'Force-logout ' + user.username + '? This invalidates all their access and refresh tokens.',
          { title: 'Revoke Sessions', okVariant: 'danger', okTitle: 'Force logout', size: 'sm' }
        );
        if (!ok) return;
        const result = await this.revokeSession({ owner: user.owner });
        if (result && result.success) {
          this.message = 'Sessions revoked for ' + user.username + '.';
        } else {
          this.error = (result && result.response && result.response.toString()) || 'Failed to revoke sessions.';
        }
      },
      async confirmImpersonate(user) {
        this.error = '';
        this.message = '';
        const ok = await this.$bvModal.msgBoxConfirm(
          'Impersonate ' + user.username + '? You will be logged in as them for 15 minutes; every action is audit-logged.',
          { title: 'Impersonate User', okVariant: 'warning', okTitle: 'Impersonate', size: 'sm' }
        );
        if (!ok) return;
        const result = await this.impersonate({ owner: user.owner });
        if (!result || !result.success) {
          this.error = (result && result.response && result.response.toString()) || 'Failed to impersonate.';
          return;
        }
        const access_token = result.response && result.response.access_token;
        if (!access_token) {
          this.error = 'Impersonation token missing from server response.';
          return;
        }
        window.localStorage.setItem('accessToken', access_token);
        window.localStorage.removeItem('refreshToken');   // impersonation has no refresh — re-login required
        this.setAccessToken(access_token);
        this.scheduleExpiry(access_token);
        this.$router.push('/app/dashboard');               // route change triggers ImpersonationBanner.decode()
      },
    },
  };
  </script>

BACKEND CONTRACT (Wave 1 endpoints; final shape ratified in 10-01-PLAN.md Task 5):

  GET /api/v2/admin/users -> { success: bool, response: Array<UserRow> }
    UserRow = { owner, username, email, admin, created, last_login, device_count }
    Non-admins get 403. Unauthenticated get 401.

  DELETE /api/v2/admin/session/:owner -> { success: bool, response: 'sessions_revoked' | failure-message }
    Writes Redis revoked:owner:{owner} = String(Date.now()) with TTL 7d.

  POST /api/v2/admin/impersonate { owner } -> { success: bool, response: { access_token } | failure-message }
    Returns 403 with response 'cannot_impersonate_admin' when target.admin === true.
    Token is a 15-min JWT with username=target_owner, impersonator_owner=admin_owner.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create vue/src/store/admin.js — Vuex module for admin actions</name>
  <files>vue/src/store/admin.js</files>
  <read_first>
    - vue/src/store/profile.js (lines 1-112) — the exact shape this module mirrors. fetchProfile -> fetchUsers; updateProfile pattern informs revokeSession/impersonate. Pay attention to `state` (no `users: []` typo), the `this.$api.$post(...)` body-stringification convention, and the destructured `({ commit })` first arg.
    - vue/src/core/api.js lines 50-52 — confirm the $api client adds the `/api/v2` prefix to every request. Pass `/admin/...` not `/api/v2/admin/...`.
    - .planning/phase-10/10-RESEARCH.md §10 (Frontend Vuex module + register) — canonical body used below.
  </read_first>
  <action>
    Create `/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/store/admin.js`. The file is a small namespaced Vuex module — copy the shape from `vue/src/store/profile.js` exactly.

    Required body (verbatim — research §10):

      export default {
        namespaced: true,
        state: { users: [] },
        mutations: {
          setUsers(state, users) { state.users = users; },
        },
        actions: {
          async fetchUsers({ commit }) {
            const result = await this.$api.$get('/admin/users');
            if (result.success) commit('setUsers', result.response);
            return result;
          },
          async revokeSession(_, { owner }) {
            return await this.$api.$delete('/admin/session/' + owner);
          },
          async impersonate(_, { owner }) {
            return await this.$api.$post('/admin/impersonate', JSON.stringify({ owner }));
          },
        },
        getters: {
          getUsers(state) { return state.users; },
        },
      };

    Path convention notes:
      - `/admin/users` — the $api client adds the `/api/v2` prefix at runtime (`core/api.js:12,61` — `apiPath = '/api/v2'` composed in `composeUrl()`). DO NOT prepend `/api/v2` manually; that produces a duplicated prefix and a 404 (memory: same gotcha as Phase 6 path mistakes).
      - The DELETE call concatenates the owner ID into the path: `'/admin/session/' + owner`. The backend route is registered as `/api/v2/admin/session/:owner` (Wave 1 Task 5).
      - The POST call stringifies the body via `JSON.stringify({ owner })` — matches the convention used in profile.js:93,98 and auth.js:88,94. The $api client does NOT auto-stringify; passing a raw object would send `[object Object]`.

    DO NOT:
      - Import VueJwtDecode here (only ImpersonationBanner.vue needs it).
      - Mutate localStorage in the impersonate action (the AdminUsers.vue handler owns the token swap so it can re-schedule the expiry timer; keeping the action a pure API call simplifies testing).
      - Set/unset any auth-store fields here (auth-module concerns belong in auth.js).
      - Return anything other than the awaited $api result — keep the contract identical to profile.js.

    File must end with a single trailing newline (consistent with profile.js).
  </action>
  <verify>
    <automated>cd /Users/igraczech/Repositories/thinx-device-api/services/console && node --check vue/src/store/admin.js && test "$(grep -c 'namespaced: true' vue/src/store/admin.js)" = "1" && test "$(grep -c 'fetchUsers' vue/src/store/admin.js)" -ge "1" && test "$(grep -c 'revokeSession' vue/src/store/admin.js)" -ge "1" && test "$(grep -c 'impersonate' vue/src/store/admin.js)" -ge "1" && test "$(grep -c \"'/admin/users'\" vue/src/store/admin.js)" = "1" && test "$(grep -c \"'/admin/session/' + owner\" vue/src/store/admin.js)" = "1" && test "$(grep -c \"'/admin/impersonate'\" vue/src/store/admin.js)" = "1" && test "$(grep -c '/api/v2/admin' vue/src/store/admin.js)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - File vue/src/store/admin.js exists.
    - node --check exits 0.
    - grep -c "namespaced: true" returns 1.
    - The three actions exist: fetchUsers, revokeSession, impersonate.
    - The three paths are `/admin/users`, `/admin/session/` + owner, `/admin/impersonate` — NEVER `/api/v2/admin/...` (no duplicated prefix).
    - The setUsers mutation is committed only inside fetchUsers; revokeSession and impersonate return the awaited result without committing.
    - The getter getUsers returns state.users.
  </acceptance_criteria>
  <done>vue/src/store/admin.js exists as a namespaced Vuex module with state.users, setUsers mutation, fetchUsers + revokeSession + impersonate actions, getUsers getter. node --check passes. All paths use the leading-slash `/admin/...` form (no `/api/v2` prefix).</done>
  <commit>feat(10-02): vuex module store/admin.js (fetchUsers, revokeSession, impersonate)</commit>
</task>

<task type="auto">
  <name>Task 2: Register the admin module in vue/src/store/index.js</name>
  <files>vue/src/store/index.js</files>
  <read_first>
    - vue/src/store/index.js — read in full (43 lines). Import block is lines 1-18; modules block is lines 22-41.
  </read_first>
  <action>
    Open `/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/store/index.js`. Two edits.

    Edit (a) — Add the import. Locate the existing `import profile from './profile';` line (currently line 12). Insert directly after it:

      import admin from './admin';

    (The exact position is stylistic — admin module is closely related to profile/auth, so grouping it with those imports keeps the file readable.)

    Edit (b) — Register the module. Locate the modules block (currently lines 23-41) and append `admin` to it. The current block reads:

      modules: {
        layout,
        auth,
        repositories,
        apikeys,
        rsakeys,
        enviros,
        channels,

        profile,
        transformers,

        devices,

        buildlog,
        auditlog,

        stats
      }

    Append `admin` on a new line after `stats` (before the closing `}`):

      modules: {
        layout,
        auth,
        repositories,
        apikeys,
        rsakeys,
        enviros,
        channels,

        profile,
        transformers,

        devices,

        buildlog,
        auditlog,

        stats,
        admin
      }

    Add the comma after `stats` so the file remains valid. Do NOT reorder existing modules.
  </action>
  <verify>
    <automated>cd /Users/igraczech/Repositories/thinx-device-api/services/console && node --check vue/src/store/index.js && test "$(grep -c \"import admin from './admin';\" vue/src/store/index.js)" = "1" && test "$(grep -cE '^\\s*admin' vue/src/store/index.js)" -ge "2"</automated>
  </verify>
  <acceptance_criteria>
    - node --check exits 0.
    - grep -c "import admin from './admin';" returns 1.
    - The modules block contains `admin` as a registered key (alongside profile, devices, etc.).
    - No existing module was removed or reordered (verified via `git diff vue/src/store/index.js` showing only additive changes).
    - cd vue && yarn build compiles vue/src/store/index.js without errors (validated in the Wave verification step).
  </acceptance_criteria>
  <done>vue/src/store/index.js imports the admin module and registers it inside the `modules: { ... }` block alongside the existing modules. No other module changed.</done>
  <commit>feat(10-02): register admin module in store/index.js</commit>
</task>

<task type="auto">
  <name>Task 3: Create vue/src/pages/AdminUsers/AdminUsers.vue — user list page</name>
  <files>vue/src/pages/AdminUsers/AdminUsers.vue</files>
  <read_first>
    - vue/src/pages/Devices/Devices.vue lines 47-92 — confirm the plain `<table class="table table-striped">` + v-for pattern (no `<b-table>`).
    - vue/src/pages/Profile/Profile.vue lines 195-313 — confirm the methods-block convention (mapActions + mapGetters spread inside methods:, called as functions) and the $bvModal.msgBoxConfirm pattern at confirmDeleteAccount (lines 299-311).
    - vue/src/pages/History/History.vue around line 192-200 — second methods-block reference.
    - .planning/phase-10/10-RESEARCH.md §1.6 + §1.7 + §4.5 + §10 (Frontend: Impersonate then store tokens) — canonical template + script bodies used below.
    - .planning/phase-10/10-RESEARCH.md §4.7 anti-patterns — DO NOT use `<b-table>`, DO NOT put mapGetters in computed:, DO NOT hand-roll JWT decode here.
  </read_first>
  <action>
    Create the directory `vue/src/pages/AdminUsers/` and the single-file component `vue/src/pages/AdminUsers/AdminUsers.vue`. Build it as a Vue 2 SFC with separate `<template>` and `<script>` sections (no `<style>` — table styling lives in the global stylesheet alongside Devices.vue's table).

    Template requirements:
      - Top-level <div> wraps a page-top-line header (mirroring the existing page layouts at Devices.vue:8-11).
      - Two b-alert blocks (one variant="danger" bound to `:show="!!error"`, one variant="success" bound to `:show="!!message"`), both dismissible.
      - A loading state: `<div v-if="loading">Loading users…</div>`.
      - When not loading, render a plain `<table class="table table-striped">` with six columns: Username, Email, Admin, Last login, Devices, Actions.
      - The Admin column shows a green `<b-badge variant="success">Yes</b-badge>` when `user.admin`, an em-dash placeholder otherwise.
      - The Actions cell has TWO buttons:
        - "Revoke" (b-button size="sm" variant="danger") — always visible; calls confirmRevoke(user)
        - "Impersonate" (b-button size="sm" variant="warning") — `v-if="!user.admin"`; calls confirmImpersonate(user)
      - The empty-state row: when `users.length === 0`, render a single `<tr>` with `<td colspan="6">No users.</td>`.
      - Below the table, custom pagination: two b-buttons (Prev / Next) with `:disabled` bindings and a `<span class="mx-2">Page {{ page }} / {{ totalPages }}</span>` between them.

    Script requirements:
      - Imports: `import { mapActions, mapGetters, mapMutations } from "vuex";`. NO `import VueJwtDecode` here (that belongs in ImpersonationBanner.vue).
      - export default with: name "AdminUsers", data, computed, async created, methods.
      - data() returns: { loading: true, error: '', message: '', page: 1, pageSize: 20 }.
      - computed.users — returns `this.getUsers() || []` (getUsers is a mapped getter; called as a function per Phase 6 G1).
      - computed.totalPages — `Math.max(1, Math.ceil(this.users.length / this.pageSize))`. The max-1 guard avoids the "Page 1 / 0" rendering when there are no users.
      - computed.pagedUsers — slice the users array by current page.
      - async created() — wrap `await this.fetchUsers()` in try/catch/finally. On success the setUsers mutation populates state.users; on error set `this.error = 'Failed to load users.'`; finally set `this.loading = false`.
      - methods block (Phase 6 G1 — ALL spreads INSIDE methods:):
        - `...mapGetters({ getUsers: 'admin/getUsers' })` — placed FIRST in methods (so the computed properties above can call this.getUsers() during the next reactive tick).
        - `...mapActions({ fetchUsers: 'admin/fetchUsers', revokeSession: 'admin/revokeSession', impersonate: 'admin/impersonate', scheduleExpiry: 'auth/scheduleExpiry' })`.
        - `...mapMutations({ setAccessToken: 'auth/setAccessToken' })`.
        - formatDate(ts) — convert ISO/epoch to localeString; em-dash for falsy.
        - async confirmRevoke(user) — clear error/message, $bvModal.msgBoxConfirm with title "Revoke Sessions", danger okVariant, okTitle "Force logout"; on confirm dispatch this.revokeSession({ owner: user.owner }); surface result.success / result.response via this.message / this.error.
        - async confirmImpersonate(user) — see the full body in <interfaces> above. Key behaviors:
          - $bvModal.msgBoxConfirm with title "Impersonate User", warning okVariant, okTitle "Impersonate".
          - On confirm, dispatch this.impersonate({ owner: user.owner }).
          - Extract `result.response.access_token`; defensively check for both `result.success` and the presence of the token before mutating localStorage.
          - `window.localStorage.setItem('accessToken', access_token)` — the impersonation token replaces the admin's token. This matches the locked OQ-3/OQ-7 "Exit = force re-login" decision (admin's original token is gone — by design).
          - `window.localStorage.removeItem('refreshToken')` — impersonation has no refresh (Wave 1 sign_with_impersonation issues only an access token; the existing refresh token would resolve to the admin's identity which defeats the point).
          - `this.setAccessToken(access_token)` — commits the new token into the Vuex auth state via the existing auth mutation; the mutation also calls `this.$api.setAccessToken` (auth.js:21-22) so subsequent $api calls carry the impersonation token.
          - `this.scheduleExpiry(access_token)` — re-schedules the existing Phase-8 expiry timer (auth.js:60-86) to the 15-min impersonation exp. When the timer fires, clearSession + redirect-to-login fires automatically.
          - `this.$router.push('/app/dashboard')` — leaves the admin users page. The route change triggers ImpersonationBanner.vue's `watch: '$route'` → `decode()` → banner becomes visible.

    DO NOT:
      - Use `<b-table>` or `<b-pagination>` (CONTEXT.md C1; research §0.1 — both are zero-occurrence in the project).
      - Put mapGetters or mapActions in `computed:` (Phase 6 G1).
      - Add a hostnameMixin or any mixins.
      - Add per-row Edit / Delete actions — out of scope (only Revoke + Impersonate per v1).
      - Compute device_count client-side from /devices — locked OQ-B; render whatever the backend sends (0 in v1).
      - Add a sort UI — v1 ships unsorted (or backend-sorted by last_seen — out of scope to verify here).
      - Render an Impersonate button on admin rows — the `v-if="!user.admin"` is the security and UX boundary (Wave 1 also rejects server-side, but the UI MUST hide the button to avoid teasing the admin with a non-functional action).
  </action>
  <verify>
    <automated>cd /Users/igraczech/Repositories/thinx-device-api/services/console && test -f vue/src/pages/AdminUsers/AdminUsers.vue && test "$(grep -c 'name: \"AdminUsers\"' vue/src/pages/AdminUsers/AdminUsers.vue)" = "1" && test "$(grep -c 'table table-striped' vue/src/pages/AdminUsers/AdminUsers.vue)" = "1" && test "$(grep -c '<b-table' vue/src/pages/AdminUsers/AdminUsers.vue)" = "0" && test "$(grep -c '<b-pagination' vue/src/pages/AdminUsers/AdminUsers.vue)" = "0" && test "$(grep -c 'pagedUsers' vue/src/pages/AdminUsers/AdminUsers.vue)" -ge "2" && test "$(grep -c 'v-if=\"!user.admin\"' vue/src/pages/AdminUsers/AdminUsers.vue)" = "1" && test "$(grep -c 'mapGetters' vue/src/pages/AdminUsers/AdminUsers.vue)" -ge "1" && test "$(grep -c 'mapActions' vue/src/pages/AdminUsers/AdminUsers.vue)" -ge "1" && test "$(grep -c 'confirmRevoke' vue/src/pages/AdminUsers/AdminUsers.vue)" -ge "2" && test "$(grep -c 'confirmImpersonate' vue/src/pages/AdminUsers/AdminUsers.vue)" -ge "2" && test "$(grep -c 'setAccessToken' vue/src/pages/AdminUsers/AdminUsers.vue)" -ge "1" && test "$(grep -c 'scheduleExpiry' vue/src/pages/AdminUsers/AdminUsers.vue)" -ge "1" && test "$(grep -c 'removeItem..refreshToken' vue/src/pages/AdminUsers/AdminUsers.vue)" = "1"</automated>
  </verify>
  <acceptance_criteria>
    - File vue/src/pages/AdminUsers/AdminUsers.vue exists.
    - name: "AdminUsers" present.
    - grep `<b-table` returns 0 and grep `<b-pagination` returns 0 (CONTEXT.md C1 enforced).
    - grep `table table-striped` returns 1.
    - `v-if="!user.admin"` appears exactly once on the Impersonate button (ADMIN-03 negative case).
    - mapGetters + mapActions + mapMutations all spread INSIDE methods: (manual code-read).
    - confirmRevoke and confirmImpersonate use `$bvModal.msgBoxConfirm` (manual code-read).
    - The impersonate flow: removeItem('refreshToken') is present exactly once; setAccessToken + scheduleExpiry are called after writing accessToken to localStorage.
    - cd vue && yarn build compiles without errors (validated in the Wave verification step after Tasks 5/6 land).
  </acceptance_criteria>
  <done>vue/src/pages/AdminUsers/AdminUsers.vue exists as a Vue 2 SFC with: a plain table + custom pagination; per-row Revoke + Impersonate buttons with $bvModal confirmation; Impersonate hidden on admin rows; mapGetters/mapActions/mapMutations spread inside methods:; the impersonate flow rewrites localStorage.accessToken, removes refreshToken, commits setAccessToken, dispatches scheduleExpiry, and pushes to /app/dashboard.</done>
  <commit>feat(10-02): AdminUsers.vue page (table + custom pagination + per-row Revoke/Impersonate)</commit>
</task>

<task type="auto">
  <name>Task 4: Create vue/src/components/ImpersonationBanner/ImpersonationBanner.vue — sticky banner with countdown</name>
  <files>vue/src/components/ImpersonationBanner/ImpersonationBanner.vue</files>
  <read_first>
    - vue/src/store/auth.js — confirm clearSession (lines 48-59) tears down localStorage + Vuex state + the expiryTimerId; ImpersonationBanner.exit() reuses this verbatim.
    - vue/src/components/Header/Header.vue around the logout handler — second reference for the clearSession + $router.push('/login') flow (the canonical "force re-login" shape).
    - .planning/phase-10/10-RESEARCH.md §4.6 (Pattern 4) — canonical component body used below.
    - .planning/phase-10/10-RESEARCH.md §6.5 + R5 — beforeDestroy clearInterval is mandatory.
    - .planning/phase-10/10-RESEARCH.md §7 OQ-3 (recommendation (a)) + OQ-5 + OQ-7 — locks the subscription model (decode on created + watch $route), the 1-second cadence, and the exit-without-server-call behavior.
  </read_first>
  <action>
    Create the directory `vue/src/components/ImpersonationBanner/` and the single-file component `vue/src/components/ImpersonationBanner/ImpersonationBanner.vue`. Implement the full body from the <interfaces> section above; this task copies it verbatim.

    Template requirements:
      - Single root `<div>` with class binding `impersonation-banner bg-warning text-dark py-2 px-3 d-flex align-items-center` and `v-if="impersonatorOwner"`.
      - Inside: a `<span class="mr-3">` showing the emoji (🎭), a bold target username (preferring `targetUsername` if set, falling back to `targetOwner`), the literal "— expires in" text, and the live `{{ countdown }}` mm:ss.
      - A `<b-button size="sm" variant="dark" @click="exit">Exit impersonation</b-button>` after the span.

    Script requirements (mirror the body above verbatim):
      - Imports: `import VueJwtDecode from "vue-jwt-decode";` (already a project dep — store/auth.js:1) and `import { mapActions } from "vuex";`. NO other imports.
      - data() returns the six fields: impersonatorOwner, targetOwner, targetUsername, expSeconds, countdown ('--:--'), tickerId (null).
      - created() calls this.decode() THEN starts the setInterval at 1000ms cadence (OQ-5) and stores the id on this.tickerId.
      - beforeDestroy() clears the interval if tickerId is set — MANDATORY (R5; otherwise the timer leaks across logouts).
      - watch: '$route' — handler calls this.decode(). This is the route-change subscription that picks up newly-issued impersonation tokens after AdminUsers.vue dispatches $router.push('/app/dashboard') (per OQ-3 (a)).
      - methods block (Phase 6 G1 — mapActions inside methods:):
        - `...mapActions({ clearSession: "auth/clearSession" })` — placed FIRST.
        - decode() — wrap in try/catch. Read window.localStorage.getItem('accessToken'); if null, clear impersonatorOwner and return. Otherwise VueJwtDecode.decode(token); if decoded and decoded.impersonator_owner is truthy, set impersonatorOwner / targetOwner (from decoded.username) / expSeconds (decoded.exp), then call this.updateCountdown() immediately so the banner doesn't show "--:--" for one tick. Catch swallows decode errors and clears impersonatorOwner.
        - updateCountdown() — compute secondsLeft = expSeconds - now; if <= 0, set countdown to '00:00' AND call this.exit() (belt-and-suspenders — auth/scheduleExpiry should already have fired); else format MM:SS with zero-padding.
        - async exit() — `await this.clearSession()` then `this.$router.push('/login')`. Matches Header.vue's logout flow verbatim (OQ-7 (a)). NO server-side call.

    Style requirements:
      - Scoped `<style scoped>` with the impersonation-banner class: `position: sticky; top: 0; z-index: 1050; border-bottom: 2px solid #856404;`.

    DO NOT:
      - Use Vuex getter binding for impersonator state (OQ-3 (b) rejected — adds Vuex coupling and an event-bus need). The decode-from-localStorage approach is the locked design.
      - Use requestAnimationFrame for the countdown (research §3 + OQ-5).
      - Make a server-side call on exit (OQ-7 (a) locked; the 15-min TTL on the server-side blacklist would re-implement what the natural exp does, and the stolen-token risk is documented in §6.3 as an accepted gap).
      - Persist the prior admin token to "restore admin session on exit" (research §4.7 anti-pattern; the locked decision is force-re-login).
      - Auto-redirect on cold-reload during impersonation (R / §6.4) — the banner correctly tells the admin what's happening; they click Exit to leave.
  </action>
  <verify>
    <automated>cd /Users/igraczech/Repositories/thinx-device-api/services/console && test -f vue/src/components/ImpersonationBanner/ImpersonationBanner.vue && test "$(grep -c 'name: \"ImpersonationBanner\"' vue/src/components/ImpersonationBanner/ImpersonationBanner.vue)" = "1" && test "$(grep -c 'VueJwtDecode' vue/src/components/ImpersonationBanner/ImpersonationBanner.vue)" -ge "2" && test "$(grep -c 'impersonator_owner' vue/src/components/ImpersonationBanner/ImpersonationBanner.vue)" -ge "1" && test "$(grep -c 'setInterval' vue/src/components/ImpersonationBanner/ImpersonationBanner.vue)" = "1" && test "$(grep -c 'clearInterval' vue/src/components/ImpersonationBanner/ImpersonationBanner.vue)" = "1" && test "$(grep -c 'beforeDestroy' vue/src/components/ImpersonationBanner/ImpersonationBanner.vue)" = "1" && test "$(grep -c '1000' vue/src/components/ImpersonationBanner/ImpersonationBanner.vue)" -ge "1" && test "$(grep -c \"watch:\" vue/src/components/ImpersonationBanner/ImpersonationBanner.vue)" = "1" && test "$(grep -c \"'auth/clearSession'\" vue/src/components/ImpersonationBanner/ImpersonationBanner.vue)" = "1" && test "$(grep -c \"\\\\\\$router.push('/login')\" vue/src/components/ImpersonationBanner/ImpersonationBanner.vue)" = "1"</automated>
  </verify>
  <acceptance_criteria>
    - File vue/src/components/ImpersonationBanner/ImpersonationBanner.vue exists.
    - name "ImpersonationBanner" present.
    - VueJwtDecode imported and used.
    - setInterval called once at 1000ms cadence; clearInterval called inside beforeDestroy.
    - watch: '$route' present; calls this.decode().
    - mapActions placed INSIDE methods: (Phase 6 G1).
    - exit() awaits clearSession then pushes /login. No server-side call.
    - Manual code-read: decode() reads window.localStorage.getItem('accessToken') and checks for decoded.impersonator_owner.
    - Manual code-read: the v-if on the root div is `impersonatorOwner` (NOT a Vuex getter).
    - cd vue && yarn build compiles without errors (validated in the Wave verification step).
  </acceptance_criteria>
  <done>vue/src/components/ImpersonationBanner/ImpersonationBanner.vue exists as a Vue 2 SFC with: a sticky `bg-warning` banner gated by `v-if="impersonatorOwner"`, MM:SS countdown updated by a 1s setInterval (cleared in beforeDestroy), decode() called in created() + on $route change, exit() that awaits auth/clearSession and pushes /login. No server-side calls, no Vuex getter binding for impersonator state, no leaked timer.</done>
  <commit>feat(10-02): ImpersonationBanner.vue (sticky banner, 1s countdown, exit -> clearSession + /login)</commit>
</task>

<task type="auto">
  <name>Task 5: Add /app/admin/users route + ADMIN_PATHS guard extension in vue/src/Routes.js</name>
  <files>vue/src/Routes.js</files>
  <read_first>
    - vue/src/Routes.js — read in full (147 lines). Imports at lines 1-24; route definitions at lines 29-129; auth guard at 131-144.
    - .planning/phase-10/10-RESEARCH.md §7 OQ-4 — locks the hardcoded array form (option (a)).
    - .planning/phase-10/10-RESEARCH.md §6.9 + R / cold-reload — the guard is best-effort (in-memory profile may not be loaded on cold-reload); the backend 403 is the real security boundary.
  </read_first>
  <action>
    Open `/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/Routes.js`. Make three edits.

    Edit (a) — Add the component import. Locate the existing `import ProfilePage from '@/pages/Profile/Profile';` line (current line 24). Insert directly below it:

      import AdminUsersPage from '@/pages/AdminUsers/AdminUsers';

    Edit (b) — Add the new child route entry. Locate the `/app` parent's children block (currently lines 51-121). Find the existing `profile` child entry at lines 115-119:

      {
        path: 'profile',
        name: 'Profile',
        component: ProfilePage,
      },

    Insert a new child entry immediately after it (i.e. as the last entry of the children array, before the closing `]` at line 121):

      {
        path: 'admin/users',
        name: 'AdminUsers',
        component: AdminUsersPage,
      },

    NOTE the child path is `admin/users` WITHOUT a leading slash — matches the convention of every other /app child (see profile at line 116: `path: 'profile'`). The full URL becomes /#/app/admin/users via the hash-mode router.

    Edit (c) — Extend the auth guard at lines 131-144. The current guard reads:

      const PUBLIC_PATHS = ['/login', '/password-reset', '/error'];
      router.beforeEach((to, from, next) => {
        if (PUBLIC_PATHS.includes(to.path)) return next();
        if (!to.path.startsWith('/app')) return next();
        const authed =
          !!(store && store.state && store.state.auth && store.state.auth.accessToken) ||
          !!(typeof window !== 'undefined' && window.localStorage.getItem('accessToken'));
        if (!authed) return next('/login');
        next();
      });

    Modify it to add an ADMIN_PATHS constant alongside PUBLIC_PATHS, and insert an admin-check between the `if (!authed) return next('/login');` line and the final `next();`. The post-patch guard reads:

      const PUBLIC_PATHS = ['/login', '/password-reset', '/error'];
      const ADMIN_PATHS = ['/app/admin'];
      router.beforeEach((to, from, next) => {
        if (PUBLIC_PATHS.includes(to.path)) return next();
        if (!to.path.startsWith('/app')) return next();
        const authed =
          !!(store && store.state && store.state.auth && store.state.auth.accessToken) ||
          !!(typeof window !== 'undefined' && window.localStorage.getItem('accessToken'));
        if (!authed) return next('/login');
        if (ADMIN_PATHS.some(p => to.path.startsWith(p))) {
          const profile = (store && store.state && store.state.profile && store.state.profile.profile);
          if (!profile || profile.admin !== true) return next('/app/dashboard');
        }
        next();
      });

    Notes:
      - ADMIN_PATHS uses a prefix match (`startsWith(p)`) so future admin sub-routes (e.g. /app/admin/audit, /app/admin/billing) are gated automatically. Per OQ-4: hardcoded array, not route metadata — v1 has exactly one admin sub-route and meta-based gating is over-engineering at this scale.
      - `profile` is read from `store.state.profile.profile` (the profile module's nested profile field — see vue/src/store/profile.js:5 + Profile.vue:284-302). On cold-reload the profile is loaded async by App.vue#created; if the guard runs before that resolves, profile is falsy and we redirect to /app/dashboard. This is acceptable: the user can re-click Admin from the sidebar (Wave 3) once profile is loaded, or the backend will 403 anyway if they bypass.
      - The fallback redirect is `/app/dashboard` (a known authenticated route), NOT `/login` (which would force a re-login for a user who was just looking around).

    DO NOT touch: PUBLIC_PATHS contents, the /login / /error / /password-reset top-level routes, the /app parent's other children, the catch-all `/*`, or the existing AUTH-03 G5 guard comment.
  </action>
  <verify>
    <automated>cd /Users/igraczech/Repositories/thinx-device-api/services/console && node --check vue/src/Routes.js && test "$(grep -c \"import AdminUsersPage from '@/pages/AdminUsers/AdminUsers';\" vue/src/Routes.js)" = "1" && test "$(grep -c \"path: 'admin/users'\" vue/src/Routes.js)" = "1" && test "$(grep -c \"name: 'AdminUsers'\" vue/src/Routes.js)" = "1" && test "$(grep -c \"const ADMIN_PATHS\" vue/src/Routes.js)" = "1" && test "$(grep -c \"ADMIN_PATHS.some\" vue/src/Routes.js)" = "1" && test "$(grep -c \"next('/app/dashboard')\" vue/src/Routes.js)" = "1" && test "$(grep -c \"profile.admin !== true\" vue/src/Routes.js)" = "1"</automated>
  </verify>
  <acceptance_criteria>
    - node --check vue/src/Routes.js exits 0.
    - The AdminUsersPage import line exists.
    - The new child route { path: 'admin/users', name: 'AdminUsers', component: AdminUsersPage } is registered under the /app parent.
    - ADMIN_PATHS constant exists at module scope alongside PUBLIC_PATHS.
    - The guard contains an `if (ADMIN_PATHS.some(p => to.path.startsWith(p)))` block AFTER the authed check and BEFORE the final `next();` call.
    - The admin-fail branch redirects to `/app/dashboard` (NOT `/login`).
    - PUBLIC_PATHS contents are byte-identical.
    - cd vue && yarn build compiles without errors (validated in the Wave verification step).
  </acceptance_criteria>
  <done>vue/src/Routes.js registers an `/app/admin/users` child route via AdminUsersPage and extends the beforeEach guard with a hardcoded ADMIN_PATHS = ['/app/admin'] prefix check that next('/app/dashboard') for non-admins. PUBLIC_PATHS and all other routes are byte-identical to before.</done>
  <commit>feat(10-02): route /app/admin/users + ADMIN_PATHS guard extension</commit>
</task>

<task type="auto">
  <name>Task 6: Mount ImpersonationBanner above <router-view> in vue/src/components/Layout/Layout.vue</name>
  <files>vue/src/components/Layout/Layout.vue</files>
  <read_first>
    - vue/src/components/Layout/Layout.vue — read in full (52 lines).
    - .planning/phase-10/10-RESEARCH.md §4.6 — confirms the banner mounts between Sidebar and the content div.
  </read_first>
  <action>
    Open `/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/components/Layout/Layout.vue`. Two edits.

    Edit (a) — Add the component import. Locate the existing import block (lines 16-24). After:

      import Header from '@/components/Header/Header';

    Insert:

      import ImpersonationBanner from '@/components/ImpersonationBanner/ImpersonationBanner';

    Edit (b) — Register the component AND mount it in the template. In the script section's `components: { Sidebar, Header }` (line 28), append `ImpersonationBanner`:

      components: { Sidebar, Header, ImpersonationBanner },

    In the template, locate lines 3-5:

      <Header />
      <Sidebar />
      <div ref="content" class="content animated fadeInUp">

    Insert `<ImpersonationBanner />` between `<Sidebar />` and `<div ref="content" ...>`:

      <Header />
      <Sidebar />
      <ImpersonationBanner />
      <div ref="content" class="content animated fadeInUp">

    Why this position:
      - Above `<router-view>` ensures the banner is visible across every /app/* page (the only routes where impersonation can be live, since impersonation requires authentication).
      - Below the static Header + Sidebar ensures the banner doesn't push the brand logo or nav offscreen.
      - The banner's CSS `position: sticky; top: 0;` keeps it pinned regardless of scroll inside the content area.

    DO NOT touch: the Helper-removed comment (line 21-22 commented-out import notes), the methods block, the computed block, the created/mounted lifecycle hooks, the Layout.scss import.
  </action>
  <verify>
    <automated>cd /Users/igraczech/Repositories/thinx-device-api/services/console && node --check vue/src/components/Layout/Layout.vue || true && test "$(grep -c 'ImpersonationBanner' vue/src/components/Layout/Layout.vue)" -ge "3" && test "$(grep -c '<ImpersonationBanner />' vue/src/components/Layout/Layout.vue)" = "1" && test "$(grep -c 'components: { Sidebar, Header, ImpersonationBanner }' vue/src/components/Layout/Layout.vue)" = "1"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'ImpersonationBanner' returns >= 3 (import, components registration, template tag — at minimum).
    - The template contains `<ImpersonationBanner />` on its own line between `<Sidebar />` and the content div.
    - The components object includes ImpersonationBanner as the third entry (`components: { Sidebar, Header, ImpersonationBanner }`).
    - All other lines of Layout.vue are byte-identical (verified via `git diff vue/src/components/Layout/Layout.vue` showing only the import, components-line, and one new template line modified).
    - cd vue && yarn build compiles without errors (validated in the Wave verification step).
  </acceptance_criteria>
  <done>vue/src/components/Layout/Layout.vue imports and registers ImpersonationBanner and mounts it as a sibling between Sidebar and the content div. No other content of Layout.vue changed.</done>
  <commit>feat(10-02): mount ImpersonationBanner above &lt;router-view&gt; in Layout.vue</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage.accessToken → ImpersonationBanner.decode | Token is user-controllable in theory (a user could write to localStorage via DevTools); the worst case is the banner showing a fake "Impersonating X" header — no security action is taken on the basis of the decoded claim alone. The real admin enforcement is server-side via the JWT signature verification. |
| AdminUsers.vue → store/admin.js → /api/v2/admin/* | Anonymous-from-the-client's-perspective POST/DELETE — the backend's requireAdmin middleware (Wave 1) is the authorization boundary. The frontend's "hide Impersonate on admin rows" guard is UX; the backend rejects admin-impersonation server-side anyway. |
| URL bar → Routes.js#beforeEach ADMIN_PATHS check | A non-admin typing /app/admin/users gets redirected to /app/dashboard via the in-memory profile check. Cold-reload race: if the profile isn't loaded yet, the user is also redirected (false negative). The backend 403 on /api/v2/admin/users is the real boundary. |
| /app/admin/users page → AdminUsers.vue impersonate flow | Token swap mutates localStorage AND Vuex AND the api client AND the expiry timer. All four updates run on the same tick (synchronous within the handler) — no partial-state window. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Elevation of Privilege | Non-admin browsing to /app/admin/users | mitigate | Routes.js#beforeEach ADMIN_PATHS check redirects to /app/dashboard when in-memory profile.admin !== true. Backend 403 is the real security boundary if the guard fails (cold-reload race per §6.9 / R6.9). |
| T-10-02-02 | Tampering | Spoofed localStorage.accessToken triggering false banner | accept | ImpersonationBanner displays based on a decoded JWT claim; a tampered token would still need a valid server-side JWT signature to actually authorize any API call. The banner alone has no privileges. |
| T-10-02-03 | Repudiation | Admin claims they didn't impersonate user X | mitigate | Wave 1 audit log captures the impersonation start (flags ["admin","impersonation","start"]) and every authenticated request under the token (flags ["admin","impersonation"]). The frontend cannot suppress these — they're emitted server-side. |
| T-10-02-04 | Denial of Service | Stuck setInterval after logout leaks timer | mitigate | beforeDestroy clearInterval is mandatory; the verify gate (grep -c clearInterval = 1) enforces this. The auth/clearSession also clears any expiryTimerId in the auth module (Phase 8 — auth.js:48-59). |
| T-10-02-05 | Information Disclosure | Impersonation token in localStorage visible to DevTools | accept | This is the same threat model as every other access token in the project; not a new exposure. The 15-min TTL bounds the lifetime. |
| T-10-02-06 | Tampering | Admin tampers with the Impersonate button hide-on-admin via DevTools | mitigate | The button is a UX nicety; clicking it (after un-hiding via DevTools) sends a POST to /api/v2/admin/impersonate which Wave 1 backend rejects with 403 "cannot_impersonate_admin". No privilege is gained. |
| T-10-02-07 | Spoofing | Forged response to /api/v2/admin/impersonate via MitM | mitigate | The whole site is served over HTTPS (production hostname console.thinx.cloud); the JWT signature verifies on subsequent requests. MitM attackers cannot forge a valid impersonation token without the Redis-held secret key. |
| T-10-02-08 | Information Disclosure | User list leaked via React/Vue devtools | accept | The list is intended to be visible to admins; the same data is accessible via the underlying API. No mitigation needed. |
| T-10-02-SC | Tampering | npm supply chain | accept | This plan installs NO new npm packages. vue-jwt-decode 0.1.0 + bootstrap-vue 2.21.2 + vue 2.6 + vuex are all pre-existing project deps. |
</threat_model>

<verification>
1. cd /Users/igraczech/Repositories/thinx-device-api/services/console && node --check vue/src/store/admin.js vue/src/store/index.js vue/src/Routes.js — all exit 0.
2. cd /Users/igraczech/Repositories/thinx-device-api/services/console && cd vue && yarn build — compiles all six touched/created files without errors or new warnings.
3. Manual (against a dev backend with Wave 1 endpoints live):
   a. Login as an admin user. Visit http://localhost:3000/#/app/admin/users — the page loads, the table shows all users from /api/v2/admin/users, each row's Devices column reads "0" (v1 placeholder).
   b. Click "Revoke" on a non-admin row → confirm modal appears → click "Force logout" → success toast/alert; the row's underlying user's next API call (use a second browser logged in as that user) returns 401.
   c. Click "Impersonate" on a non-admin row → confirm modal → click "Impersonate" → page navigates to /app/dashboard; the yellow ImpersonationBanner appears at the top with "🎭 Impersonating <username> — expires in 14:59 [Exit impersonation]" and the countdown ticks down each second.
   d. Under impersonation: navigate to /app/profile — the page shows the IMPERSONATED user's profile (not the admin's). Reload the page — the banner reappears (decode() runs in created()).
   e. Click "Exit impersonation" → window pushes to /#/login; localStorage is cleared (verify via DevTools); re-login as admin works normally.
   f. Visit http://localhost:3000/#/app/admin/users as a non-admin user → Routes.js#beforeEach redirects to /app/dashboard.
   g. Attempt to fetch /api/v2/admin/users directly via curl as a non-admin → 403 (backend boundary).
   h. Locate an admin row in the table → confirm no Impersonate button is rendered for that row (only Revoke).
4. cd /Users/igraczech/Repositories/thinx-device-api/services/console && grep -rn '<b-table' vue/src/ — confirm still zero occurrences (CONTEXT.md C1 anti-regression).
5. cd /Users/igraczech/Repositories/thinx-device-api/services/console && grep -rn 'computed: { ...mapGetters' vue/src/pages/AdminUsers/ vue/src/components/ImpersonationBanner/ — confirm zero occurrences (Phase 6 G1 anti-regression).
6. cd /Users/igraczech/Repositories/thinx-device-api/services/console && diff vue/package.json (vs HEAD) — confirm no dep changes.
7. Audit log surfacing: from the History page, filter by "admin" flag — the impersonation start + every action under impersonation appears. Existing rows (filter by "info"/"danger") are unaffected.
</verification>

<success_criteria>
- All six tasks land in the console submodule (`/Users/igraczech/Repositories/thinx-device-api/services/console/`). Parent monorepo git status is clean for this wave.
- cd vue && yarn build compiles cleanly.
- All three admin capabilities are user-driven end-to-end: list users, revoke sessions (with confirm modal), impersonate non-admins (with confirm modal + banner + countdown + exit).
- Impersonating an admin row is impossible from the UI (button hidden) AND from the backend (Wave 1 rejects 403). The hide-on-admin button is the locked OQ + the Cypress ADMIN-03 negative case.
- The Routes.js guard redirects non-admins away from /app/admin/* paths to /app/dashboard (best-effort UX; the backend 403 is the security boundary).
- No new npm packages installed in the submodule (`vue/package.json` unchanged).
- The ImpersonationBanner timer is cleared on every logout / route teardown (R5 mitigation; verify gate enforces clearInterval presence).
- Phase 6 G1 + CONTEXT.md C1 anti-regressions hold: mapGetters/mapActions inside methods:; no `<b-table>` / `<b-pagination>` introduced.
- Wave 2 is deployable via parent submodule bump AFTER Wave 1 is live in the parent's deployed image (memory `deployment-console-thinx-cloud`). Wave 3 (Profile.vue tab swap + Sidebar nav + Cypress flip + REQUIREMENTS/ROADMAP close-out) follows independently.
- Each task commit is atomic and individually revertable (six commits suggested under the `feat(10-02):` prefix).
</success_criteria>

<output>
Create /Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-10/10-02-SUMMARY.md when done.

Suggested commit sequence (console submodule, branch `thinx-staging`):
  1. feat(10-02): vuex module store/admin.js (fetchUsers, revokeSession, impersonate)
  2. feat(10-02): register admin module in store/index.js
  3. feat(10-02): AdminUsers.vue page (table + custom pagination + per-row Revoke/Impersonate)
  4. feat(10-02): ImpersonationBanner.vue (sticky banner, 1s countdown, exit -> clearSession + /login)
  5. feat(10-02): route /app/admin/users + ADMIN_PATHS guard extension
  6. feat(10-02): mount ImpersonationBanner above <router-view> in Layout.vue

Out of scope for this wave (do not implement; explicit `OUT OF SCOPE` markers):
- Profile.vue Admin-tab placeholder swap → Wave 3 (10-03-PLAN.md).
- Sidebar.vue conditional Admin NavLink → Wave 3 (10-03-PLAN.md) per OQ-A.
- Cypress assertion flip (admin.spec.js stays TODO-only at this wave) → Wave 3 (10-03-PLAN.md).
- REQUIREMENTS.md ADMIN-01..03 status flip Pending → Verified → Wave 3.
- ROADMAP.md Phase 10 row bump → Wave 3.
- Real device_count aggregation → v1.1 follow-up (locked OQ-B).
- Sidebar Admin NavLink for the same reason ROADMAP says Wave 3 — discoverability is part of the close-out, not the core feature delivery.
- History page flagFilterOptions extension to include "admin" + "impersonation" tags → optional polish (research §A9 footnote); does not block Phase 10 completion.
</output>
