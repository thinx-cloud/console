---
phase: 10-admin-features
plan: 01
type: execute
wave: 1
depends_on:
  - 10-00
repo: parent-monorepo
files_modified:
  - lib/middleware/requireAdmin.js
  - lib/router.admin.js
  - lib/thinx/audit.js
  - lib/thinx/jwtlogin.js
  - lib/router.js
  - thinx-core.js
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
tags:
  - backend
  - express
  - redis
  - jwt
  - admin
  - parent-monorepo

must_haves:
  truths:
    - "GET /api/v2/admin/users returns 200 + an array of {owner, username, email, admin, created, last_login, device_count: 0} when called by an admin; 403 for non-admins; 401 when unauthenticated"
    - "DELETE /api/v2/admin/session/:owner writes Redis key revoked:owner:{owner} = String(Date.now()) with TTL 7*24*60*60 seconds; emits alog.log(admin_owner, ..., ['admin','revoke']); returns 200"
    - "POST /api/v2/admin/impersonate with body {owner} returns 200 + {access_token: <JWT>} when target.admin !== true; returns 403 when target.admin === true; emits alog.log(admin_owner, ..., ['admin','impersonation','start'])"
    - "The auth-middleware block at lib/router.js:102-113 now performs a Redis blacklist check (fail-open on Redis error per R1), injects req.session.impersonator_owner when the JWT carries that claim, and emits one per-request alog.log entry with flags ['admin','impersonation'] when impersonating"
    - "lib/thinx/audit.js#log accepts BOTH a string flag (existing callers, unchanged) AND an array of strings (new admin callers); the change is a single line: flags: Array.isArray(flag) ? flag : [flag]"
    - "lib/thinx/jwtlogin.js exposes a new sign_with_impersonation(target_owner, impersonator_owner, callback) method emitting a 15-min JWT with username=target_owner + impersonator_owner claim; NO refresh token issued"
    - "thinx-core.js registers require('./lib/router.admin.js')(app) AFTER the existing require('./lib/router.user.js')(app) line"
    - "All ops use redis@5.8.2 .legacy() callback API — NO async/await on redis calls (per CONTEXT.md C2)"
    - "No new npm packages added — parent-repo package.json dependencies/devDependencies unchanged"
  artifacts:
    - path: "lib/middleware/requireAdmin.js"
      provides: "Admin-gating middleware factory (validateSession + sanitka.owner + app.owner.profile + admin === true check; 401/403)"
      contains: "requireAdmin"
    - path: "lib/router.admin.js"
      provides: "Three admin endpoints (GET users, DELETE session, POST impersonate) all gated by requireAdmin"
      contains: "/api/v2/admin/users"
    - path: "lib/thinx/audit.js"
      provides: "alog.log() patched to accept string OR array flag (1-line non-breaking change)"
      contains: "Array.isArray(flag) ? flag : [flag]"
    - path: "lib/thinx/jwtlogin.js"
      provides: "sign_with_impersonation method (15-min JWT, impersonator_owner claim, no refresh)"
      contains: "sign_with_impersonation"
    - path: "lib/router.js"
      provides: "JWT-verify block extended with Redis blacklist + impersonation audit log + req.session.impersonator_owner injection"
      contains: "revoked:owner:"
    - path: "thinx-core.js"
      provides: "router.admin.js registration"
      contains: "router.admin.js"
  key_links:
    - from: "router.admin.js handlers"
      to: "lib/middleware/requireAdmin.js"
      via: "second argument on app.get/post/delete('/api/v2/admin/*', requireAdmin, handler)"
      pattern: "requireAdmin"
    - from: "lib/router.js JWT verify"
      to: "app.redis_client.get('revoked:owner:' + payload.username, ...)"
      via: "Redis blacklist check after JWT verify; fails open on error"
      pattern: "revoked:owner:"
    - from: "lib/router.js JWT verify"
      to: "req.session.impersonator_owner + alog.log per request"
      via: "when payload.impersonator_owner is present"
      pattern: "impersonator_owner"
    - from: "POST /api/v2/admin/impersonate"
      to: "app.login.sign_with_impersonation"
      via: "issues a 15-min JWT with impersonator_owner claim"
      pattern: "sign_with_impersonation"
---

<objective>
Ship the backend half of Phase 10 (Admin Features) in the parent monorepo `/Users/igraczech/Repositories/thinx-device-api/`. Three new admin endpoints + an admin-gating middleware factory + a Redis session-blacklist hook in the existing auth middleware + a JWT signer for impersonation tokens + a 1-line audit-flag patch + a router registration line. All five mandatory CONTEXT.md corrections (C1-C5) are honored; all six planner-internal decisions (OQ-1..6) are resolved per `10-RESEARCH.md §7`.

Purpose: The Vue console (Phase 10 Wave 2) needs three live backend endpoints — list users, revoke sessions, start impersonation — and a server-side enforcement of the locked decisions (impersonate non-admins only; coarse all-sessions revocation; audit log every impersonated action). Without Wave 1 live in parent `thinx-staging`, Wave 2's `AdminUsers.vue` is a dead page. Wave 0's `admin.spec.js` (console submodule) is forward-looking — it references the Wave 2 route, not Wave 1 endpoints — so the dependency is one-way: Wave 1 backend must be live before Wave 2 frontend deploys via the parent submodule-bump (per memory `deployment-console-thinx-cloud`).

Output: 2 new files (`lib/middleware/requireAdmin.js`, `lib/router.admin.js`) + 4 patched files (`lib/thinx/audit.js`, `lib/thinx/jwtlogin.js`, `lib/router.js`, `thinx-core.js`) — all in the parent monorepo (`/Users/igraczech/Repositories/thinx-device-api/`). Commits land in parent `thinx-staging`. Existing test suite (`npm test` in the parent repo) continues to pass; manual curl smoke verifies the three endpoints behave correctly for admin / non-admin / unauthenticated callers.

CRITICAL CROSS-REPO NOTE: every file path in this plan starts at the parent-monorepo root (`/Users/igraczech/Repositories/thinx-device-api/`), NOT at the console submodule root. `lib/...` here means `/Users/igraczech/Repositories/thinx-device-api/lib/...`. Do not commit any of these changes inside the `services/console/` submodule.
</objective>

<execution_context>
@/Users/igraczech/.claude/get-shit-done/workflows/execute-plan.md
@/Users/igraczech/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/ROADMAP.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/REQUIREMENTS.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-10/10-CONTEXT.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-10/10-RESEARCH.md
@/Users/igraczech/Repositories/thinx-device-api/lib/router.js
@/Users/igraczech/Repositories/thinx-device-api/lib/router.profile.js
@/Users/igraczech/Repositories/thinx-device-api/lib/router.auth.js
@/Users/igraczech/Repositories/thinx-device-api/lib/router.gdpr.js
@/Users/igraczech/Repositories/thinx-device-api/lib/router.logs.js
@/Users/igraczech/Repositories/thinx-device-api/lib/thinx/jwtlogin.js
@/Users/igraczech/Repositories/thinx-device-api/lib/thinx/audit.js
@/Users/igraczech/Repositories/thinx-device-api/lib/thinx/util.js
@/Users/igraczech/Repositories/thinx-device-api/lib/thinx/sanitka.js
@/Users/igraczech/Repositories/thinx-device-api/lib/thinx/owner.js
@/Users/igraczech/Repositories/thinx-device-api/thinx-core.js

<interfaces>
EXISTING JWT-VERIFY BLOCK (lib/router.js:102-113, verbatim):

  if ((typeof (req.headers['authorization']) !== "undefined") || (typeof (req.headers['Authorization']) !== "undefined")) {
    app.login.verify(req, (error, payload) => {
      if (error == null) {
        req.session.owner = payload.username;
        next();
      } else {
        res.status(403).end(); // FIXME: Change to 401 Unauthorized in tests as well!
      }
    });
    return;
  }

Task 4 PATCHES this block to (a) inject req.session.impersonator_owner when payload.impersonator_owner is present, (b) emit a per-request audit-log entry under impersonation (per OQ-6), and (c) perform a Redis blacklist check that fails OPEN on Redis errors (per R1). The patch keeps the existing 403-on-verify-error branch intact and the `return;` at the end.

EXISTING audit.js#log (lib/thinx/audit.js:13-29, only the relevant line):

  let record = {
    "message": message,
    "owner": owner,
    "date": mtime,
    "flags": [flag]   // ← Task 2 changes ONLY this line
  };

Task 2 PATCHES line 27 from `"flags": [flag]` to `"flags": Array.isArray(flag) ? flag : [flag]`. No other change. Existing string-flag callers (router.auth.js:80,97,201,206,312; apikey.js:96; router.google.js:63,225) continue to produce the same `[flag]` single-element array. New array-flag callers in router.admin.js (Task 5) produce multi-element arrays directly.

EXISTING jwtlogin.js methods (lib/thinx/jwtlogin.js:83-120) — reference shape for Task 3:

  sign(uid, callback) {
      this.fetchOrCreateSecretKey((secretkey) => {
          let payload = { username: uid, scope: '/api/', exp: Math.floor(Date.now() / 1000) + (60 * 60) };
          let cb = (err, jwt_token) => { if (err !== null) console.log("[jwt] sign callback error", err); callback(jwt_token); };
          jwt.sign(payload, secretkey, jwt_options, cb);
      });
  }

Task 3 APPENDS a new method sign_with_impersonation(target_owner, impersonator_owner, callback) directly after sign_with_refresh (current line 120) and before the verify_impl method (line 124). 15-min exp, no refresh token, impersonator_owner claim in the payload.

EXISTING router registration block (thinx-core.js:350-358, the require() lines for routers):

  require('./lib/router.google.js')(app);
  require('./lib/router.logs.js')(app);
  require('./lib/router.mesh.js')(app);
  require('./lib/router.profile.js')(app);
  require('./lib/router.rsakey.js')(app);
  require('./lib/router.slack.js')(app);
  require('./lib/router.source.js')(app);
  require('./lib/router.transfer.js')(app);
  require('./lib/router.user.js')(app);

Task 6 INSERTS `require('./lib/router.admin.js')(app);` directly AFTER the existing `require('./lib/router.user.js')(app);` line. The order matters only insofar as the new router must register after the auth middleware in router.js is already wired (which it is — router.js is required earlier in thinx-core.js).

REDIS CLIENT (CONTEXT.md C2 — .legacy() callback API):
The app exposes app.redis_client which is the `.legacy()` wrapper around node-redis 5.8.2. ALL ops are callback-style. Examples from the existing codebase:
  app.redis_client.set('revoked:owner:' + owner, String(Date.now()), (err) => { ... });
  app.redis_client.expire('revoked:owner:' + owner, 7 * 24 * 60 * 60);
  app.redis_client.get('revoked:owner:' + payload.username, (err, ts) => { ... });

DO NOT use async/await on redis calls. DO NOT switch to ioredis. DO NOT import redis from inside any of these files — the .legacy() client is owned by thinx-core.js:99-100 and exposed via app.redis_client.

OWNER PROFILE SHAPE (lib/thinx/owner.js:290-322 — reference for Task 1 admin check):
app.owner.profile(owner, callback) where callback(success, profile). On success, profile is an object that includes:
  { owner: string, username: string, admin: boolean, info: { email: string, ... }, ... }
The admin === true check is the security boundary. profile.email may live at profile.email OR profile.info.email depending on doc version (CONTEXT.md C5; corroborated by Profile.vue:118-135 which reads both forms — Task 5 listUsers similarly tolerates both shapes).

SANITKA + UTIL (existing helpers; reuse verbatim):
  Sanitka: const Sanitka = require("./thinx/sanitka"); const sanitka = new Sanitka();  // sanitka.owner(x) strips bad chars
  Util.validateSession(req)                                                              // returns boolean
  Util.responder(res, success, response)                                                 // standard 200 wrapper
  Util.failureResponse(res, status, message)                                             // standard non-200 wrapper
Path inside `lib/middleware/requireAdmin.js` is "../thinx/util" and "../thinx/sanitka" (one extra `../` because it sits one directory deeper). Path inside `lib/router.admin.js` is "./thinx/util", "./thinx/sanitka", "./thinx/audit" (same depth as the other lib/router.*.js files).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/middleware/requireAdmin.js (admin-gating factory)</name>
  <files>lib/middleware/requireAdmin.js</files>
  <read_first>
    - /Users/igraczech/Repositories/thinx-device-api/lib/router.profile.js — read in full (66 lines). Pattern source for `Util.validateSession`, `sanitka.owner(req.session.owner)`, and `app.owner.profile(owner, callback)` — the same three calls anchor requireAdmin.
    - /Users/igraczech/Repositories/thinx-device-api/lib/thinx/owner.js around lines 290-322 — confirm `profile()` callback shape (success, profile) and that profile.admin is a boolean.
    - /Users/igraczech/Repositories/thinx-device-api/lib/thinx/util.js around lines 56-81 — confirm `validateSession(req)` returns a boolean (truthy when req.session.owner is a non-empty string).
    - /Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-10/10-RESEARCH.md §10 (Complete `requireAdmin.js`) — canonical code body used verbatim below.
    - /Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-10/10-RESEARCH.md §7 OQ-1 — confirms separate-file (option a) is the chosen home for the middleware.
  </read_first>
  <action>
    Create the directory `/Users/igraczech/Repositories/thinx-device-api/lib/middleware/` if it does not yet exist, then create `/Users/igraczech/Repositories/thinx-device-api/lib/middleware/requireAdmin.js` as a thin Express-style middleware factory that closes over the Express `app` (needed for `app.owner.profile`).

    The file MUST export a single factory function taking `app` and returning an Express middleware function. The returned middleware MUST:
      1. Run Util.validateSession(req); return 401 if false. (Mirrors the first line of every handler in router.profile.js.)
      2. Sanitize req.session.owner via sanitka.owner(); return 401 if the result is undefined. (Identical to router.profile.js:12-13.)
      3. Call app.owner.profile(owner, callback). If !success OR !profile OR profile.admin !== true → return 403. Otherwise call next().

    Required imports (verbatim):
      const Util = require("../thinx/util");
      const Sanitka = require("../thinx/sanitka"); const sanitka = new Sanitka();

    Required module.exports shape (verbatim):
      module.exports = function (app) {
        return function requireAdmin(req, res, next) {
          if (!Util.validateSession(req)) return res.status(401).end();
          const owner = sanitka.owner(req.session.owner);
          if (typeof (owner) === "undefined") return res.status(401).end();
          app.owner.profile(owner, (success, profile) => {
            if (!success || !profile || profile.admin !== true) {
              return res.status(403).end();
            }
            next();
          });
        };
      };

    The named inner function (`function requireAdmin(req, res, next)`) is intentional — it shows up in stack traces and Express debug logs, which makes 403s in production easier to triage. Do NOT replace it with an arrow function.

    Why a factory (per CONTEXT.md C5 + RESEARCH §1.2):
      - Middleware needs access to `app.owner` (the Owner instance from thinx-core.js:109).
      - Every other lib/router.*.js file in the codebase uses the same factory shape — this middleware mirrors that convention so future additions (e.g. requireSuperAdmin) live in the same dir.
      - The path `../thinx/util` and `../thinx/sanitka` reflects the one-extra-directory depth of lib/middleware/ relative to lib/.

    DO NOT:
      - Cache the admin flag in Redis (research §0.5 (c) — admin endpoints are low-traffic; one CouchDB lookup per call is fine for v1).
      - Add a per-route admin flag on the JWT payload (research §0.5 (b) — invasive; defer).
      - Use async/await — the existing pattern is callback-based; mixing introduces unnecessary error-handling churn.
      - Set req.session.admin or any other mutable session field — the middleware MUST be idempotent and side-effect-free except for the next() call.
  </action>
  <verify>
    <automated>cd /Users/igraczech/Repositories/thinx-device-api && node --check lib/middleware/requireAdmin.js && test "$(grep -c 'module.exports = function (app)' lib/middleware/requireAdmin.js)" = "1" && test "$(grep -c 'Util.validateSession(req)' lib/middleware/requireAdmin.js)" = "1" && test "$(grep -c 'app.owner.profile(owner' lib/middleware/requireAdmin.js)" = "1" && test "$(grep -c 'profile.admin !== true' lib/middleware/requireAdmin.js)" = "1" && test "$(grep -c 'res.status(403)' lib/middleware/requireAdmin.js)" = "1" && test "$(grep -c 'res.status(401)' lib/middleware/requireAdmin.js)" = "2"</automated>
  </verify>
  <acceptance_criteria>
    - File /Users/igraczech/Repositories/thinx-device-api/lib/middleware/requireAdmin.js exists.
    - node --check exits 0.
    - The file exports a factory: `module.exports = function (app) { return function requireAdmin(req, res, next) { ... }; }`.
    - Exactly one Util.validateSession(req) call.
    - Exactly one app.owner.profile(owner, callback) call.
    - Exactly one `profile.admin !== true` check, falling through to res.status(403).end().
    - Two res.status(401).end() branches (validateSession false; sanitka.owner undefined).
    - require paths use "../thinx/util" and "../thinx/sanitka" (one extra `../` because of the new subdirectory).
    - No async/await anywhere in the file.
  </acceptance_criteria>
  <done>lib/middleware/requireAdmin.js exists as an Express-style middleware factory exporting `module.exports = function (app) { return function requireAdmin(req, res, next) { ... }; }`, with exactly the three security checks above (validateSession, sanitka.owner, profile.admin === true), and parses with node --check.</done>
  <commit>feat(10-01): add requireAdmin middleware factory (lib/middleware/requireAdmin.js)</commit>
</task>

<task type="auto">
  <name>Task 2: Patch lib/thinx/audit.js#log to accept string OR array flag (1-line change)</name>
  <files>lib/thinx/audit.js</files>
  <read_first>
    - /Users/igraczech/Repositories/thinx-device-api/lib/thinx/audit.js — read in full (68 lines). The change is a single line at line 27.
    - /Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-10/10-CONTEXT.md correction C4 — locks the patch shape.
    - /Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-10/10-RESEARCH.md §0.4 + §1.5 — confirms this is non-breaking; existing string-flag callers continue to produce a single-element array.
  </read_first>
  <action>
    Open `/Users/igraczech/Repositories/thinx-device-api/lib/thinx/audit.js`. Locate line 27, which currently reads:

      "flags": [flag]

    Replace it with EXACTLY:

      "flags": Array.isArray(flag) ? flag : [flag]

    Do NOT change any other line. Do NOT touch the function signature `log(owner, message, flag, callback)`. Do NOT add a new method. Do NOT add a JSDoc comment that mentions the array-form — the patch is intentionally minimal and self-documenting.

    Why this is non-breaking (per research §0.4):
      - Every existing caller passes either a string ("info", "danger", "warning") or undefined (defaulted to "info" via the line-14 guard).
      - For string input, `Array.isArray("info")` is false → falls through to `[flag]` → identical behavior to the pre-patch code.
      - For undefined input, line 14 sets flag = "info" before this line runs → same as above.
      - Only new callers in router.admin.js (Task 5) pass arrays like `["admin", "revoke"]` or `["admin", "impersonation", "start"]`.

    Why this matters semantically (per research §1.5):
      - The Phase-7 History page (vue/src/pages/History/History.vue:122-127, 158) filters audit-log rows by `item.flags.some(f => this.auditFlagFilter.includes(f))`. Multi-flag entries appear under any of their flags' filters.
      - If we stringified the array into the message ("admin/impersonation/start") instead of storing all three flags, the History filter would not catch impersonation events under the "admin" filter — losing a key audit-traceability feature.
  </action>
  <verify>
    <automated>cd /Users/igraczech/Repositories/thinx-device-api && node --check lib/thinx/audit.js && test "$(grep -c 'Array.isArray(flag) ? flag : \[flag\]' lib/thinx/audit.js)" = "1" && test "$(grep -c '\"flags\": \[flag\]' lib/thinx/audit.js)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - node --check exits 0.
    - grep -c 'Array.isArray(flag) ? flag : [flag]' returns 1.
    - grep -c '"flags": [flag]' returns 0 (the old line is gone).
    - The function signature `log(owner, message, flag, callback)` is byte-identical.
    - No other line in the file changed (diff shows exactly one line modified).
    - Existing parent-repo `npm test` suite still passes (or fails in the same places it failed pre-patch — the patch must not introduce new failures).
    - Manual code-read: line 14 default-to-"info" guard is preserved; line 27 is the only modification.
  </acceptance_criteria>
  <done>lib/thinx/audit.js line 27 now reads `"flags": Array.isArray(flag) ? flag : [flag]`. Every other line is byte-identical. node --check passes. Existing test suite still passes.</done>
  <commit>fix(10-01): audit.log accepts string OR array flag (non-breaking)</commit>
</task>

<task type="auto">
  <name>Task 3: Add sign_with_impersonation method to lib/thinx/jwtlogin.js</name>
  <files>lib/thinx/jwtlogin.js</files>
  <read_first>
    - /Users/igraczech/Repositories/thinx-device-api/lib/thinx/jwtlogin.js — read in full (160 lines). The new method mirrors the shape of sign_with_refresh (lines 98-120) but emits only an access token with a 15-min exp and an impersonator_owner claim.
    - /Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-10/10-RESEARCH.md §1.4 (JWT Issuance with Custom Claims) — canonical body used below.
    - /Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-10/10-RESEARCH.md §3.3 + A1 — confirms jsonwebtoken@9.0.3 auto-adds `iat` to the payload by default (no `noTimestamp` set anywhere); the blacklist check in Task 4 relies on this.
  </read_first>
  <action>
    Open `/Users/igraczech/Repositories/thinx-device-api/lib/thinx/jwtlogin.js`. Locate the `sign_with_refresh` method (current lines 98-120). Insert a new method `sign_with_impersonation(target_owner, impersonator_owner, callback)` immediately after `sign_with_refresh` (after its closing `}` at line 120) and BEFORE the `// Step 3: Verify` comment block (current line 122).

    Required new method body (verbatim — mirrors existing sign() at lines 83-96 in style):

      sign_with_impersonation(target_owner, impersonator_owner, callback) {
          this.fetchOrCreateSecretKey((secretkey) => {
              let payload = {
                  username: target_owner,                 // becomes req.session.owner downstream (the impersonated user)
                  scope: '/api/',
                  impersonator_owner: impersonator_owner, // the admin who started impersonation
                  exp: Math.floor(Date.now() / 1000) + (15 * 60) // 15 minutes — hard cap, no refresh token
              };
              let cb = (err, jwt_token) => {
                  if (err !== null) console.log("[jwt] impersonation sign error", err);
                  callback(jwt_token);
              };
              jwt.sign(payload, secretkey, jwt_options, cb);
          });
      }

    KEY contract notes:
      - `username: target_owner` — the JWT identifies the impersonated user. The auth middleware (Task 4) reads payload.username into req.session.owner; downstream handlers operate as the target user.
      - `impersonator_owner: impersonator_owner` — the audit-trail claim. Task 4 reads this into req.session.impersonator_owner and emits a per-request audit log.
      - exp: now + 900 seconds (15 minutes). The locked decision; no refresh token issued (per CONTEXT.md "Exit impersonation = force re-login").
      - jsonwebtoken@9.0.3 auto-adds `iat` to the payload (Assumption A1; verified by reading jwtlogin.js — no `noTimestamp: true` anywhere). The blacklist check in Task 4 depends on this.

    DO NOT:
      - Issue a refresh token alongside (research §1.4 — locked: impersonation is short-lived; exit = re-login).
      - Add a `jti` claim (research §7 OQ-7 — defer; v1 uses the (owner, iat) blacklist form only).
      - Change `sign()` or `sign_with_refresh()` — both stay byte-identical.
      - Use async/await — the existing methods are callback-based.
      - Export the new method as a top-level function — it MUST be a method on the JWTLogin class (alongside sign and sign_with_refresh).

    Indentation: use 4 spaces (matches the file's existing indent — see sign() at line 83). The method opens at the same column as sign() and sign_with_refresh().
  </action>
  <verify>
    <automated>cd /Users/igraczech/Repositories/thinx-device-api && node --check lib/thinx/jwtlogin.js && test "$(grep -c 'sign_with_impersonation(target_owner, impersonator_owner, callback)' lib/thinx/jwtlogin.js)" = "1" && test "$(grep -c 'impersonator_owner: impersonator_owner' lib/thinx/jwtlogin.js)" = "1" && test "$(grep -c '(15 \* 60)' lib/thinx/jwtlogin.js)" = "1" && test "$(grep -c 'sign(uid, callback)' lib/thinx/jwtlogin.js)" = "1" && test "$(grep -c 'sign_with_refresh(uid, callback)' lib/thinx/jwtlogin.js)" = "1"</automated>
  </verify>
  <acceptance_criteria>
    - node --check exits 0.
    - grep -c 'sign_with_impersonation(target_owner, impersonator_owner, callback)' returns 1.
    - grep -c 'impersonator_owner: impersonator_owner' returns 1.
    - grep -c '(15 * 60)' returns 1 (the 15-minute exp arithmetic; no other place uses 900s).
    - The existing sign() and sign_with_refresh() methods are byte-identical — verified by `git diff lib/thinx/jwtlogin.js` showing only an additive block.
    - The new method sits BETWEEN sign_with_refresh (closing `}` at original line 120) and the `// Step 3: Verify` comment block.
    - No refresh-token sibling is issued by the new method.
  </acceptance_criteria>
  <done>lib/thinx/jwtlogin.js exposes a new sign_with_impersonation method as a sibling of sign and sign_with_refresh. The method emits a single JWT with username=target_owner, an impersonator_owner claim, and a 15-minute exp. No refresh token. node --check passes. sign and sign_with_refresh are byte-identical.</done>
  <commit>feat(10-01): JWTLogin.sign_with_impersonation (15-min token, impersonator_owner claim)</commit>
</task>

<task type="auto">
  <name>Task 4: Patch lib/router.js:102-113 — Redis blacklist + impersonation injection + per-request audit log</name>
  <files>lib/router.js</files>
  <read_first>
    - /Users/igraczech/Repositories/thinx-device-api/lib/router.js — read in full (205 lines). The patch targets the JWT-verify block at 102-113. The next block (Cookie or other auth, starting at line 115) MUST be byte-identical after the patch.
    - /Users/igraczech/Repositories/thinx-device-api/lib/router.auth.js around lines 128-129 — reference for the .legacy() redis.set + redis.expire callback pattern.
    - /Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-10/10-RESEARCH.md §1.3 + §4.4 + §7 OQ-6 — the canonical patch body.
    - /Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-10/10-RESEARCH.md §6.1 + R1 — the fail-OPEN policy on Redis errors is locked.
    - /Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-10/10-RESEARCH.md §6.5 + R5 — the auth middleware applies to EVERY /api/* request; the per-request Redis call is acceptable for v1 (low-traffic single-developer config).
    - Confirm the file currently does NOT import the audit module — Task 4 adds that import near the top.
  </read_first>
  <action>
    Open `/Users/igraczech/Repositories/thinx-device-api/lib/router.js`. Make TWO edits.

    Edit (a) — Add the audit-log import. Find the existing top-of-file requires (around lines 1-15, alongside other module-level imports). Add the following line:

      const AuditLog = require("./thinx/audit"); var alog = new AuditLog();

    Place it directly after the existing const-requires near the top of the file (mirror the placement used by router.auth.js — `const AuditLog = require("./thinx/audit"); var alog = new AuditLog();` is the canonical idiom; same instance per file). If the file already has a Sanitka or Util require near the top, place the AuditLog line directly below it. Do NOT use an ES-module import; do NOT use const-let mixing — match the file's existing var/const convention exactly.

    Edit (b) — Replace the JWT-verify block at lines 102-113. The current block (verbatim) is:

      if ((typeof (req.headers['authorization']) !== "undefined") || (typeof (req.headers['Authorization']) !== "undefined")) {
        app.login.verify(req, (error, payload) => {
          // for JWT debugging: console.log("🔨 [debug] JWT Secret verification result:", { error }, { payload });
          if (error == null) {
            req.session.owner = payload.username;
            next();
          } else {
            res.status(403).end(); // FIXME: Change to 401 Unauthorized in tests as well!
          }
        });
        return;
      }

    Replace it with the following (verbatim — preserve the original 6-space body indentation of this top-level if-block):

      if ((typeof (req.headers['authorization']) !== "undefined") || (typeof (req.headers['Authorization']) !== "undefined")) {
        app.login.verify(req, (error, payload) => {
          // for JWT debugging: console.log("🔨 [debug] JWT Secret verification result:", { error }, { payload });
          if (error == null) {
            req.session.owner = payload.username;
            // Impersonation claim injection + per-request audit log (Phase 10 ADMIN-03, OQ-6).
            if (payload.impersonator_owner) {
              req.session.impersonator_owner = payload.impersonator_owner;
              alog.log(
                payload.impersonator_owner,
                "IMPERSONATED " + req.method + " " + req.path + " as " + payload.username,
                ["admin", "impersonation"]
              );
            }
            // Session blacklist check (Phase 10 ADMIN-02). Fails OPEN on Redis errors (R1).
            app.redis_client.get("revoked:owner:" + payload.username, (rerr, ts) => {
              if (rerr) {
                console.log("[warning] blacklist check failed", rerr);
                return next();
              }
              if (ts) {
                const iatMs = (payload.iat || 0) * 1000;
                if (iatMs < parseInt(ts, 10)) {
                  return res.status(401).end();
                }
              }
              next();
            });
          } else {
            res.status(403).end(); // FIXME: Change to 401 Unauthorized in tests as well!
          }
        });
        return;
      }

    Why this exact shape (per research §4.4 + §6.1 + §7 OQ-6):
      - Impersonation injection runs BEFORE the blacklist check so the audit log fires even if the request is about to be 401'd by the blacklist — admins still want to see "impersonator X tried to act after Y revoked them".
      - The redis.get callback wraps the whole next-or-401 decision; on Redis errors we log + next() (fail-open per R1 — otherwise a Redis outage takes the whole site down). The cost is that during a Redis outage, revoked tokens stay valid; that's the documented trade-off in §6.1.
      - parseInt(ts, 10) — the Redis value is a string (because .legacy() returns strings from SET). The Date.now() comparison is in milliseconds; payload.iat is in seconds per RFC 7519 (jsonwebtoken@9.0.3 auto-adds it — A1).
      - The "Cookie or other auth" block at line 115+ MUST remain byte-identical. The patch is bounded by the existing top-level if-block; everything after it is untouched.

    DO NOT:
      - Add a `jti` claim check (out of scope; v1 uses iat).
      - Cache the blacklist value (Redis is fast enough; caching adds a stale-data window for revocation, which defeats the point).
      - Skip the blacklist check for /api/device/* (research §6.6 — the optimization is unnecessary for v1; device endpoints use API-key auth not JWT, so they don't enter this branch anyway).
      - Use a Promise or async wrapper around the redis.get — the .legacy() API is callback-only.
      - Add `else if` chains that change the original branching structure for the verify-failure case.
  </action>
  <verify>
    <automated>cd /Users/igraczech/Repositories/thinx-device-api && node --check lib/router.js && test "$(grep -c 'revoked:owner:' lib/router.js)" = "1" && test "$(grep -c 'impersonator_owner' lib/router.js)" -ge "3" && test "$(grep -c 'IMPERSONATED ' lib/router.js)" = "1" && test "$(grep -c 'new AuditLog()' lib/router.js)" = "1" && test "$(grep -c 'blacklist check failed' lib/router.js)" = "1" && test "$(grep -c 'res.status(401).end()' lib/router.js)" -ge "1"</automated>
  </verify>
  <acceptance_criteria>
    - node --check exits 0.
    - The AuditLog require + instantiation line appears exactly once near the top of the file.
    - grep 'revoked:owner:' returns 1 (the single redis.get key in the JWT-verify block).
    - grep 'impersonator_owner' returns ≥ 3 (claim read, session assignment, alog.log first arg).
    - grep 'IMPERSONATED ' returns 1.
    - The "Cookie or other auth" block (currently starting around line 115) is byte-identical — verified by `git diff lib/router.js` showing only the JWT-verify block + the top-of-file require modified.
    - The original 403-on-verify-error branch is preserved.
    - The function still ends the JWT-auth path with `return;` after the if-block (so the rest of the middleware doesn't run for JWT-authed requests).
    - No async/await on any redis call.
    - Manual code-read: the alog.log call DOES NOT log the JWT itself or any portion of the token (per §6.10 — log owner IDs only).
  </acceptance_criteria>
  <done>lib/router.js JWT-verify block now (a) injects req.session.impersonator_owner from payload.impersonator_owner when present, (b) emits one alog.log(impersonator_owner, "IMPERSONATED METHOD path as username", ["admin","impersonation"]) per impersonated request, and (c) performs a fail-open Redis blacklist check that 401s when payload.iat * 1000 < parseInt(ts, 10). All other code in lib/router.js is byte-identical. node --check passes.</done>
  <commit>feat(10-01): router.js JWT-verify gains Redis blacklist + impersonation injection + audit hook</commit>
</task>

<task type="auto">
  <name>Task 5: Create lib/router.admin.js with three endpoints (GET users, DELETE session, POST impersonate)</name>
  <files>lib/router.admin.js</files>
  <read_first>
    - /Users/igraczech/Repositories/thinx-device-api/lib/router.profile.js — read in full. Factory function shape (`module.exports = function (app) { ... }`); use of `Util.validateSession`, `Util.responder`, `sanitka.owner`, `app.owner.profile`. Every handler in this plan follows the same skeleton.
    - /Users/igraczech/Repositories/thinx-device-api/lib/router.gdpr.js around lines 49-53 — reference for the redis.expire + redis.keys callback pattern.
    - /Users/igraczech/Repositories/thinx-device-api/lib/thinx/owner.js around lines 290-322 — confirm profile() callback shape; profile.admin is boolean; profile.email vs profile.info.email duality.
    - /Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-10/10-RESEARCH.md §10 (Complete `router.admin.js` sketch) — canonical body used below.
    - /Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-10/10-CONTEXT.md OQ-B — device_count: 0 placeholder is locked for v1.
    - /Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-10/10-RESEARCH.md §6.11 — `userlib.list({ include_docs: true })` is the chosen enumeration path for v1 (no new design-doc view).
  </read_first>
  <action>
    Create `/Users/igraczech/Repositories/thinx-device-api/lib/router.admin.js` as a new factory-pattern router file. The file MUST be the third lib/router.*.js entry that wires Util + Sanitka + AuditLog at the top, instantiate the requireAdmin middleware via the factory from Task 1, and register THREE endpoints — all gated by requireAdmin.

    Top-of-file imports (verbatim — mirror lib/router.profile.js + lib/router.auth.js):

      const Util = require("./thinx/util");
      const Sanitka = require("./thinx/sanitka"); var sanitka = new Sanitka();
      const AuditLog = require("./thinx/audit"); var alog = new AuditLog();

    Factory + middleware instantiation:

      module.exports = function (app) {
        const requireAdmin = require("./middleware/requireAdmin")(app);
        const user = app.owner;
        const userlib = user.userlib;

        // ... handler functions ...

        app.get("/api/v2/admin/users", requireAdmin, listUsers);
        app.delete("/api/v2/admin/session/:owner", requireAdmin, revokeSession);
        app.post("/api/v2/admin/impersonate", requireAdmin, impersonate);
      };

    Handler 1 — listUsers (ADMIN-01):

      function listUsers(req, res) {
        userlib.list({ include_docs: true }, (err, body) => {
          if (err) return Util.failureResponse(res, 500, "user_list_failed");
          const users = body.rows
            .filter(r => r.doc && r.doc.owner)
            .map(r => ({
              owner: r.doc.owner,
              username: r.doc.username,
              email: (r.doc.info && r.doc.info.email) || r.doc.email,
              admin: r.doc.admin === true,
              created: r.doc.created || r.doc.create_date || null,
              last_login: r.doc.last_seen || r.doc.last_login || null,
              device_count: 0   // v1.1 follow-up (locked OQ-B); real count via Redis cache or CouchDB view
            }));
          Util.responder(res, true, users);
        });
      }

    Notes:
      - `userlib.list({ include_docs: true })` is `_all_docs` against `managed_users`. Acceptable for < 500 users (research §6.11 + Assumption A2).
      - The `r.doc.owner` filter excludes design-doc rows (which have an `_id` prefix of `_design/`).
      - `email` reads from both shapes (profile.info.email and profile.email) per CONTEXT.md C5.
      - `device_count: 0` is the locked v1 placeholder (OQ-B). The schema field MUST exist so Wave 2's AdminUsers.vue can render the column without conditionals — the v1.1 follow-up replaces the literal 0 with a real count.
      - Each row also exposes `created` and `last_login` to match the REQUIREMENTS.md ADMIN-01 row shape exactly.

    Handler 2 — revokeSession (ADMIN-02):

      function revokeSession(req, res) {
        const target_owner = sanitka.owner(req.params.owner);
        const admin_owner = sanitka.owner(req.session.owner);
        if (!target_owner) return Util.failureResponse(res, 400, "missing_owner");
        const key = "revoked:owner:" + target_owner;
        const now_ms = String(Date.now());
        app.redis_client.set(key, now_ms, (serr) => {
          if (serr) return Util.failureResponse(res, 500, "redis_write_failed");
          app.redis_client.expire(key, 7 * 24 * 60 * 60);  // refresh-token max lifetime
          alog.log(admin_owner, "Sessions revoked for " + target_owner, ["admin", "revoke"]);
          Util.responder(res, true, "sessions_revoked");
        });
      }

    Notes:
      - TTL 7 * 24 * 60 * 60 seconds = the refresh-token max lifetime (research §1.3). After 7 days no JWT issued before the revoke can still be valid; the entry becomes pointless.
      - The auth-middleware patch in Task 4 reads this key on every JWT-authed request; the iat comparison rejects pre-revoke tokens.
      - alog.log uses the array form ["admin", "revoke"] — possible only because Task 2 patched audit.js#log to accept arrays. The History page will index this entry under both "admin" and "revoke" flag filters.
      - Coarse granularity (all sessions per owner) is the locked decision; do NOT key by JWT jti or per-session.

    Handler 3 — impersonate (ADMIN-03):

      function impersonate(req, res) {
        const target_owner = sanitka.owner(req.body && req.body.owner);
        const admin_owner = sanitka.owner(req.session.owner);
        if (!target_owner) return Util.failureResponse(res, 400, "missing_owner");
        user.profile(target_owner, (success, profile) => {
          if (!success) return Util.failureResponse(res, 404, "target_not_found");
          if (profile.admin === true) return Util.failureResponse(res, 403, "cannot_impersonate_admin");
          app.login.sign_with_impersonation(target_owner, admin_owner, (token) => {
            if (!token) return Util.failureResponse(res, 500, "token_sign_failed");
            alog.log(admin_owner, "Impersonation started for " + target_owner, ["admin", "impersonation", "start"]);
            Util.responder(res, true, { access_token: token });
          });
        });
      }

    Notes:
      - `req.body && req.body.owner` — defensive guard; req.body may be undefined if Content-Type was wrong on the request (Express body-parser declines).
      - Reject when `profile.admin === true` returns 403 with response "cannot_impersonate_admin" — the locked decision; do NOT issue a token even if the admin caller is the same person as the target.
      - The audit entry runs BEFORE Util.responder so an admin acting in bad faith can't game the timing — the log is durable even if the response fails.
      - No refresh token is issued (locked); Wave 2 (AdminUsers.vue) writes only `localStorage.accessToken` and removes `refreshToken`.

    DO NOT:
      - Add a separate Redis impersonation ledger (research §0 Deferred Ideas — "audit log alone covers it").
      - Add a `/api/v2/admin/impersonate/exit` endpoint (research §7 OQ-7 — "force re-login" is the locked flow; client-side clearSession suffices).
      - Compute device_count in v1 (locked OQ-B — placeholder 0).
      - Hand-roll JWT signing — always call app.login.sign_with_impersonation (the JWTLogin method added in Task 3).
      - Log the JWT itself in the audit message (research §6.10 — never log tokens).
      - Use async/await for redis or CouchDB ops — callbacks only (CONTEXT.md C2).
      - Add v1 endpoints for v1.1 features (impersonate-an-admin with multi-admin approval; per-session revocation; account suspension).
  </action>
  <verify>
    <automated>cd /Users/igraczech/Repositories/thinx-device-api && node --check lib/router.admin.js && test "$(grep -c 'module.exports = function (app)' lib/router.admin.js)" = "1" && test "$(grep -c 'require(\"./middleware/requireAdmin\")(app)' lib/router.admin.js)" = "1" && test "$(grep -c 'app.get(\"/api/v2/admin/users\"' lib/router.admin.js)" = "1" && test "$(grep -c 'app.delete(\"/api/v2/admin/session/:owner\"' lib/router.admin.js)" = "1" && test "$(grep -c 'app.post(\"/api/v2/admin/impersonate\"' lib/router.admin.js)" = "1" && test "$(grep -c 'device_count: 0' lib/router.admin.js)" = "1" && test "$(grep -c 'sign_with_impersonation' lib/router.admin.js)" = "1" && test "$(grep -c 'cannot_impersonate_admin' lib/router.admin.js)" = "1" && test "$(grep -c '\\[\"admin\", \"revoke\"\\]' lib/router.admin.js)" = "1" && test "$(grep -c '\\[\"admin\", \"impersonation\", \"start\"\\]' lib/router.admin.js)" = "1"</automated>
  </verify>
  <acceptance_criteria>
    - File /Users/igraczech/Repositories/thinx-device-api/lib/router.admin.js exists.
    - node --check exits 0.
    - The file is a factory: `module.exports = function (app) { ... }`.
    - The requireAdmin middleware is instantiated exactly once via `require("./middleware/requireAdmin")(app)`.
    - Three endpoints registered, each with `requireAdmin` as the second argument: GET /api/v2/admin/users, DELETE /api/v2/admin/session/:owner, POST /api/v2/admin/impersonate.
    - Every row of the users response includes `device_count: 0` (literal).
    - The impersonate handler rejects `profile.admin === true` with response "cannot_impersonate_admin" (verified by grep).
    - alog.log array-form is used: `["admin", "revoke"]` for the revoke action and `["admin", "impersonation", "start"]` for impersonation start.
    - No async/await on redis or CouchDB calls.
    - The file does NOT introduce any new top-level Express middleware (only handler registrations).
    - Manual code-read: no JWT or token-shaped string is ever interpolated into an alog.log message.
  </acceptance_criteria>
  <done>lib/router.admin.js exports a factory that registers three admin endpoints under /api/v2/admin/* — all gated by requireAdmin. listUsers returns rows including device_count: 0; revokeSession writes a Redis blacklist entry + audit log; impersonate returns a 15-min JWT (via app.login.sign_with_impersonation) and rejects admin targets with 403. node --check passes.</done>
  <commit>feat(10-01): router.admin.js — GET users / DELETE session / POST impersonate</commit>
</task>

<task type="auto">
  <name>Task 6: Register router.admin.js in thinx-core.js</name>
  <files>thinx-core.js</files>
  <read_first>
    - /Users/igraczech/Repositories/thinx-device-api/thinx-core.js around lines 335-360 — confirm the router-registration block order. The new line sits immediately after `require('./lib/router.user.js')(app);`.
  </read_first>
  <action>
    Open `/Users/igraczech/Repositories/thinx-device-api/thinx-core.js`. Locate the require block around lines 350-358 that currently reads:

      require('./lib/router.google.js')(app);
      require('./lib/router.logs.js')(app);
      require('./lib/router.mesh.js')(app);
      require('./lib/router.profile.js')(app);
      require('./lib/router.rsakey.js')(app);
      require('./lib/router.slack.js')(app);
      require('./lib/router.source.js')(app);
      require('./lib/router.transfer.js')(app);
      require('./lib/router.user.js')(app);

    Insert immediately after the `require('./lib/router.user.js')(app);` line (i.e. as the new last entry of this block, before the next non-require line):

      require('./lib/router.admin.js')(app);

    Match the indentation of the surrounding lines (12 spaces — same as the existing router requires). Do NOT touch any other require. Do NOT reorder the existing lines.

    Why this position:
      - Router registration is order-insensitive for path-distinct routers — /api/v2/admin/* does not collide with any existing route.
      - Placing after router.user.js keeps the alphabetical-ish grouping of user-related routers together.
      - The new admin router depends on app.owner and app.login both being initialized — both are set earlier in thinx-core.js (~lines 109 and 130-ish); the require call here resolves them lazily, so order within this block doesn't matter beyond stylistic convention.
  </action>
  <verify>
    <automated>cd /Users/igraczech/Repositories/thinx-device-api && node --check thinx-core.js && test "$(grep -c \"require('./lib/router.admin.js')(app);\" thinx-core.js)" = "1" && test "$(grep -c \"require('./lib/router.user.js')(app);\" thinx-core.js)" = "1"</automated>
  </verify>
  <acceptance_criteria>
    - node --check exits 0.
    - grep -c "require('./lib/router.admin.js')(app);" returns 1.
    - The new line sits directly below the existing `require('./lib/router.user.js')(app);` line.
    - All other router requires are unmoved and unmodified.
    - Indentation matches the surrounding block (12 spaces).
  </acceptance_criteria>
  <done>thinx-core.js registers `require('./lib/router.admin.js')(app);` immediately after `require('./lib/router.user.js')(app);`. node --check passes. No other line in thinx-core.js changed.</done>
  <commit>feat(10-01): register router.admin.js in thinx-core</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → `/api/v2/admin/*` | Authenticated request bearing a JWT; the auth middleware verifies, the requireAdmin middleware re-checks admin status via CouchDB. |
| JWT payload (`impersonator_owner` claim) → `req.session.impersonator_owner` | Trusted because the JWT is HS512-signed by the server using the Redis-held secret; clients cannot forge the claim. |
| Redis blacklist key (`revoked:owner:{owner}`) → auth middleware | Trusted; Redis is in the same Docker network; the .legacy() callback path is the only one used. Fails OPEN on Redis errors (R1). |
| Admin endpoint → `userlib.list` | CouchDB returns user docs including `admin: boolean`; the admin flag is the only thing the response can reveal that wouldn't already be in a normal user-profile fetch. |
| `req.body.owner` on `/admin/impersonate` → `sanitka.owner()` → CouchDB lookup | Untrusted input sanitized via the same `sanitka.owner()` pattern used across the codebase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Elevation of Privilege | requireAdmin middleware | mitigate | Always re-checks `profile.admin === true` via `app.owner.profile()` per request (per CONTEXT.md C5). No JWT-claim cache — the admin flag is read from CouchDB on every admin call. Forgery requires CouchDB tampering, not JWT manipulation. |
| T-10-01-02 | Tampering | Redis blacklist key | mitigate | Key writes only happen via DELETE /api/v2/admin/session/:owner (gated by requireAdmin). The auth middleware reads — never writes — the key. TTL of 7 days bounds memory growth. |
| T-10-01-03 | Denial of Service | Redis outage cascading to 401-all | mitigate | The blacklist check in lib/router.js fails OPEN on Redis errors (R1): logs `[warning] blacklist check failed` and continues. Trade-off: revoked tokens stay valid during outages — documented in §6.1. |
| T-10-01-04 | Repudiation | Impersonation actions untraceable | mitigate | Every authenticated request under an impersonation token emits an alog.log entry with flags ["admin","impersonation"] (per OQ-6). The impersonation start event additionally emits flags ["admin","impersonation","start"]. Admins cannot disclaim actions taken while impersonating. |
| T-10-01-05 | Elevation of Privilege | Cross-admin impersonation | mitigate | POST /api/v2/admin/impersonate rejects `profile.admin === true` with 403 "cannot_impersonate_admin". The locked decision; no exception flag, no override. |
| T-10-01-06 | Information Disclosure | User list exposes all user records to admin | accept | This is the intended capability (ADMIN-01). The list contains only fields the admin would otherwise see via individual profile lookups (owner, username, email, admin, created, last_seen). No password hashes, no API keys, no JWT secrets are included in the response. |
| T-10-01-07 | Tampering | Stolen impersonation JWT used after admin "Exit" | accept | 15-min hard exp bounds blast radius; audit log captures issuance (T-10-01-04 traceability). No server-side kill switch in v1 (locked decision; documented R2 + §7 OQ-7). |
| T-10-01-08 | Repudiation | alog.log writes are async with no retry on CouchDB failure | accept | Matches the existing pattern across router.auth.js et al. CouchDB durability is the project's standing assumption. Loss rate is the same as for every other audit-log write in the codebase. |
| T-10-01-09 | Information Disclosure | iat-vs-blacklist race window (~1ms) | accept | Sub-millisecond window between revoke write and in-flight request acceptance; inherent to async writes. Documented in R3 / §6.2. The security model already accepts that a stolen JWT is valid until exp; this race window is strictly smaller than that gap. |
| T-10-01-10 | Tampering | sanitka.owner on req.body.owner / req.params.owner | mitigate | Every untrusted owner-id input passes through sanitka.owner() (per the existing codebase convention — confirmed in router.profile.js:12). Bad chars are stripped; undefined returns early with 400. |
| T-10-01-SC | Tampering | npm supply chain | accept | This plan installs NO new npm packages. Backend keeps redis@5.8.2 (.legacy()), jsonwebtoken@9.0.3, nano@10.1.4 — all years-old, multi-million-downloads, MIT. |
</threat_model>

<verification>
1. cd /Users/igraczech/Repositories/thinx-device-api && node --check lib/middleware/requireAdmin.js && node --check lib/router.admin.js && node --check lib/router.js && node --check lib/thinx/audit.js && node --check lib/thinx/jwtlogin.js && node --check thinx-core.js — all exit 0.

2. cd /Users/igraczech/Repositories/thinx-device-api && npm test (or whatever the existing test command is in package.json) — existing parent-repo test suite still passes; no new failures.

3. Manual curl smoke (against a local dev backend with seeded admin + non-admin accounts):
   a. Login as an admin user — obtain access_token.
   b. curl -s -H "Authorization: Bearer $ADMIN_TOKEN" http://localhost:7442/api/v2/admin/users — expect 200 + JSON array of user rows, each with device_count: 0.
   c. curl -s -X DELETE -H "Authorization: Bearer $ADMIN_TOKEN" http://localhost:7442/api/v2/admin/session/<non-admin-owner> — expect 200; then redis-cli GET 'revoked:owner:<non-admin-owner>' returns the timestamp; redis-cli TTL returns ~604800.
   d. Using the non-admin user's prior access_token (issued before the revoke): curl -s -H "Authorization: Bearer $NONADMIN_TOKEN" http://localhost:7442/api/v2/profile — expect 401 (blacklist match — iat < ts).
   e. curl -s -X POST -H "Authorization: Bearer $ADMIN_TOKEN" -H "Content-Type: application/json" -d '{"owner":"<non-admin-owner>"}' http://localhost:7442/api/v2/admin/impersonate — expect 200 + {access_token: "..."} where the JWT decodes to {username: non-admin-owner, impersonator_owner: admin-owner, exp: now+900}.
   f. curl -s -X POST -H "Authorization: Bearer $ADMIN_TOKEN" -H "Content-Type: application/json" -d '{"owner":"<another-admin-owner>"}' http://localhost:7442/api/v2/admin/impersonate — expect 403 "cannot_impersonate_admin".
   g. curl -s -H "Authorization: Bearer $IMPERSONATION_TOKEN" http://localhost:7442/api/v2/profile — expect 200 + the impersonated user's profile; tail the audit log table and confirm a new row with flags ["admin","impersonation"].
   h. curl -s -H "Authorization: Bearer $NONADMIN_TOKEN" http://localhost:7442/api/v2/admin/users — expect 403.
   i. curl -s http://localhost:7442/api/v2/admin/users — expect 401 (no Authorization header).

4. Audit-log verification — open the History page (or query managed_logs directly):
   a. The DELETE /admin/session call produced a row with flags: ["admin","revoke"] and message "Sessions revoked for <target>".
   b. The POST /admin/impersonate call produced a row with flags: ["admin","impersonation","start"] and message "Impersonation started for <target>".
   c. Each request made while impersonating produced one row with flags: ["admin","impersonation"] and message "IMPERSONATED <METHOD> <path> as <target>".

5. Verify existing audit callers (router.auth.js, apikey.js, router.google.js) still produce single-element flag arrays — open any pre-existing audit log row and confirm flags is still ["info"] / ["error"] / etc.

6. Verify nothing else in the parent repo regressed: cd /Users/igraczech/Repositories/thinx-device-api && git diff --stat — should show exactly two new files (lib/middleware/requireAdmin.js, lib/router.admin.js) and four modified files (lib/thinx/audit.js, lib/thinx/jwtlogin.js, lib/router.js, thinx-core.js). No other diff.
</verification>

<success_criteria>
- All six tasks above land in the parent monorepo (`/Users/igraczech/Repositories/thinx-device-api/`), NOT in the console submodule. The console submodule's git status is clean for this wave.
- Existing parent-repo test suite still passes — no new failures introduced.
- All three admin endpoints respond correctly per the curl smoke matrix in <verification>.
- Audit log shows the three flag shapes documented above; existing audit callers continue to produce single-element flag arrays (non-breaking patch).
- Redis blacklist check fails OPEN on Redis errors (R1): bringing Redis down temporarily does NOT cause /api/v2/profile or any other JWT-authed request to 401.
- The locked decisions are honored at the wire level: impersonate-an-admin → 403; coarse revocation; audit-log-only impersonation logging.
- No new npm packages installed in the parent repo (`/Users/igraczech/Repositories/thinx-device-api/package.json` unchanged).
- Wave 1 is deployable independently — the new endpoints can sit live behind admin auth without any frontend consumer (Wave 2 deploys via the parent submodule-bump path per memory `deployment-console-thinx-cloud`).
- Each task commit is atomic and individually revertable (six commits suggested under the `feat(10-01):` / `fix(10-01):` prefixes).
</success_criteria>

<output>
Create /Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-10/10-01-SUMMARY.md when done.

Suggested commit sequence (parent monorepo, branch `thinx-staging`):
  1. feat(10-01): add requireAdmin middleware factory (lib/middleware/requireAdmin.js)
  2. fix(10-01): audit.log accepts string OR array flag (non-breaking)
  3. feat(10-01): JWTLogin.sign_with_impersonation (15-min token, impersonator_owner claim)
  4. feat(10-01): router.js JWT-verify gains Redis blacklist + impersonation injection + audit hook
  5. feat(10-01): router.admin.js — GET users / DELETE session / POST impersonate
  6. feat(10-01): register router.admin.js in thinx-core

Out of scope for this wave (do not implement; explicit `OUT OF SCOPE` markers):
- Frontend AdminUsers.vue, ImpersonationBanner.vue, store/admin.js, Routes.js entry, Layout.vue banner mount → Wave 2 (10-02-PLAN.md).
- Profile.vue Admin-tab placeholder swap → Wave 3 (10-03-PLAN.md).
- Sidebar.vue conditional Admin NavLink → Wave 3 (10-03-PLAN.md) per OQ-A.
- Cypress assertion flip → Wave 3 (10-03-PLAN.md).
- Real device_count aggregation → v1.1 follow-up (locked OQ-B; v1 ships 0 placeholder).
- Server-side impersonation kill-switch endpoint → deferred (R2 / §7 OQ-7); 15-min TTL bounds blast radius for v1.
- Per-session granularity on revocation → deferred (locked; coarse is the v1 spec).
</output>
