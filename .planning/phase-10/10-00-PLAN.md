---
phase: 10-admin-features
plan: 00
type: execute
wave: 0
depends_on: []
repo: console-submodule
files_modified:
  - vue/cypress/integration/admin.spec.js
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
tags:
  - cypress
  - e2e
  - test-stub
  - vue
  - admin

must_haves:
  truths:
    - "vue/cypress/integration/admin.spec.js exists and uses describe('Admin Features feature', function() { ... })"
    - "beforeEach calls cy.viewport(1536, 754), cy.login(), and cy.visit('http://localhost:3000/#/app/admin/users') — admin pages live under /app and require authentication"
    - "The spec contains exactly 4 it() blocks: ADMIN-01 user-list rendering, ADMIN-02 revoke confirm + DELETE flow, ADMIN-03 impersonate confirm + banner flow, ADMIN-03 negative (Impersonate hidden on admin rows)"
    - "Each it() body is a single TODO comment, no assertions, no cy.get / cy.intercept — assertions arrive in Wave 3 after Wave 2 ships"
    - "No it.only anywhere"
    - "cy.login() is called exactly once — inside beforeEach (NOT inside any it() body) — because every admin spec is gated by /app authentication"
    - "The file ends with a single trailing newline; mirrors history.spec.js formatting"
  artifacts:
    - path: "vue/cypress/integration/admin.spec.js"
      provides: "Wave 0 e2e stub for the Admin Features capabilities (ADMIN-01..03)"
      contains: "Admin Features feature"
  key_links:
    - from: "admin.spec.js beforeEach"
      to: "/app/admin/users (route lands in Wave 2)"
      via: "cy.visit on hash-mode router"
      pattern: "cy\\.visit\\('http://localhost:3000/#/app/admin/users"
    - from: "admin.spec.js beforeEach"
      to: "cy.login()"
      via: "Cypress custom command defined in vue/cypress/support/commands.ts"
      pattern: "cy\\.login\\(\\)"
---

<objective>
Create the Wave 0 Cypress spec stub for the Admin Features capabilities (user list, session revocation, impersonation, and the impersonate-button-hidden-on-admin-rows negative case).

Purpose: The Cypress stub gives later waves a non-MISSING automated verify target (Nyquist compliance). Wave 2 will make the spec actually runnable when the `/app/admin/users` route lands; Wave 3 will flip the TODO comments into real assertions. No production code is touched in this wave; the only artifact is one new file inside the console submodule.

This stub is structurally similar to `vue/cypress/integration/history.spec.js` (Phase 7 canonical shape — see `vue/cypress/integration/history.spec.js`) rather than `auth-extras.spec.js`: admin routes are authenticated (they live under `/app`), so `cy.login()` AND `cy.visit('/app/admin/users')` BOTH belong in `beforeEach`, not inside individual it() bodies. The one exception is `auth-extras.spec.js` (Phase 8), where some specs targeted an unauthenticated `/password-reset` route — that pattern does not apply here.

Output: One new Cypress spec file at `vue/cypress/integration/admin.spec.js`. No store changes, no route changes, no component changes. Single commit in the console submodule.
</objective>

<execution_context>
@/Users/igraczech/.claude/get-shit-done/workflows/execute-plan.md
@/Users/igraczech/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/ROADMAP.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-10/10-CONTEXT.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-10/10-RESEARCH.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/integration/history.spec.js
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/integration/auth-extras.spec.js
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/support/commands.ts

<interfaces>
From vue/cypress/integration/history.spec.js (canonical authenticated-stub structure that this plan mirrors):

  describe('History feature', function() {

    beforeEach(() => {
      cy.viewport(1536, 754);
      cy.login();
      cy.visit('http://localhost:3000/#/app/history');
    });

    it('Should load the history page with both Audit Log and Build Log tabs (HIST-01)', function() { /* TODO HIST-01: ... */ });
    // ... single-line TODO body per it(), no assertions
  });

Admin Features adaptation (this plan):

  describe('Admin Features feature', function() {

    beforeEach(() => {
      cy.viewport(1536, 754);
      cy.login();
      cy.visit('http://localhost:3000/#/app/admin/users');
    });

    it('Should render the admin user-list page with pagination controls (ADMIN-01)', function() { /* TODO ADMIN-01: ... */ });
    it('Should show a confirmation modal then revoke sessions on confirm (ADMIN-02)', function() { /* TODO ADMIN-02: ... */ });
    it('Should show a confirmation modal then start impersonation on confirm (ADMIN-03)', function() { /* TODO ADMIN-03: ... */ });
    it('Should hide the Impersonate action on admin rows (ADMIN-03 negative)', function() { /* TODO ADMIN-03: ... */ });
  });

Notes:
- cy.login() is a custom command in vue/cypress/support/commands.ts (lines 43-50). Parameters are declared but unused — it reads credentials from cypress/fixtures/thinx.json. Call with NO arguments.
- Viewport 1536x754 is the project convention (used in profile.spec.js, history.spec.js, dashboard.spec.js, devices.spec.js).
- Cypress 9.5.4 mocha-style syntax (pinned in vue/package.json).
- Do NOT use it.only.
- The /app/admin/users route does not yet exist in Routes.js — Wave 2 will add it as a sibling of /app/profile (see vue/src/Routes.js:115-119 for the /app/profile pattern). The Wave 0 stub references the URL the route WILL resolve to; running the spec at this wave is expected to fail on cy.visit at the /app/admin/users redirect — that's fine for a stub.
- Per memory `thinx-console-vue-conventions`: do not introduce assertions in stubs. Wave 3 flips TODO comments into cy.get / cy.intercept calls.
- All four it() bodies are single TODO comments — no cy.get, cy.intercept, cy.wait, or assertions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Cypress spec stub for the Admin Features capabilities (ADMIN-01..03 + ADMIN-03 negative)</name>
  <files>vue/cypress/integration/admin.spec.js</files>
  <read_first>
    - vue/cypress/integration/history.spec.js — reference describe/beforeEach/it() formatting (leading blank line, 2-space indent inside describe, blank line between it() blocks, trailing semicolons, single trailing newline). The Admin stub follows this exact structure.
    - vue/cypress/integration/auth-extras.spec.js — secondary reference (note: this stub does NOT mirror its no-login-in-beforeEach quirk; that quirk was specific to the unauthenticated /password-reset route).
    - vue/cypress/support/commands.ts — confirm cy.login() takes no required args (lines 43-50).
    - .planning/phase-10/10-RESEARCH.md §1.8 — canonical it() skeletons + descriptions used verbatim below.
  </read_first>
  <action>
    Create vue/cypress/integration/admin.spec.js with a top-level describe('Admin Features feature', function() { ... }).

    In beforeEach call EXACTLY three things, in this order:
      1. cy.viewport(1536, 754)
      2. cy.login()
      3. cy.visit('http://localhost:3000/#/app/admin/users')

    This matches the history.spec.js / profile.spec.js shape verbatim — admin routes are under /app and require authentication, so cy.login() belongs in beforeEach, not inside individual it() bodies. (Do NOT mirror the auth-extras.spec.js comment "No login call here — most of these specs hit an unauthenticated route." — that comment was specific to AUTH-01/02 which targeted /#/password-reset.)

    Add the following 4 it(...) blocks in this exact order:

    1. it('Should render the admin user-list page with pagination controls (ADMIN-01)', function() { /* TODO ADMIN-01: assert <table class="table table-striped"> renders with at least one row; assert Prev / Next pagination buttons visible; no console errors */ });

    2. it('Should show a confirmation modal then revoke sessions on confirm (ADMIN-02)', function() { /* TODO ADMIN-02: locate a non-admin row, click Revoke, assert b-modal opens; click Force logout, assert success toast and that the row's revoked_at is updated server-side via cy.intercept on DELETE /api/v2/admin/session/:owner */ });

    3. it('Should show a confirmation modal then start impersonation on confirm (ADMIN-03)', function() { /* TODO ADMIN-03: click Impersonate on a non-admin row, assert b-modal opens; click confirm, assert ImpersonationBanner mounts with 🎭 text and MM:SS countdown visible; assert localStorage.accessToken decodes to a JWT with an impersonator_owner claim */ });

    4. it('Should hide the Impersonate action on admin rows (ADMIN-03 negative)', function() { /* TODO ADMIN-03 negative: locate an admin row (b-badge "Yes" in the Admin column), assert no Impersonate button is rendered inside that row */ });

    Mirror the formatting of history.spec.js exactly:
    - Leading blank line at the top of the file
    - describe with a 2-space indent for its body
    - Blank line between describe header and beforeEach
    - Blank line between beforeEach and the first it()
    - Blank line between each it() block
    - Each it() ends with a semicolon
    - Single trailing newline at end of file
    - Valid ES2017 JavaScript (node --check passes)

    Do NOT use it.only. Do NOT add any cy.get, cy.intercept, cy.wait, or assertion calls. Do NOT add console.log statements. Do NOT add fixture loads. cy.login() MUST appear exactly once — inside beforeEach, NOT inside any it() body.
  </action>
  <verify>
    <automated>cd /Users/igraczech/Repositories/thinx-device-api/services/console && node --check vue/cypress/integration/admin.spec.js && test "$(grep -c '^  it(' vue/cypress/integration/admin.spec.js)" = "4" && test "$(grep -c 'it.only' vue/cypress/integration/admin.spec.js)" = "0" && test "$(grep -c 'cy.login()' vue/cypress/integration/admin.spec.js)" = "1" && test "$(grep -c \"describe('Admin Features feature'\" vue/cypress/integration/admin.spec.js)" = "1" && test "$(grep -c 'TODO ADMIN-' vue/cypress/integration/admin.spec.js)" -ge "4"</automated>
  </verify>
  <acceptance_criteria>
    - File vue/cypress/integration/admin.spec.js exists.
    - node --check exits 0 (valid JavaScript).
    - grep -c "^  it(" returns 4.
    - grep -c "it.only" returns 0.
    - grep -c "cy.login()" returns 1 — called exactly once, inside beforeEach.
    - grep -c "cy.visit('http://localhost:3000/#/app/admin/users')" returns 1.
    - grep -c "describe('Admin Features feature'" returns 1.
    - grep -c "TODO ADMIN-" returns at least 4 — one TODO per it() body (ADMIN-01, ADMIN-02, ADMIN-03 in two it() blocks count as ≥4 mentions).
    - Each of ADMIN-01, ADMIN-02, ADMIN-03 appears at least once in it() descriptions (ADMIN-03 appears twice — one positive + one negative).
    - Manual code-read: no cy.get, cy.intercept, cy.wait, or any assertion — all four it() bodies are single comments.
    - Manual code-read: beforeEach contains exactly three Cypress calls in order (viewport, login, visit).
  </acceptance_criteria>
  <done>vue/cypress/integration/admin.spec.js exists, parses with node --check, contains 4 it() stubs covering ADMIN-01, ADMIN-02, ADMIN-03 positive, and ADMIN-03 negative; uses no .only; calls cy.login() exactly once inside beforeEach; is recognised by yarn cy:open as a runnable no-assert spec alongside the existing login, devices, device-detail, dashboard, profile, history, and auth-extras specs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cypress runner → dev server | Test spec runs against http://localhost:3000 — same origin as the real app |
| cy.login() → /login form | Custom command authenticates with static test fixture credentials from cypress/fixtures/thinx.json |
| cy.visit('/app/admin/users') → not-yet-existing route | Spec deliberately references a route that lands in Wave 2; the cy.visit will redirect to /app/dashboard or 404 until then — accepted Wave 0 stub limitation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-00-01 | Information Disclosure | static test credentials in cy.login() | accept | Test creds are dev-only in cypress/fixtures/thinx.json (pre-existing across Phases 4–8). No change in this plan. |
| T-10-00-02 | Tampering | spec file committed to repo | mitigate | Stub contains no secrets, no eval, no arbitrary require, no network calls beyond cy.login() + cy.visit(). Only describe/it/cy.* calls. |
| T-10-00-03 | Elevation of Privilege | premature reference to admin route | accept | The /app/admin/users route does not exist at Wave 0; visiting it at this wave will simply redirect to /app/dashboard (the global router guard at Routes.js:131-144 allows authenticated users on /app/*, so the visit becomes a no-op rather than a security event). No privilege boundary is crossed. |
| T-10-00-SC | Tampering | npm supply chain | accept | This plan installs NO new npm packages. Cypress 9.5.4 already pinned in vue/package.json. |
</threat_model>

<verification>
1. cd /Users/igraczech/Repositories/thinx-device-api/services/console && node --check vue/cypress/integration/admin.spec.js exits 0.
2. yarn --cwd vue cy:open opens the Cypress runner sidebar and lists admin.spec.js alongside the existing login, devices, device-detail, dashboard, profile, history, and auth-extras specs.
3. Selecting admin.spec.js shows 4 pending it() blocks — all pass vacuously at this wave (TODO-only bodies, no assertions). cy.visit may redirect to /app/dashboard or 404; that is expected.
4. None of the existing specs (login, devices, device-detail, dashboard, profile, history, auth-extras) are altered — confirmed by `git diff vue/cypress/integration/` showing only the new file.
5. Manual file inspection confirms cy.login() appears only inside beforeEach, never inside an it() body.
</verification>

<success_criteria>
- admin.spec.js exists, parses with node --check, and passes all grep gates from Task 1 acceptance_criteria.
- The Cypress runner accepts the new spec without parse errors.
- No production code is modified — files_modified contains exactly one entry, the new spec file.
- Wave 1 (backend) can land without depending on this stub; Wave 2 (frontend) makes the stub runnable; Wave 3 flips the TODOs into real assertions.
- Commit message follows the project's chore(10-00): convention.
</success_criteria>

<output>
Create /Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-10/10-00-SUMMARY.md when done.

Suggested commit (single, submodule):
  chore(10-00): seed admin.spec.js Cypress stub (ADMIN-01..03 + negative)
</output>
