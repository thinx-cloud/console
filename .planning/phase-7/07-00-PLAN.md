---
phase: 07-history-improvements
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - vue/cypress/integration/history.spec.js
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03
  - HIST-04
  - HIST-05
tags:
  - cypress
  - e2e
  - test-stub
  - vue

must_haves:
  truths:
    - "vue/cypress/integration/history.spec.js exists and uses the same describe/beforeEach/cy.login()/cy.visit pattern as profile.spec.js"
    - "The spec contains exactly 5 it(...) blocks, one per HIST-01..05, with TODO bodies and no assertions"
    - "No it.only is used"
    - "cy.visit targets http://localhost:3000/#/app/history"
  artifacts:
    - path: "vue/cypress/integration/history.spec.js"
      provides: "Wave 0 e2e stub for the History page (HIST-01..05 coverage)"
      contains: "History feature"
  key_links:
    - from: "history.spec.js beforeEach"
      to: "cy.login() + cy.visit('http://localhost:3000/#/app/history')"
      via: "Cypress custom command defined in vue/cypress/support/commands.ts"
      pattern: "cy\\.login\\(\\)"
---

<objective>
Create the Wave 0 Cypress spec stub for the History page.

Purpose: The Cypress stub gives Wave 1 and Wave 2 tasks a non-MISSING automated verify target (Nyquist compliance) — Wave 1 (router + filter predicates) and Wave 2 (date-range / flag-filter / HIST-03 Expand toggle UI) will fill in the assertions against this stub. No production code is touched in this wave; the only artifact is one new file.

Output: One new Cypress spec file at vue/cypress/integration/history.spec.js. No store changes, no route changes, no component changes.
</objective>

<execution_context>
@/Users/igraczech/Repositories/thinx-device-api/services/console/.claude/get-shit-done/workflows/execute-plan.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/ROADMAP.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-7/07-RESEARCH.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-7/07-CONTEXT.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/integration/profile.spec.js
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/integration/dashboard.spec.js
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/support/commands.ts
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/Routes.js

<interfaces>
From vue/cypress/integration/profile.spec.js (reference structure — copy describe/beforeEach pattern exactly):

  describe('Profile feature', function() {
    beforeEach(() => {
      cy.viewport(1536, 754);
      cy.login();
      cy.visit('http://localhost:3000/#/app/profile');
    });
    it('Should ... (PROF-01)', function() { /* TODO PROF-01: ... */ });
    // ... single-line TODO body per it(), no assertions
  });

Notes:
- cy.login() is a custom command in vue/cypress/support/commands.ts (lines 43-50). Parameters are declared but unused — it reads credentials from cypress/fixtures/thinx.json. Call with NO arguments.
- Viewport 1536x754 is the project convention (used in profile.spec.js and dashboard.spec.js beforeEach).
- Cypress 9.5.4 mocha-style syntax (pinned in vue/package.json).
- Do NOT use it.only.
- History route: /#/app/history (registered at Routes.js:69-73 as a flat child of /app; hash-mode router per Routes.js:27). Wave 1 will add child routes /audit and /builds; for Wave 0 the bare /#/app/history is correct — the redirect Wave 1 introduces will keep this URL working.
- This stub must NOT call cy.get, cy.intercept, or any assertion — those land in Wave 1 / Wave 2.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Cypress spec stub for the History page (HIST-01..05)</name>
  <files>vue/cypress/integration/history.spec.js</files>
  <read_first>
    - vue/cypress/integration/profile.spec.js — reference describe/beforeEach/cy.login()/cy.visit pattern; copy the structure verbatim (5 it() blocks instead of 7, History route instead of Profile route)
    - vue/cypress/integration/dashboard.spec.js — second reference for the same stub pattern at a different route
    - vue/cypress/support/commands.ts — confirm cy.login() takes no required args (lines 43-50)
    - vue/src/Routes.js — confirm the history route resolves to /#/app/history (lines 69-73)
    - .planning/phase-7/07-RESEARCH.md §1.5 — Wave 0 stub conventions for this phase
  </read_first>
  <action>
    Create vue/cypress/integration/history.spec.js with top-level describe('History feature', function() { ... }). In beforeEach call cy.viewport(1536, 754), then cy.login(), then cy.visit('http://localhost:3000/#/app/history'). Add the following 5 it(...) blocks in this order, each with a single-line TODO comment as the body — no assertions (Waves 1 and 2 fill them in):

    1. it('Should load the history page with both Audit Log and Build Log tabs (HIST-01)', function() { /* TODO HIST-01: verify both b-tab titles ("Audit Log" and "Build Log") render; no console errors */ })
    2. it('Should reflect tab state in URL — /app/history/audit and /app/history/builds (HIST-02)', function() { /* TODO HIST-02: click each tab, assert URL hash matches /app/history/audit and /app/history/builds respectively */ })
    3. it('Should allow expanding a build row to read the full log inline (HIST-03)', function() { /* TODO HIST-03: click Expand on a build row with a log, assert the <pre> no longer has the 120px max-height clipping */ })
    4. it('Should filter both tabs by a date range (HIST-04)', function() { /* TODO HIST-04: set from/to inputs, assert filtered rows have item.date within range */ })
    5. it('Should filter audit log by warning/danger flag checkboxes (HIST-05)', function() { /* TODO HIST-05: uncheck "danger" in flag-filter group, assert no rows with flags: ['danger'] remain */ })

    Mirror the formatting of profile.spec.js exactly: a leading blank line at the top of the file, describe with a 2-space indent for its body, blank line between each it() block, single trailing newline at end of file. Do NOT use it.only. Do NOT add any cy.get, cy.intercept, or assertion calls — those go in Waves 1 and 2. Ensure the file is valid ES2017 JavaScript.
  </action>
  <verify>
    <automated>node --check /Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/integration/history.spec.js && grep -c "^  it(" /Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/integration/history.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - File vue/cypress/integration/history.spec.js exists.
    - node --check exits 0 (valid JavaScript).
    - grep -c "^  it(" returns 5.
    - grep -c "it.only" returns 0 — the spec does not lock the Cypress runner to a single test.
    - grep -c "cy.login()" returns 1 — called exactly once in beforeEach.
    - grep -c "cy.visit('http://localhost:3000/#/app/history')" returns 1.
    - grep -c "TODO HIST-" returns at least 5 — one TODO per HIST-XX requirement.
    - grep -c "describe('History feature'" returns 1.
    - At least one it() description matches each of HIST-01, HIST-02, HIST-03, HIST-04, HIST-05.
  </acceptance_criteria>
  <done>history.spec.js exists, parses with node --check, contains 5 it() stubs covering HIST-01..05, uses no .only, and is recognised by yarn cy:open as a runnable no-assert spec alongside the existing login, devices, device-detail, dashboard, and profile specs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cypress runner → dev server | Test spec runs against http://localhost:3000 — same origin as the real app |
| cy.login() → /login form | Custom command authenticates with static test fixture credentials from cypress/fixtures/thinx.json |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-00-01 | Information Disclosure | static test credentials in cy.login() | accept | Test creds are dev-only in cypress/fixtures/thinx.json (pre-existing across Phases 4–6). No change in this plan. |
| T-07-00-02 | Tampering | spec file committed to repo | mitigate | Stub contains no secrets, no eval, no arbitrary require, no network calls beyond cy.login() + cy.visit(). Only describe/it/cy.* calls. |
| T-07-00-SC | Tampering | npm supply chain | accept | This plan installs NO new npm packages. Cypress 9.5.4 already pinned in vue/package.json. |
</threat_model>

<verification>
1. yarn --cwd vue cy:open opens the Cypress runner sidebar and lists history.spec.js alongside the existing login, devices, device-detail, dashboard, and profile specs.
2. Selecting history.spec.js shows 5 pending it() blocks — all pass vacuously (empty TODO bodies).
3. node --check vue/cypress/integration/history.spec.js exits 0.
4. None of the existing specs (login, devices, device-detail, dashboard, profile) are altered.
</verification>

<success_criteria>
- history.spec.js exists, parses with node --check, and passes all grep gates from Task 1 acceptance_criteria.
- The Cypress runner accepts the new spec without parse errors.
- No production code is modified — files_modified contains exactly one entry, the new spec file.
</success_criteria>

<output>
Create /Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-7/07-00-SUMMARY.md when done
</output>
