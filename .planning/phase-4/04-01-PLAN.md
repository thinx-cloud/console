---
phase: 04-device-management
plan: 01
type: execute
wave: 1
depends_on:
  - 04-00
files_modified:
  - vue/src/pages/Devices/Devices.vue
autonomous: true
requirements:
  - DEVI-01
  - DEVI-02
  - DEVI-03
  - DEVI-04
  - DEVI-05
  - DEVI-06
  - DEVI-07
  - DEVI-08
  - DEVI-09
tags:
  - vue
  - bootstrap-vue
  - devices
  - frontend

must_haves:
  truths:
    - "Devices.vue toolbar row renders grid/list toggle buttons, 8 category pills (All + 7 categories from D-02), sort dropdown with 3 options (Last update, Platform, Alias), and a live search input (per D-05)"
    - "Active category pill uses variant primary; inactive pills use variant outline-secondary with inline borderColor/color matching the category hex (per D-01, D-02)"
    - "A `filteredItems` computed property chains category filter → search filter (alias + mac, case-insensitive) → sort, and is used by both list and grid v-for (per D-03, D-04, RESEARCH Pattern 1)"
    - "Grid view is rendered with `<b-row v-if=\"viewMode === 'grid'\">` and Bootstrap `cols=\"12\" sm=\"6\" md=\"4\"` cards including category badge, alias, platform, firmware, last seen (`fromNow`), checkbox, Detail + Build buttons (per D-06, D-07)"
    - "Table view shows a per-row danger Revoke button in the Actions column that calls `revokeRow(device.udid)` and triggers `$bvModal.msgBoxConfirm` then `revokeDevices([udid])` (per D-13)"
    - "Sort never mutates `this.items` — it always runs on a spread copy `[...result].sort(...)`"
    - "`filterCategory = 'All'` shows all devices (no filter applied)"
    - "Search input is case-insensitive substring match against alias OR mac"
  artifacts:
    - path: "vue/src/pages/Devices/Devices.vue"
      provides: "Devices list page with toolbar (toggle + pills + sort + search), grid view, per-row revoke"
      contains: "filteredItems"
      min_lines: 320
  key_links:
    - from: "Devices.vue template (table tbody v-for and grid b-col v-for)"
      to: "Devices.vue computed.filteredItems"
      via: "v-for=\"device in filteredItems\""
      pattern: "v-for=\"device in filteredItems\""
    - from: "Devices.vue revokeRow(udid)"
      to: "devices/revokeDevices store action"
      via: "this.revokeDevices([udid]) after msgBoxConfirm"
      pattern: "this\\.revokeDevices\\(\\[udid\\]\\)"
    - from: "filterCategory pill click"
      to: "data.filterCategory"
      via: "@click=\"filterCategory = cat\""
      pattern: "filterCategory = cat"
---

<objective>
Extend `vue/src/pages/Devices/Devices.vue` with the toolbar controls (grid/list toggle, category pill filter, sort dropdown, live search), the grid view alongside the existing table, the per-row Revoke button, and the `filteredItems` computed pipeline that backs all of the above.

Purpose: DEVI-01 through DEVI-05 are the user-visible list enhancements still missing from the Vue console. DEVI-06–09 are already implemented (bulk revoke, transfer modal, push-config modal, build firmware) and are listed here only because they live in this file and gain smoke coverage from this plan's Cypress stub additions.

Output: One file modified — `Devices.vue` — gaining ~100 lines (toolbar template, grid template, `filteredItems` computed, `revokeRow` method, new data props, `categoryColor` helper). No new packages, no new store actions, no new routes.
</objective>

<execution_context>
@/Users/igraczech/Repositories/thinx-device-api/services/console/.claude/get-shit-done/workflows/execute-plan.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/ROADMAP.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/REQUIREMENTS.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-4/04-CONTEXT.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-4/04-RESEARCH.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-4/04-PATTERNS.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/pages/Devices/Devices.vue
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/store/devices.js

<interfaces>
From vue/src/pages/Devices/Devices.vue (current state — read before editing):

Existing data() lines 116–127:
  items[], loading, error, message, selectedUdids[], enviros[], transferForm, pushForm
  → ADD: viewMode ('list'), filterCategory ('All'), sortBy ('lastupdate'), searchText ('')

Existing computed lines 128–132:
  isSelected, selectedCount, isAllSelected
  → ADD: filteredItems (category filter → search filter → spread+sort)

Existing methods lines 136–219:
  ...mapGetters({ getItems: 'devices/getItems', getEnviros: 'enviros/getItems' }) — already in methods block (NOT computed) per project pattern
  ...mapActions({ fetchItems, revokeDevices, transferDevices, pushConfiguration, buildFirmware, fetchEnviros })
  isSelected_(udid), toggleDevice(udid), checkAll(ev), viewDevice(udid), buildDevice(udid)
  confirmRevoke() — existing bulk revoke pattern (lines 163-177) — MODEL for new revokeRow(udid)
  transfer(), pushConfig() — existing modal handlers, no change
  loadData() — Promise.all([fetchItems, fetchEnviros])
  → ADD: revokeRow(udid), categoryColor(cat)

Existing template lines 27–71:
  `<table v-else class="table table-striped">` — change tbody v-for from `items` to `filteredItems`
  Empty-state row colspan=7 with `v-if="!items.length"` — change to `!filteredItems.length`
  Per-row Actions column (line 62-65): Detail + Build buttons present → ADD danger Revoke button after Build
  → ADD: toolbar div before line 27, grid b-row block after line 71

Project pattern (CRITICAL):
  mapGetters spread INTO methods (line 137), not computed. Getters called as functions: `this.getItems()`.
  Filters block (lines 105-115): `fromNow` filter defined inline — KEEP as-is, do not extract.

Category color map (from RESEARCH.md, verified against src/html/assets/global/css/components.css):
  yellow-crusta → #f3c200
  red-intense   → #e35b5a
  purple-studio → #8E44AD
  blue          → #3598dc
  green         → #32c5d2
  green-dark    → #4DB3A2
  grey-mint     → #525e64

Category list order (from D-02): ['All', 'yellow-crusta', 'red-intense', 'purple-studio', 'blue', 'green', 'green-dark', 'grey-mint']
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add toolbar data props, filteredItems computed, categoryColor helper, and revokeRow method</name>
  <files>vue/src/pages/Devices/Devices.vue</files>
  <read_first>
    - vue/src/pages/Devices/Devices.vue — read fully; lines 116-127 (data), 128-132 (computed), 136-219 (methods), 163-177 (confirmRevoke pattern to mirror)
    - vue/src/store/devices.js — confirm revokeDevices signature is `(udids: string[]) => { success, message? }`
    - .planning/phase-4/04-PATTERNS.md — lines 39-95 (filteredItems block), lines 147-162 (revokeRow adaptation), lines 253-270 (CATEGORY_COLORS + categoryColor helper)
    - .planning/phase-4/04-RESEARCH.md — Pattern 1 (filteredItems), Pattern 2 (category color pills), Common Pitfalls #3 (sort spread)
  </read_first>
  <behavior>
    - data() now returns the original 8 fields PLUS: `viewMode: 'list'`, `filterCategory: 'All'`, `sortBy: 'lastupdate'`, `searchText: ''` (4 new properties, all session-only — no persistence per D-06).
    - computed gains exactly one new property: `filteredItems` that returns an array derived from `this.items` after (1) category filter (skipped when filterCategory === 'All' or falsy), (2) search filter (alias OR mac, case-insensitive substring), (3) sort via spread-then-sort `[...result].sort(...)`. Sort branches: 'lastupdate' descending on numeric `lastupdate` (fallback 0), 'platform' ascending via `localeCompare`, 'alias' ascending via `localeCompare`, default return 0.
    - methods gains exactly two new methods: `revokeRow(udid)` (mirrors confirmRevoke for a single udid — `$bvModal.msgBoxConfirm`, `revokeDevices([udid])`, success → `this.message` + `loadData()`, failure → `this.error`) AND `categoryColor(cat)` that returns the hex from the inline `CATEGORY_COLORS` map (default `'#525e64'`).
    - `CATEGORY_COLORS` is declared as a module-scope `const` at the top of the `<script>` block, after the existing `import { mapGetters, mapActions } from 'vuex';` line and BEFORE `export default { ... }` (per D-02 — static lookup table, not reactive).
    - `this.items` is NEVER mutated by the new code: `filteredItems` always spreads before sorting, and `revokeRow` uses an explicit `[udid]` literal.
  </behavior>
  <action>
    Open `vue/src/pages/Devices/Devices.vue`. Make script-block edits only — DO NOT modify the template in this task; the template edits are Task 2.

    Step (a) — Insert `CATEGORY_COLORS` constant. After the existing `import { mapGetters, mapActions } from 'vuex';` line (line 101) and before `export default {` (line 103), insert a `const CATEGORY_COLORS` object literal with exactly these seven keys and hex values: `yellow-crusta` → `#f3c200`, `red-intense` → `#e35b5a`, `purple-studio` → `#8E44AD`, `blue` → `#3598dc`, `green` → `#32c5d2`, `green-dark` → `#4DB3A2`, `grey-mint` → `#525e64`.

    Step (b) — Extend `data()` (lines 116-127). Add four new properties at the END of the returned object, immediately after the existing `pushForm: { selectedEnviros: [], reset_devices: false },` line: `viewMode: 'list',`, then `filterCategory: 'All',`, then `sortBy: 'lastupdate',`, then `searchText: '',`. Keep all 8 existing properties unchanged.

    Step (c) — Extend `computed` (lines 128-132). Add a `filteredItems()` method as the LAST property in the `computed` block (after `isAllSelected`). Implementation requirements:
    - Initialize `let result = this.items;`.
    - Category filter: when `this.filterCategory` is truthy AND not equal to the string `'All'`, reassign `result = result.filter(d => d.category === this.filterCategory);`.
    - Search filter: when `this.searchText` is truthy, compute `const q = this.searchText.toLowerCase();` then reassign `result` to a filter that keeps a device when `(d.alias || '').toLowerCase().includes(q)` OR `(d.mac || '').toLowerCase().includes(q)`.
    - Sort: reassign `result = [...result].sort((a, b) => { ... });` (spread is mandatory to avoid mutating `this.items`). Sort branches:
      * `this.sortBy === 'lastupdate'` → return `new Date(b.lastupdate || 0).getTime() - new Date(a.lastupdate || 0).getTime()` (descending). Use `Date` constructor to handle both numeric timestamps and ISO strings; invalid dates produce `NaN` which `.getTime()` propagates — guard with `|| 0` on the input.
      * `this.sortBy === 'platform'` → return `(a.platform || '').localeCompare(b.platform || '')` (ascending).
      * `this.sortBy === 'alias'` → return `(a.alias || '').localeCompare(b.alias || '')` (ascending).
      * Default → return `0`.
    - End with `return result;`.

    Step (d) — Extend `methods` block. Add exactly two methods at the END of the methods block (after `loadData`):
    - `async revokeRow(udid)`: call `await this.$bvModal.msgBoxConfirm('Revoke this device? This cannot be undone.', { title: 'Confirm Revoke', okVariant: 'danger', okTitle: 'Revoke' })` and bind the return value to `confirmed`. If `!confirmed` return immediately. Otherwise call `await this.revokeDevices([udid])` and bind to `result`. On `result.success`: set `this.message = 'Device revoked.'` and call `this.loadData()`. On failure: set `this.error = result.message || 'Failed to revoke device.'`.
    - `categoryColor(cat)`: return `CATEGORY_COLORS[cat] || '#525e64'`.

    DO NOT touch: `confirmRevoke` (bulk), `transfer`, `pushConfig`, `buildDevice`, `viewDevice`, `loadData`, the existing `mapGetters` spread, `mapActions` spread, `checkAll`, `toggleDevice`, `isSelected_`, the `filters` block, or any template/style content. KEEP `mapGetters` inside `methods` (project convention — moving it to `computed` would break the established pattern from Plans 03-01/03-02 and the rest of the codebase).
  </action>
  <verify>
    <automated>cd /Users/igraczech/Repositories/thinx-device-api/services/console && grep -c "filteredItems" vue/src/pages/Devices/Devices.vue && grep -c "CATEGORY_COLORS" vue/src/pages/Devices/Devices.vue && grep -c "revokeRow" vue/src/pages/Devices/Devices.vue && grep -c "\[\.\.\.result\]\.sort" vue/src/pages/Devices/Devices.vue && grep -c "viewMode:" vue/src/pages/Devices/Devices.vue && grep -c "filterCategory:" vue/src/pages/Devices/Devices.vue && grep -c "sortBy:" vue/src/pages/Devices/Devices.vue && grep -c "searchText:" vue/src/pages/Devices/Devices.vue && grep -v '^[[:space:]]*//' vue/src/pages/Devices/Devices.vue | grep -c "this\.items\.sort" || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "filteredItems"` returns at least 2 (computed definition + at least one internal reference inside the body).
    - `grep -c "CATEGORY_COLORS"` returns at least 2 (declaration + reference in categoryColor).
    - `grep -c "revokeRow"` returns at least 1 (method definition; template references arrive in Task 2).
    - `grep -F "this.revokeDevices([udid])"` matches exactly 1 line (single-device revoke; bulk pattern uses `[...this.selectedUdids]` and is NOT changed).
    - `grep -c "\[\.\.\.result\]\.sort"` returns 1 — spread-before-sort enforced (the only sort in filteredItems).
    - `grep -v '^[[:space:]]*//' vue/src/pages/Devices/Devices.vue | grep -c "this\.items\.sort"` returns 0 — no in-place mutation of `this.items` anywhere in non-comment code.
    - `grep -c "viewMode:"` returns 1; `grep -c "filterCategory:"` returns 1; `grep -c "sortBy:"` returns 1; `grep -c "searchText:"` returns 1 — all four new data props present exactly once.
    - `grep -c "categoryColor"` returns at least 1 (method definition).
    - `awk '/methods: {/,/^  },$/' vue/src/pages/Devices/Devices.vue | grep -c "mapGetters"` returns 1 — mapGetters STILL inside methods, not moved to computed.
    - `grep -c "^import "` returns 1 — no new `import` statements added beyond the original Vuex import.
    - The Vue dev server (`yarn --cwd vue serve`) compiles the file without syntax errors; the existing `/app/devices` page still renders with the unmodified table (template is unchanged in this task).
  </acceptance_criteria>
  <done>Script block has `CATEGORY_COLORS` const, 4 new data properties, `filteredItems` computed with spread-then-sort, `revokeRow` method, and `categoryColor` method. Template is unchanged so the build still runs (table still v-for over `items` — Task 2 swaps to `filteredItems`).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add toolbar template, grid view block, per-row Revoke button, and switch v-for to filteredItems</name>
  <files>vue/src/pages/Devices/Devices.vue</files>
  <read_first>
    - vue/src/pages/Devices/Devices.vue — read AGAIN after Task 1's script-block changes; focus on template lines 1-98
    - .planning/phase-4/04-PATTERNS.md — lines 274-307 (toolbar template), lines 312-350 (grid view block), lines 196-208 (per-row Revoke button placement)
    - .planning/phase-4/04-CONTEXT.md — D-05 (single toolbar row), D-06 (grid card content), D-07 (responsive col sizes)
    - vue/cypress/integration/devices.spec.js — created in Plan 04-00; the toolbar identifiers chosen here must be selectable from these specs in later waves
  </read_first>
  <behavior>
    - A single toolbar `<div class="d-flex align-items-center flex-wrap mb-3 gap-2">` is inserted immediately before the existing `<div v-if="loading">` line (currently line 26). Toolbar content order (per D-05): grid/list toggle icons (2 buttons) → 8 category pill buttons (All + 7 colors) → `<div class="ml-auto d-flex align-items-center">` containing sort `b-form-select` and search `b-form-input`.
    - The toolbar is always visible — it is NOT wrapped in `v-if="!loading"`. Users can configure filters before data arrives.
    - The existing `<table>` element is wrapped with `v-if="viewMode === 'list'"` (replacing the bare `v-else`). Its `<tbody>` `v-for` is changed from `items` to `filteredItems`. The empty-state row's `v-if` is changed from `!items.length` to `!filteredItems.length`.
    - A new `<b-row v-if="viewMode === 'grid'">` block is inserted after the closing `</table>` (current line 71) and before the existing `<!-- Transfer Modal -->` comment. Each card renders: category badge with inline `backgroundColor: categoryColor(device.category)` and white text color, alias, platform, firmware, `device.lastupdate | fromNow`, the same checkbox pattern as the list (using `isSelected_` and `toggleDevice`), and Detail + Build buttons (re-using existing `viewDevice` and `buildDevice` handlers). The grid loop uses `filteredItems`. An empty-state `<b-col v-if="!filteredItems.length">` displays the literal text "No devices match the current filter."
    - The per-row Actions cell (currently line 62-65) gains a third button — danger variant Revoke — after the existing Build button. It calls `revokeRow(device.udid)` (the method added in Task 1).
    - The original `<div v-if="loading">Loading...</div>` is preserved unchanged so loading state still renders between the toolbar and the table/grid.
  </behavior>
  <action>
    Open `vue/src/pages/Devices/Devices.vue` (already touched by Task 1). Make the following template edits:

    Step (a) — Toolbar. Insert a `<div class="d-flex align-items-center flex-wrap mb-3 gap-2">` block immediately BEFORE the existing `<div v-if="loading">` element (currently line 26). The toolbar must contain, in order:
    - A grid/list LIST toggle `b-button`: `size="sm"`, `:variant="viewMode === 'list' ? 'primary' : 'outline-secondary'"`, `@click="viewMode = 'list'"`, `class="mr-1"`, body is a `<span>` containing the HTML entity `&#9776;` (hamburger).
    - A grid/list GRID toggle `b-button`: `size="sm"`, `:variant="viewMode === 'grid' ? 'primary' : 'outline-secondary'"`, `@click="viewMode = 'grid'"`, `class="mr-1"`, body is a `<span>` containing the HTML entity `&#9635;` (grid square).
    - Eight category pill `b-button` elements via `v-for="cat in ['All', 'yellow-crusta', 'red-intense', 'purple-studio', 'blue', 'green', 'green-dark', 'grey-mint']"` with `:key="cat"`, `size="sm"`, `:variant="filterCategory === cat ? 'primary' : 'outline-secondary'"`, `:style="filterCategory !== cat && cat !== 'All' ? { borderColor: categoryColor(cat), color: categoryColor(cat) } : {}"`, `@click="filterCategory = cat"`, `class="mr-1"`, body `{{ cat }}`.
    - A right-aligned wrapper `<div class="ml-auto d-flex align-items-center">` containing:
      * Sort: `<b-form-select v-model="sortBy" size="sm" style="width:140px" class="mr-2">` with `:options="[{ value: 'lastupdate', text: 'Last Update' }, { value: 'platform', text: 'Platform' }, { value: 'alias', text: 'Alias' }]"`.
      * Search: `<b-form-input v-model="searchText" placeholder="Search alias or MAC..." size="sm" style="width:200px" />`.

    Step (b) — Table tag. Change the existing `<table v-else class="table table-striped">` opening tag to `<table v-if="viewMode === 'list'" class="table table-striped">`. The `v-else` becomes the explicit `viewMode === 'list'` guard.

    Step (c) — Table tbody v-for. Change the existing `<tr v-for="(device, index) in items" :key="device.udid">` to `<tr v-for="(device, index) in filteredItems" :key="device.udid">`. Also change the empty-state row's `v-if="!items.length"` to `v-if="!filteredItems.length"`.

    Step (d) — Per-row Revoke button. In the Actions cell of the table row (currently line 62-65, after the Build button), add a third `<b-button>` with `size="sm"`, `variant="danger"`, `@click="revokeRow(device.udid)"`, `class="ml-1"`, body text "Revoke".

    Step (e) — Grid view block. Immediately AFTER the closing `</table>` and BEFORE the `<!-- Transfer Modal -->` comment, insert a `<b-row v-if="viewMode === 'grid'">` block. Inside the row:
    - A `<b-col v-for="device in filteredItems" :key="device.udid" cols="12" sm="6" md="4" class="mb-3">` containing a `<b-card class="h-100">` with:
      * `<template #header>` containing a category badge `<span class="badge mr-2" :style="{ backgroundColor: categoryColor(device.category), color: '#fff' }">{{ device.category }}</span>` followed by `<strong>{{ device.alias }}</strong>`.
      * Three paragraphs: `<p class="mb-1"><strong>Platform:</strong> {{ device.platform }}</p>`, `<p class="mb-1 text-muted small">{{ device.firmware }}</p>`, `<p class="mb-2 text-muted small">{{ device.lastupdate | fromNow }}</p>`.
      * A checkbox: `<div class="abc-checkbox d-inline-block mr-2"><input type="checkbox" :id="'grid-checkbox-' + device.udid" :checked="isSelected_(device.udid)" @change="toggleDevice(device.udid)" /><label :for="'grid-checkbox-' + device.udid" /></div>`.
      * Two action buttons: `<b-button size="sm" variant="primary" @click="viewDevice(device.udid)" class="mr-1">Detail</b-button>` and `<b-button size="sm" variant="secondary" @click="buildDevice(device.udid)">Build</b-button>`.
    - An empty-state column AFTER the v-for: `<b-col v-if="!filteredItems.length" cols="12"><p class="text-muted">No devices match the current filter.</p></b-col>`.

    DO NOT remove the existing `<div v-if="loading">Loading...</div>` line — it must remain between the toolbar and the views. DO NOT change the Transfer Modal, Push Config Modal, breadcrumb, page title, or the top action buttons row (lines 11-21). DO NOT add or remove any `<style>` blocks.
  </action>
  <verify>
    <automated>cd /Users/igraczech/Repositories/thinx-device-api/services/console && grep -c "v-for=\"device in filteredItems\"" vue/src/pages/Devices/Devices.vue && grep -c "v-for=\"(device, index) in filteredItems\"" vue/src/pages/Devices/Devices.vue && grep -c "@click=\"revokeRow" vue/src/pages/Devices/Devices.vue && grep -c "viewMode === 'grid'" vue/src/pages/Devices/Devices.vue && grep -c "viewMode === 'list'" vue/src/pages/Devices/Devices.vue && grep -c "filterCategory === cat" vue/src/pages/Devices/Devices.vue && grep -c "v-model=\"sortBy\"" vue/src/pages/Devices/Devices.vue && grep -c "v-model=\"searchText\"" vue/src/pages/Devices/Devices.vue && grep -c "yellow-crusta" vue/src/pages/Devices/Devices.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "v-for=\"device in filteredItems\""` returns 1 (the grid `b-col` loop).
    - `grep -c "v-for=\"(device, index) in filteredItems\""` returns 1 (the table `tbody` loop).
    - `grep -c "items.length"` filtered to non-comment lines via `grep -v '^[[:space:]]*//'` returns 0 instances of `!items.length` — both empty-state guards reference `filteredItems.length` instead.
    - `grep -c "!filteredItems.length"` returns 2 (table empty-state row + grid empty-state column).
    - `grep -c "@click=\"revokeRow"` returns 1 (per-row danger button).
    - `grep -c "viewMode === 'list'"` returns 1 (table guard).
    - `grep -c "viewMode === 'grid'"` returns 1 (grid b-row guard).
    - `grep -c "@click=\"viewMode = 'list'\""` returns 1 and `grep -c "@click=\"viewMode = 'grid'\""` returns 1 (toolbar toggle buttons).
    - `grep -c "filterCategory === cat"` returns 1 (active-pill variant binding).
    - `grep -c "@click=\"filterCategory = cat\""` returns 1 (pill click handler).
    - `grep -c "v-model=\"sortBy\""` returns 1 (sort dropdown bound).
    - `grep -c "v-model=\"searchText\""` returns 1 (search input bound).
    - `grep -c "yellow-crusta"` returns at least 2 (toolbar v-for array + CATEGORY_COLORS map from Task 1).
    - `grep -c "categoryColor(device.category)"` returns at least 1 (grid badge style binding).
    - `grep -c "Search alias or MAC"` returns 1 (search input placeholder).
    - The Vue dev server (`yarn --cwd vue serve`) compiles the file without template errors; `/app/devices` renders the toolbar and either the table or grid based on `viewMode`.
    - In the running app: clicking the grid toggle button switches `viewMode` to `'grid'` and renders `b-card` elements (visually verifiable; e2e DEVI-04 test can later assert on `.card` selector).
    - In the running app: typing a substring into the search input filters the visible rows (DEVI-03 behavior).
    - In the running app: clicking a category pill other than 'All' filters the list to rows where `device.category === pill` (DEVI-01).
    - In the running app: changing the sort dropdown re-orders the visible rows (DEVI-02).
    - In the running app: clicking a per-row Revoke button opens an `$bvModal.msgBoxConfirm`; confirming dispatches `devices/revokeDevices` and reloads the list (DEVI-05).
  </acceptance_criteria>
  <done>Template renders toolbar + both view modes + per-row Revoke. `filteredItems` drives both the table and the grid v-for. The list page satisfies DEVI-01 through DEVI-05 in the browser; DEVI-06–09 continue to work via the unchanged bulk/transfer/push/build buttons at the top.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → searchText | Free-text search input stays client-side only (never sent to API in this plan) — no injection surface |
| user input → filterCategory | Bound to `v-model`/`@click` with a hardcoded array of 8 known strings (per D-02) — no user-supplied string reaches the filter |
| per-row Revoke click → devices/revokeDevices | UDID sourced from the rendered device list (not user-typed); store action already validates server-side |
| sort selection → filteredItems | `sortBy` value is bound to a `b-form-select` with 3 fixed options — no arbitrary code path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | per-row Revoke button | mitigate | Wrap in `$bvModal.msgBoxConfirm` (per D-13) so accidental clicks require explicit confirmation — same control already in place for bulk revoke (confirmRevoke, lines 163-177). Passes `[udid]` literal to `revokeDevices` — never `selectedUdids`. |
| T-04-01-02 | Tampering | `this.items` mutation via in-place sort | mitigate | `filteredItems` computed enforces `result = [...result].sort(...)` (spread-before-sort, per RESEARCH Pitfall #3). Acceptance criterion explicitly greps for absence of `this.items.sort` in non-comment code. |
| T-04-01-03 | Tampering | category pill click setting `filterCategory` | mitigate | Pill `v-for` iterates a hardcoded 8-element array literal in the template — there is no path to set `filterCategory` to an attacker-controlled string. The filter compares strict equality against the hardcoded list. |
| T-04-01-04 | Information Disclosure | search input echoed in DOM | accept | Vue's `{{ }}` interpolation auto-escapes; `v-model` on `b-form-input` is safe. No `v-html` is introduced. Search query is never sent to the backend (client-side filter only per D-04). |
| T-04-01-05 | Repudiation | revoke action without audit trail | accept | Audit logging is the backend's responsibility (existing `/device` DELETE endpoint already logs); frontend dispatches the existing `devices/revokeDevices` action unchanged. Out of scope for Phase 4 UI work. |
| T-04-01-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NO new npm packages. RESEARCH.md Package Legitimacy Audit explicitly states "No new packages are installed in this phase." All used libraries (Vue 2.6.14, BootstrapVue 2.21.2, Vuex 3.6.2) are pre-existing in package.json. |
</threat_model>

<verification>
1. Run dev server: `yarn --cwd vue serve` and visit `http://localhost:3000/#/app/devices` after login.
2. Confirm the toolbar row renders above the table with: list toggle (active), grid toggle (inactive), 8 pills (All highlighted), sort dropdown defaulting to "Last Update", search input on the right.
3. Click "yellow-crusta" pill — table shows only devices whose `category === 'yellow-crusta'` (DEVI-01). Click "All" — full list returns.
4. Switch sort to "Alias" — rows reorder alphabetically by alias (DEVI-02).
5. Type a substring into the search input — list filters live to devices whose alias or MAC contains the substring case-insensitively (DEVI-03).
6. Click the grid toggle button — table disappears, b-card grid appears with the same `filteredItems` set respecting all current filters (DEVI-04).
7. In list view, click a per-row "Revoke" button — confirm modal appears; clicking Revoke dispatches `devices/revokeDevices([udid])` and reloads the list (DEVI-05).
8. The existing top-row buttons (bulk Revoke, Transfer, Push Config) and the per-row Build/Detail buttons continue to work (DEVI-06–09 smoke).
9. Cypress sanity: `yarn --cwd vue cy:open` shows `devices.spec.js` with the Wave 0 stubs from Plan 04-00; specs still load.
</verification>

<success_criteria>
- DEVI-01: Category filter pill row functional; clicking a non-"All" pill filters the list.
- DEVI-02: Sort dropdown changes row order according to selected field, defaulting to "Last Update" descending.
- DEVI-03: Search input filters by alias OR mac case-insensitive substring.
- DEVI-04: Grid/list toggle switches between table and `b-card` grid views.
- DEVI-05: Per-row Revoke button triggers `$bvModal.msgBoxConfirm` and on confirmation dispatches `devices/revokeDevices([udid])`.
- DEVI-06–09: Bulk revoke, transfer, push config, and build firmware continue to work unchanged.
- All acceptance criteria grep gates from both tasks pass.
- No regressions in Devices.vue template rendering (Vue build succeeds).
</success_criteria>

<output>
Create /Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-4/04-01-SUMMARY.md when done
</output>
