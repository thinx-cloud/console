---
phase: 04-device-management
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - vue/src/pages/Devices/DeviceDetail.vue
  - IMPROVEMENTS.md
autonomous: true
requirements:
  - DEVI-10
  - DEVI-11
tags:
  - vue
  - bootstrap-vue
  - device-detail
  - buildlog
  - transformers
  - frontend

must_haves:
  truths:
    - "DeviceDetail.vue's loadDevice() dispatches devices/fetchItems, buildlog/fetchBuildLog, and transformers/fetchItems in parallel via Promise.all (per D-08, D-10, D-11)"
    - "Build History card renders a table of build entries (date, build ID, status) filtered client-side by `b.udid === this.$route.params.udid` (per D-08)"
    - "Environment Variables card renders a read-only key-value table from `device.environment` with a null/empty guard and a 'No environment variables.' fallback (per D-09, RESEARCH Pitfall #4)"
    - "Transformer Assignment card shows a multi-select b-form-select bound to `editForm.transformers` with options derived from `transformers/getItems` mapped to `{ value: t.utid, text: t.alias || t.utid }`, with a Save Transformers button that dispatches `updateDevice({ udid, changes: { transformers: [...utids] } })` (per D-10)"
    - "Device Logs card (only rendered when `device.last_build_id` is truthy) shows scrollable `<pre>` blocks containing build log lines filtered client-side by `b.build_id === device.last_build_id` (per D-11, RESEARCH Pitfall #5)"
    - "Actions card gains a third button 'Transfer Device' (variant warning) that opens b-modal id `transfer-modal` (per D-12)"
    - "Transfer modal mirrors the Devices.vue transfer modal structure (target email input + 2 migrate checkboxes) and dispatches `devices/transferDevices({ udids: [device.udid], to, mig_sources, mig_apikeys })` (per D-12)"
    - "buildlog dispatch path is `buildlog/fetchBuildLog` — NOT `buildlog/fetchItems` (RESEARCH Pitfall #1)"
    - "mapGetters remains spread into `methods` (not moved to `computed`) — project convention preserved (RESEARCH Pitfall #2)"
    - "IMPROVEMENTS.md records the future `GET /device/:udid/builds` backend endpoint per D-08"
  artifacts:
    - path: "vue/src/pages/Devices/DeviceDetail.vue"
      provides: "Device detail page with build history, environment, transformer assignment, device logs, and transfer button"
      contains: "transferDevice"
      min_lines: 250
    - path: "IMPROVEMENTS.md"
      provides: "Backend improvement tracking note for dedicated per-device build history endpoint"
      contains: "GET /device/:udid/builds"
  key_links:
    - from: "DeviceDetail.vue loadDevice()"
      to: "buildlog/fetchBuildLog + transformers/fetchItems + devices/fetchItems"
      via: "Promise.all([this.fetchItems(), this.fetchBuildLog(), this.fetchTransformers()])"
      pattern: "Promise\\.all\\(\\[this\\.fetchItems\\(\\), this\\.fetchBuildLog\\(\\), this\\.fetchTransformers\\(\\)\\]\\)"
    - from: "DeviceDetail.vue saveTransformers()"
      to: "devices/updateDevice store action"
      via: "this.updateDevice({ udid, changes: { transformers: ... } })"
      pattern: "changes: \\{ transformers:"
    - from: "DeviceDetail.vue transferDevice()"
      to: "devices/transferDevices store action"
      via: "this.transferDevices({ udids: [this.device.udid], ... })"
      pattern: "udids: \\[this\\.device\\.udid\\]"
    - from: "Build History card v-for"
      to: "this.buildHistory data array"
      via: "filtered from getBuildItems() by udid"
      pattern: "v-for=\"\\(build, i\\) in buildHistory\""
    - from: "Device Logs card v-for"
      to: "this.deviceLogs data array"
      via: "filtered from getBuildItems() by last_build_id"
      pattern: "v-for=\"\\(entry, i\\) in deviceLogs\""
---

<objective>
Extend `vue/src/pages/Devices/DeviceDetail.vue` with the five new sections that complete DEVI-11 (Build History, Environment Variables, Transformer Assignment, Device Logs, Transfer button + modal) and add IMPROVEMENTS.md to track the deferred per-device build history backend endpoint (per D-08).

Purpose: DEVI-10 (route to detail page) and the metadata/edit/build/revoke parts of DEVI-11 are already in place. This plan delivers the remaining DEVI-11 sections that bring the Vue device detail page to feature parity with the legacy AngularJS view. All backing store actions already exist; this is pure SFC + Vuex-dispatch work.

Output: One Vue SFC modified (~150 lines added) and one new markdown improvement-tracking file. No new packages, no new store actions, no new routes.
</objective>

<execution_context>
@/Users/igraczech/Repositories/thinx-device-api/services/console/.claude/get-shit-done/workflows/execute-plan.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/ROADMAP.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/REQUIREMENTS.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-4/04-CONTEXT.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-4/04-RESEARCH.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-4/04-PATTERNS.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/pages/Devices/DeviceDetail.vue
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/pages/Devices/Devices.vue
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/store/devices.js
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/store/buildlog.js
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/src/store/transformers.js

<interfaces>
From vue/src/pages/Devices/DeviceDetail.vue (current state — read before editing):

Template (lines 1-76):
  - `<div v-if="device">` root, contains breadcrumb + h1 + b-alerts, then a single `<b-row>` with two `<b-col md="6">` columns.
  - LEFT column (md=6, lines 16-37): Device Info card + Network card.
  - RIGHT column (md=6, lines 40-68): Edit Device card + Actions card + Linked Repository (v-if="device.source") card.
  - Loading state and not-found state are sibling `<div v-else-if>` / `<div v-else>` blocks after the main `<div v-if="device">` (lines 71-75).
  - Actions card (lines 60-63) currently has TWO buttons: Build Firmware (secondary) + Revoke Device (danger). → ADD a third button: Transfer Device (warning) that opens `transfer-modal`.

Script (lines 78-156):
  - Single `import { mapGetters, mapActions } from 'vuex';` (line 79).
  - `filters: { fromNow(val) { ... } }` block (lines 83-93) — already present, KEEP unchanged.
  - data() (lines 94-102): { device, loading, error, message, editForm: { alias, description } }
    → EXTEND editForm with `transformers: []` AND add three new top-level props: `buildHistory: []`, `deviceLogs: []`, `transferForm: { to: '', mig_sources: false, mig_apikeys: false }`.
  - methods (lines 106-154):
      ...mapGetters({ getByUdid: 'devices/getByUdid' }) — line 107 — EXTEND with `getBuildItems: 'buildlog/getItems'` and `getTransformers: 'transformers/getItems'`.
      ...mapActions({ fetchItems, revokeDevices, buildFirmware, updateDevice }) — lines 108-113 — EXTEND with `fetchBuildLog: 'buildlog/fetchBuildLog'`, `fetchTransformers: 'transformers/fetchItems'`, `transferDevices: 'devices/transferDevices'`.
      loadDevice (lines 114-124): currently `await this.fetchItems()` only. REPLACE with a `Promise.all([...])` over the three fetch actions, then populate buildHistory and deviceLogs from `getBuildItems()`.
      saveField (lines 125-135), buildDevice (137-141), revokeDevice (142-153): KEEP UNCHANGED.
      → ADD: saveTransformers(), transferDevice(bvModalEvt).

Project pattern (CRITICAL):
  - mapGetters spread INTO methods (line 107), not computed. Getters are called as functions: `this.getByUdid()(udid)`. KEEP this pattern.
  - The codebase has ONE exception where a computed property is acceptable: a pure derivation with no side effects (e.g. transformerOptions). Per PATTERNS.md, adding a `computed` block is allowed in this file to host `transformerOptions`. The existing file currently has NO computed block — adding one is fine; the new computed must NOT shadow any method name.

Store action and getter contracts (verified from source):
  - devices/updateDevice({ udid, changes }) → { success, message? }  (devices.js:63-66)
  - devices/transferDevices({ udids: string[], to: string, mig_sources: bool, mig_apikeys: bool }) → { success, message? }  (devices.js:59-62)
  - devices/revokeDevices(udids: string[]) → { success, message? }  (devices.js:46-50)
  - buildlog/fetchBuildLog() → returns state.items (normalized array)  (buildlog.js:40-46)
  - buildlog/getItems → array of normalized items  (buildlog.js:49-51)
  - transformers/fetchItems() → returns state.items  (transformers.js:29-35)
  - transformers/getItems → array of { id, utid, alias, body }  (transformers.js:64-66)

Normalized buildlog item shape (verified from buildlog.js:68-80):
  { id, build_id: string, udid: string, date: string, name: string, status: 'OK'|'RUNNING'|'UNKNOWN'|string, log: string[], raw }
  → Build History filters by `b.udid === this.$route.params.udid`.
  → Device Logs filters by `b.build_id === this.device.last_build_id`.

Devices.vue transfer modal pattern (PATTERNS.md lines 215-223, source Devices.vue lines 73-81) — copy verbatim with these adjustments:
  - title="Transfer Device" (singular, not "Transfer Devices")
  - paragraph: "Transfer this device to another owner."
  - @ok handler: `transferDevice` (singular)
  - dispatch payload uses `udids: [this.device.udid]` (single-element array)

CATEGORY context — not needed in this plan (DeviceDetail does not render category pills).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend data(), getters, actions, and loadDevice() to fetch buildlog and transformers in parallel</name>
  <files>vue/src/pages/Devices/DeviceDetail.vue</files>
  <read_first>
    - vue/src/pages/Devices/DeviceDetail.vue — read fully; focus on lines 94-102 (data), 107 (mapGetters), 108-113 (mapActions), 114-124 (loadDevice)
    - vue/src/store/buildlog.js — confirm action name is `fetchBuildLog` (line 40) and getter is `getItems` (line 49); normalized item shape (lines 68-80)
    - vue/src/store/transformers.js — confirm action name is `fetchItems` (line 29) and getter is `getItems` (line 64)
    - vue/src/store/devices.js — confirm `updateDevice` signature is `({ udid, changes })` and returns `{ success, message? }` (lines 63-66)
    - .planning/phase-4/04-PATTERNS.md — lines 369-417 (mapGetters/mapActions extension), lines 420-441 (extended loadDevice)
    - .planning/phase-4/04-RESEARCH.md — Pitfall #1 (buildlog action name), Pitfall #2 (mapGetters in methods), Pitfall #5 (filter fields), Pitfall #6 (transformers must be fetched)
  </read_first>
  <behavior>
    - `data()` returns `device`, `loading`, `error`, `message`, `editForm` (now `{ alias, description, transformers }`), plus three new top-level properties: `buildHistory: []`, `deviceLogs: []`, `transferForm: { to: '', mig_sources: false, mig_apikeys: false }`.
    - The methods block's `mapGetters` spread now includes three keys: `getByUdid: 'devices/getByUdid'`, `getBuildItems: 'buildlog/getItems'`, `getTransformers: 'transformers/getItems'`.
    - The methods block's `mapActions` spread now includes seven keys: the original four (`fetchItems`, `revokeDevices`, `buildFirmware`, `updateDevice`) plus `fetchBuildLog: 'buildlog/fetchBuildLog'`, `fetchTransformers: 'transformers/fetchItems'`, `transferDevices: 'devices/transferDevices'`.
    - `loadDevice()` issues a single `Promise.all([this.fetchItems(), this.fetchBuildLog(), this.fetchTransformers()])` and awaits it before resolving the device from `getByUdid()(udid)`. When the device is found, `editForm.alias`, `editForm.description`, and `editForm.transformers = [...(this.device.transformers || [])]` are set, and `buildHistory` / `deviceLogs` are populated from `getBuildItems()` filtered by `udid` and `last_build_id` respectively. When the device is not found, all three arrays remain empty arrays and `editForm.transformers` stays its default `[]`.
    - `mapGetters` MUST remain spread inside `methods` (not moved to `computed`) per project convention (RESEARCH Pitfall #2).
    - The buildlog dispatch path MUST be `buildlog/fetchBuildLog` — using `buildlog/fetchItems` would silently fail (RESEARCH Pitfall #1).
    - `this.buildHistory` is the result of filtering `getBuildItems()` by `b.udid === udid`; `this.deviceLogs` is the result of filtering the same array by `b.build_id === this.device.last_build_id`. These are two different filtered views of the same dataset (RESEARCH Pitfall #5).
    - `saveField`, `buildDevice`, and `revokeDevice` methods remain BYTE-IDENTICAL to the current implementation.
  </behavior>
  <action>
    Open `vue/src/pages/Devices/DeviceDetail.vue`. Make script-block edits only — DO NOT modify the template in this task; the template edits are Task 2.

    Step (a) — Extend data(). Modify the existing `editForm` value (line 100) from `{ alias: '', description: '' }` to `{ alias: '', description: '', transformers: [] }`. Then append three new properties to the returned object AFTER the modified `editForm` line: `buildHistory: [],`, `deviceLogs: [],`, `transferForm: { to: '', mig_sources: false, mig_apikeys: false },`. The full returned object now has 8 properties.

    Step (b) — Extend mapGetters in methods. Modify line 107 from `...mapGetters({ getByUdid: 'devices/getByUdid' }),` to `...mapGetters({ getByUdid: 'devices/getByUdid', getBuildItems: 'buildlog/getItems', getTransformers: 'transformers/getItems' }),`. DO NOT move this spread to `computed` — keep it inside `methods` per project pattern.

    Step (c) — Extend mapActions. Modify the existing `...mapActions({ ... })` block (lines 108-113) to add three new entries AFTER `updateDevice: 'devices/updateDevice',`: `fetchBuildLog: 'buildlog/fetchBuildLog',` then `fetchTransformers: 'transformers/fetchItems',` then `transferDevices: 'devices/transferDevices',`. The full mapActions spread now has 7 keys.

    Step (d) — Replace loadDevice() (lines 114-124). The new implementation MUST:
    - Set `this.loading = true`.
    - Read `const udid = this.$route.params.udid;` before the await.
    - `await Promise.all([this.fetchItems(), this.fetchBuildLog(), this.fetchTransformers()]);` — these three dispatches run in parallel.
    - `this.device = this.getByUdid()(udid) || null;` (preserves current behavior).
    - If `this.device` is truthy: set `this.editForm.alias = this.device.alias;`, `this.editForm.description = this.device.description || '';`, `this.editForm.transformers = [...(this.device.transformers || [])];`. Then `const allBuilds = this.getBuildItems() || [];` followed by `this.buildHistory = allBuilds.filter(b => b.udid === udid);` and `this.deviceLogs = allBuilds.filter(b => b.build_id === this.device.last_build_id);`.
    - Set `this.loading = false;` at the end.
    - Use `async` keyword and `await` syntax (matches existing methods style).

    DO NOT touch: `name`, `filters` (fromNow), `created()`, `saveField`, `buildDevice`, `revokeDevice`, the `<template>` block, the `<style>` (none currently), or the `import` line. New methods (`saveTransformers`, `transferDevice`) and the `transformerOptions` computed are deferred to Task 2.
  </action>
  <verify>
    <automated>cd /Users/igraczech/Repositories/thinx-device-api/services/console && grep -c "Promise\.all(\[this\.fetchItems(), this\.fetchBuildLog(), this\.fetchTransformers()\])" vue/src/pages/Devices/DeviceDetail.vue && grep -c "fetchBuildLog: 'buildlog/fetchBuildLog'" vue/src/pages/Devices/DeviceDetail.vue && grep -c "fetchTransformers: 'transformers/fetchItems'" vue/src/pages/Devices/DeviceDetail.vue && grep -c "transferDevices: 'devices/transferDevices'" vue/src/pages/Devices/DeviceDetail.vue && grep -c "getBuildItems: 'buildlog/getItems'" vue/src/pages/Devices/DeviceDetail.vue && grep -c "getTransformers: 'transformers/getItems'" vue/src/pages/Devices/DeviceDetail.vue && grep -c "buildHistory: \[\]" vue/src/pages/Devices/DeviceDetail.vue && grep -c "deviceLogs: \[\]" vue/src/pages/Devices/DeviceDetail.vue && grep -c "transferForm:" vue/src/pages/Devices/DeviceDetail.vue && grep -c "transformers: \[\]" vue/src/pages/Devices/DeviceDetail.vue && grep -v '^[[:space:]]*//' vue/src/pages/Devices/DeviceDetail.vue | grep -c "buildlog/fetchItems" || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Promise\.all(\[this\.fetchItems(), this\.fetchBuildLog(), this\.fetchTransformers()\])"` returns 1 — single parallel fetch in loadDevice.
    - `grep -c "fetchBuildLog: 'buildlog/fetchBuildLog'"` returns 1 — correct buildlog action name (NOT fetchItems).
    - `grep -v '^[[:space:]]*//' vue/src/pages/Devices/DeviceDetail.vue | grep -c "buildlog/fetchItems"` returns 0 — wrong action name absent from non-comment code.
    - `grep -c "fetchTransformers: 'transformers/fetchItems'"` returns 1 — transformers action correctly named (transformers IS fetchItems).
    - `grep -c "transferDevices: 'devices/transferDevices'"` returns 1.
    - `grep -c "getBuildItems: 'buildlog/getItems'"` returns 1.
    - `grep -c "getTransformers: 'transformers/getItems'"` returns 1.
    - `grep -c "buildHistory: \[\]"` returns 1; `grep -c "deviceLogs: \[\]"` returns 1; `grep -c "transferForm:"` returns at least 1 (data declaration).
    - `grep -c "transformers: \[\]"` returns at least 1 — editForm.transformers initialised to empty array.
    - `grep -c "this\.buildHistory = allBuilds\.filter(b => b\.udid === udid)"` returns 1 — build history filter uses udid from route.
    - `grep -c "this\.deviceLogs = allBuilds\.filter(b => b\.build_id === this\.device\.last_build_id)"` returns 1 — device logs filter uses last_build_id.
    - `grep -c "editForm\.transformers = \[\.\.\.(this\.device\.transformers || \[\])\]"` returns 1 — transformers initialised from device shallow copy.
    - `awk '/methods: {/,/^  },$/' vue/src/pages/Devices/DeviceDetail.vue | grep -c "mapGetters"` returns 1 — mapGetters STILL inside methods, not moved to computed.
    - `grep -c "^import "` returns 1 — no new import statements added.
    - The Vue dev server (`yarn --cwd vue serve`) compiles the file without errors. Navigating to `/app/device/:udid` for any existing device still renders the current UI (Task 2 adds the new template sections).
  </acceptance_criteria>
  <done>Script block has extended data() with editForm.transformers + 3 new top-level props, extended mapGetters (3 keys), extended mapActions (7 keys), and loadDevice() that fetches devices+buildlog+transformers in parallel and populates buildHistory + deviceLogs. Template unchanged so build still runs; current page renders as before plus new data is fetched/populated in memory.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add saveTransformers, transferDevice methods, transformerOptions computed, and all template sections (Environment, Transformers, Build History, Device Logs, Transfer button + modal)</name>
  <files>vue/src/pages/Devices/DeviceDetail.vue</files>
  <read_first>
    - vue/src/pages/Devices/DeviceDetail.vue — read AGAIN after Task 1's script-block changes; focus on template lines 60-68 (Actions + Linked Repository area) and lines 39-69 (right column layout)
    - vue/src/pages/Devices/Devices.vue — lines 73-81 (transfer modal template) and lines 178-196 (transfer method) — pattern to adapt for single device
    - .planning/phase-4/04-PATTERNS.md — lines 503-510 (Actions card with Transfer button), lines 516-548 (env + transformer cards), lines 552-563 (transformerOptions computed), lines 566-609 (build history + device logs cards), lines 614-647 (transfer modal + transferDevice method)
    - .planning/phase-4/04-CONTEXT.md — D-08 (build history table columns), D-09 (env null guard), D-10 (multi-select save), D-11 (scrollable pre logs), D-12 (transfer button + modal)
    - .planning/phase-4/04-RESEARCH.md — Pitfall #4 (device.environment null guard), Common Pitfalls #5 (filter fields)
    - .planning/phase-4/04-VALIDATION.md — confirms DEVI-10 / DEVI-11 are covered by `device-detail.spec.js` (created in Plan 04-00); the section element identifiers chosen here must be testable
  </read_first>
  <behavior>
    - The script block gains exactly two new methods (`saveTransformers`, `transferDevice`) appended to the end of the methods block (after `revokeDevice`) and exactly one new top-level `computed` block containing exactly one computed property: `transformerOptions`.
    - `saveTransformers()` calls `await this.updateDevice({ udid: this.device.udid, changes: { transformers: this.editForm.transformers } })`; on success sets `this.message = 'Transformers saved.'` and awaits `this.loadDevice()`; on failure sets `this.error = result.message || 'Failed to save transformers.'`.
    - `transferDevice(bvModalEvt)` calls `bvModalEvt.preventDefault()`, returns early if `this.transferForm.to.trim()` is empty, then dispatches `await this.transferDevices({ udids: [this.device.udid], to: this.transferForm.to.trim(), mig_sources: this.transferForm.mig_sources, mig_apikeys: this.transferForm.mig_apikeys })`. On success: sets `this.message = 'Transfer request sent.'`, calls `this.$bvModal.hide('transfer-modal')`, resets `this.transferForm` to its default object. On failure: sets `this.error = result.message || 'Failed to transfer device.'`.
    - `transformerOptions` computed returns `(this.getTransformers() || []).map(t => ({ value: t.utid, text: t.alias || t.utid }))` — pure derivation, no side effects.
    - Template additions (all inside the `<div v-if="device">` root):
      * Actions card (current lines 60-63): a third button is appended AFTER the existing Revoke button, with `variant="warning"`, `@click="$bvModal.show('transfer-modal')"`, `class="mr-2 mb-2"`, label "Transfer Device".
      * RIGHT column gains four NEW `<b-card>` sections AFTER the existing Linked Repository card (which is a `v-if` and may not render). Order: Environment Variables card, Transformer Assignment card, Build History card, Device Logs card.
      * Environment Variables card title "Environment Variables (masked)"; body is `v-if="device.environment && Object.keys(device.environment).length"` wrapping a `<table class="table table-sm table-borderless mb-0">` with a `<tr v-for="(val, key) in device.environment" :key="key">`. The else branch is `<p class="text-muted mb-0">No environment variables.</p>`.
      * Transformer Assignment card title "Transformer Assignment"; body has a `<b-form-group label="Assigned transformers">` containing `<b-form-select multiple v-model="editForm.transformers" :options="transformerOptions" :select-size="5" />`, followed by a `<b-button variant="primary" size="sm" @click="saveTransformers">Save Transformers</b-button>`.
      * Build History card title "Build History"; body is `v-if="buildHistory.length"` wrapping `<table class="table table-striped table-sm">` with thead Date | Build ID | Status and `<tr v-for="(build, i) in buildHistory" :key="i">` rendering `build.date | fromNow`, `<code>{{ build.build_id }}</code>`, and a status `<b-badge :variant="build.status === 'OK' ? 'success' : 'danger'">{{ build.status || '—' }}</b-badge>`. Else branch: `<p class="text-muted mb-0">No build history.</p>`.
      * Device Logs card has outer `v-if="device.last_build_id"`, title "Device Logs (last build)"; body `v-if="deviceLogs.length"` containing `<pre v-for="(entry, i) in deviceLogs" :key="i" style="max-height:300px;overflow-y:auto;font-size:12px;background:#1e1e1e;color:#ddd;padding:1rem;border-radius:4px">{{ Array.isArray(entry.log) ? entry.log.join('\n') : (entry.log || '') }}</pre>`. Else branch: `<p class="text-muted mb-0">No log entries for last build.</p>`.
    - A `<b-modal id="transfer-modal">` is appended INSIDE the `<div v-if="device">` root, AFTER the closing `</b-row>` and BEFORE the `</div>` that closes the root. It has title "Transfer Device", `@ok="transferDevice"`, ok-title "Transfer", ok-variant "warning", contents (in order): `<p>Transfer this device to another owner.</p>`, a `<b-form-group label="Target owner email">` wrapping `<b-form-input id="transfer-to" v-model="transferForm.to" placeholder="owner@example.com" required />`, then `<b-form-checkbox v-model="transferForm.mig_sources" class="mb-2">Migrate source repositories</b-form-checkbox>` and `<b-form-checkbox v-model="transferForm.mig_apikeys">Migrate API keys</b-form-checkbox>`.
    - The Vue DevTools / browser should not throw `Cannot convert undefined or null to object` for devices with `device.environment === null` (RESEARCH Pitfall #4) — the `v-if` guard prevents iteration over null.
    - All existing template content (Device Info, Network, Edit Device, original Actions buttons, Linked Repository) renders BYTE-IDENTICAL to before — only additions, no edits to existing markup besides the third Action button.
  </behavior>
  <action>
    Open `vue/src/pages/Devices/DeviceDetail.vue` (already touched by Task 1). Make script + template edits in this single task.

    Step (a) — Add computed block. After the `data()` block and BEFORE `created()`, insert a new top-level option:
    ```
    computed: {
      transformerOptions() {
        return (this.getTransformers() || []).map(t => ({ value: t.utid, text: t.alias || t.utid }));
      },
    },
    ```
    Make sure to add a trailing comma after the closing `},` so `created()` follows cleanly.

    Step (b) — Append two new methods to the END of the methods block (immediately after the existing `revokeDevice` method, before the closing `},` of methods):
    - `async saveTransformers()`: see behavior block. Use `await this.updateDevice({ udid: this.device.udid, changes: { transformers: this.editForm.transformers } })`. On `result.success`: `this.message = 'Transformers saved.'`; `await this.loadDevice();`. Else: `this.error = result.message || 'Failed to save transformers.';`.
    - `async transferDevice(bvModalEvt)`: call `bvModalEvt.preventDefault();`. If `!this.transferForm.to.trim()` return. Otherwise `const result = await this.transferDevices({ udids: [this.device.udid], to: this.transferForm.to.trim(), mig_sources: this.transferForm.mig_sources, mig_apikeys: this.transferForm.mig_apikeys });`. On success: `this.message = 'Transfer request sent.';`, `this.$bvModal.hide('transfer-modal');`, `this.transferForm = { to: '', mig_sources: false, mig_apikeys: false };`. On failure: `this.error = result.message || 'Failed to transfer device.';`.

    Step (c) — Extend the Actions card (current lines 60-63). After the existing `<b-button variant="danger" @click="revokeDevice" class="mr-2 mb-2">Revoke Device</b-button>` line and BEFORE the closing `</b-card>`, insert:
    ```
            <b-button variant="warning" @click="$bvModal.show('transfer-modal')" class="mr-2 mb-2">Transfer Device</b-button>
    ```

    Step (d) — Append four new cards to the RIGHT column (`<b-col md="6">`) AFTER the existing Linked Repository card (which closes around current line 67). Insert these cards in order, each as a sibling `<b-card>` within the right `<b-col>`:

    Environment Variables card:
    ```
            <b-card title="Environment Variables (masked)" class="mb-3">
              <div v-if="device.environment && Object.keys(device.environment).length">
                <table class="table table-sm table-borderless mb-0">
                  <tr v-for="(val, key) in device.environment" :key="key">
                    <td class="text-muted" style="width:200px">{{ key }}</td>
                    <td><code>{{ val }}</code></td>
                  </tr>
                </table>
              </div>
              <p v-else class="text-muted mb-0">No environment variables.</p>
            </b-card>
    ```

    Transformer Assignment card:
    ```
            <b-card title="Transformer Assignment" class="mb-3">
              <b-form-group label="Assigned transformers">
                <b-form-select multiple v-model="editForm.transformers" :options="transformerOptions" :select-size="5" />
              </b-form-group>
              <b-button variant="primary" size="sm" @click="saveTransformers">Save Transformers</b-button>
            </b-card>
    ```

    Build History card:
    ```
            <b-card title="Build History" class="mb-3">
              <div v-if="buildHistory.length">
                <table class="table table-striped table-sm">
                  <thead>
                    <tr>
                      <th>Date</th>
                      <th>Build ID</th>
                      <th>Status</th>
                    </tr>
                  </thead>
                  <tbody>
                    <tr v-for="(build, i) in buildHistory" :key="i">
                      <td>{{ build.date | fromNow }}</td>
                      <td><code>{{ build.build_id }}</code></td>
                      <td>
                        <b-badge :variant="build.status === 'OK' ? 'success' : 'danger'">{{ build.status || '—' }}</b-badge>
                      </td>
                    </tr>
                  </tbody>
                </table>
              </div>
              <p v-else class="text-muted mb-0">No build history.</p>
            </b-card>
    ```

    Device Logs card (outer v-if for last_build_id):
    ```
            <b-card v-if="device.last_build_id" title="Device Logs (last build)" class="mb-3">
              <div v-if="deviceLogs.length">
                <pre
                  v-for="(entry, i) in deviceLogs"
                  :key="i"
                  style="max-height:300px;overflow-y:auto;font-size:12px;background:#1e1e1e;color:#ddd;padding:1rem;border-radius:4px"
                >{{ Array.isArray(entry.log) ? entry.log.join('\n') : (entry.log || '') }}</pre>
              </div>
              <p v-else class="text-muted mb-0">No log entries for last build.</p>
            </b-card>
    ```

    Step (e) — Append the Transfer modal AFTER the closing `</b-row>` (current line 69) and BEFORE the closing `</div>` that ends the `<div v-if="device">` root (current line 70):
    ```
        <b-modal id="transfer-modal" title="Transfer Device" @ok="transferDevice" ok-title="Transfer" ok-variant="warning">
          <p>Transfer this device to another owner.</p>
          <b-form-group label="Target owner email" label-for="transfer-to">
            <b-form-input id="transfer-to" v-model="transferForm.to" placeholder="owner@example.com" required />
          </b-form-group>
          <b-form-checkbox v-model="transferForm.mig_sources" class="mb-2">Migrate source repositories</b-form-checkbox>
          <b-form-checkbox v-model="transferForm.mig_apikeys">Migrate API keys</b-form-checkbox>
        </b-modal>
    ```

    DO NOT remove or modify: the breadcrumb, page title, alerts, Device Info card, Network card, Edit Device card, the existing two Action buttons (Build Firmware + Revoke Device), or the Linked Repository card. DO NOT add any `<style>` block. DO NOT modify the `filters` block, `data()` (already done in Task 1), `created()`, `saveField`, `buildDevice`, `revokeDevice`, or the `import` statement.
  </action>
  <verify>
    <automated>cd /Users/igraczech/Repositories/thinx-device-api/services/console && grep -c "saveTransformers" vue/src/pages/Devices/DeviceDetail.vue && grep -c "transferDevice(bvModalEvt)" vue/src/pages/Devices/DeviceDetail.vue && grep -c "transformerOptions" vue/src/pages/Devices/DeviceDetail.vue && grep -c "id=\"transfer-modal\"" vue/src/pages/Devices/DeviceDetail.vue && grep -c "Transfer Device" vue/src/pages/Devices/DeviceDetail.vue && grep -c "Environment Variables (masked)" vue/src/pages/Devices/DeviceDetail.vue && grep -c "Transformer Assignment" vue/src/pages/Devices/DeviceDetail.vue && grep -c "Build History" vue/src/pages/Devices/DeviceDetail.vue && grep -c "Device Logs (last build)" vue/src/pages/Devices/DeviceDetail.vue && grep -c "v-for=\"(build, i) in buildHistory\"" vue/src/pages/Devices/DeviceDetail.vue && grep -c "v-for=\"(entry, i) in deviceLogs\"" vue/src/pages/Devices/DeviceDetail.vue && grep -c "v-for=\"(val, key) in device.environment\"" vue/src/pages/Devices/DeviceDetail.vue && grep -c "v-model=\"editForm.transformers\"" vue/src/pages/Devices/DeviceDetail.vue && grep -c "udids: \[this\.device\.udid\]" vue/src/pages/Devices/DeviceDetail.vue && grep -c "changes: { transformers: this\.editForm\.transformers }" vue/src/pages/Devices/DeviceDetail.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "saveTransformers"` returns at least 2 (method definition + Save Transformers @click binding).
    - `grep -c "transferDevice(bvModalEvt)"` returns 1 — single method definition with the bvModalEvt parameter.
    - `grep -c "transformerOptions"` returns at least 2 (computed definition + :options binding).
    - `grep -c "computed: {"` returns 1 — a single computed block was added.
    - `grep -c "id=\"transfer-modal\""` returns 1 — single modal id.
    - `grep -c "\$bvModal.show('transfer-modal')"` returns 1 — single trigger button.
    - `grep -c "Environment Variables (masked)"` returns 1 (b-card title).
    - `grep -c "Transformer Assignment"` returns 1.
    - `grep -c "Build History"` returns 1.
    - `grep -c "Device Logs (last build)"` returns 1.
    - `grep -c "v-for=\"(build, i) in buildHistory\""` returns 1.
    - `grep -c "v-for=\"(entry, i) in deviceLogs\""` returns 1.
    - `grep -c "v-for=\"(val, key) in device.environment\""` returns 1.
    - `grep -c "v-if=\"device.environment && Object.keys(device.environment).length\""` returns 1 — null/empty guard present.
    - `grep -c "v-model=\"editForm.transformers\""` returns 1 — multi-select bound to editForm.transformers.
    - `grep -c "udids: \[this\.device\.udid\]"` returns 1 — transfer dispatch uses single-element array.
    - `grep -c "changes: { transformers: this\.editForm\.transformers }"` returns 1 — updateDevice payload shape correct.
    - `grep -c "this\.\$bvModal\.hide('transfer-modal')"` returns 1 — modal closed on successful transfer.
    - `grep -c "Save Transformers"` returns 1 — button label present.
    - `awk '/methods: {/,/^  },$/' vue/src/pages/Devices/DeviceDetail.vue | grep -c "mapGetters"` returns 1 — mapGetters STILL inside methods.
    - `grep -c "^import "` returns 1 — no new import statements added.
    - The Vue dev server (`yarn --cwd vue serve`) compiles the file without template errors.
    - Navigating to `/app/device/:udid` for an existing device renders all five new cards (Environment Variables, Transformer Assignment, Build History, Device Logs only if last_build_id is set) plus the Transfer Device button in the Actions card.
    - Clicking Transfer Device opens the modal; submitting with empty email is a no-op; submitting with a valid email dispatches `devices/transferDevices` and closes the modal.
    - Selecting transformers in the multi-select and clicking Save Transformers dispatches `devices/updateDevice` with the correct payload shape.
    - For a device with `environment === null`: the Environment Variables card renders the "No environment variables." fallback without throwing (RESEARCH Pitfall #4 mitigated).
    - For a device with no builds: Build History card renders "No build history." without throwing.
  </acceptance_criteria>
  <done>DeviceDetail.vue script has saveTransformers + transferDevice methods, transformerOptions computed. Template has Environment Variables, Transformer Assignment, Build History, Device Logs cards, the Transfer Device button in the Actions card, and the transfer modal. Page renders all DEVI-11 sections; transfer + transformer assignment work end-to-end through existing store actions.</done>
</task>

<task type="auto">
  <name>Task 3: Create IMPROVEMENTS.md tracking the deferred per-device build history endpoint</name>
  <files>IMPROVEMENTS.md</files>
  <read_first>
    - .planning/phase-4/04-CONTEXT.md — D-08 note ("Add a note to IMPROVEMENTS.md tracking the future dedicated per-device build history endpoint")
    - .planning/phase-4/04-RESEARCH.md — Deferred Ideas section ("Dedicated per-device build history API endpoint")
    - .planning/phase-4/04-CONTEXT.md — Specifics section ("Note to add to IMPROVEMENTS.md: 'Add `GET /device/:udid/builds` endpoint for per-device build history (currently requires client-side filter of full build log)'")
  </read_first>
  <action>
    Create a new file `IMPROVEMENTS.md` in the services/console working directory (project root for this console service — same directory as `package.json` and the existing `IMPLEMENTATION_PLAN.md`). The file is a simple markdown list of backend/UX improvement notes that arose during Vue migration but were deferred. Use this exact structure:

    - Top-level heading: `# THiNX Console — Improvements & Deferred Backend Work`
    - One short paragraph explaining the file's purpose (deferred items surfaced during Vue migration that need backend or cross-cutting work).
    - A `## Backend API Improvements` section with a markdown bulleted list containing at least one entry:
      * `**GET /device/:udid/builds** — Per-device build history endpoint. Currently the Vue console fetches the full account-wide build log via `GET /logs/build` and filters client-side by `udid` in `DeviceDetail.vue` (see Phase 4, plan 04-02). A dedicated endpoint would reduce payload size and allow server-side pagination. Surfaced by: Phase 4 (DEVI-11). Decision ref: D-08.`
    - End the file with a single trailing newline.

    Do NOT add any other content, do NOT include emojis. The file must be plain Markdown valid in any Markdown parser.
  </action>
  <verify>
    <automated>test -f /Users/igraczech/Repositories/thinx-device-api/services/console/IMPROVEMENTS.md && grep -c "GET /device/:udid/builds" /Users/igraczech/Repositories/thinx-device-api/services/console/IMPROVEMENTS.md && grep -c "^## Backend API Improvements" /Users/igraczech/Repositories/thinx-device-api/services/console/IMPROVEMENTS.md && grep -c "DEVI-11" /Users/igraczech/Repositories/thinx-device-api/services/console/IMPROVEMENTS.md && grep -c "D-08" /Users/igraczech/Repositories/thinx-device-api/services/console/IMPROVEMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - File `services/console/IMPROVEMENTS.md` exists.
    - `grep -c "GET /device/:udid/builds"` returns at least 1 — endpoint name present.
    - `grep -c "^# THiNX Console — Improvements"` returns 1 — top-level heading.
    - `grep -c "^## Backend API Improvements"` returns 1 — backend section heading.
    - `grep -c "DEVI-11"` returns at least 1 — requirement traceability.
    - `grep -c "D-08"` returns at least 1 — decision traceability.
    - File ends with a single trailing newline (`tail -c 1` returns the newline byte).
  </acceptance_criteria>
  <done>IMPROVEMENTS.md exists at services/console root, mentions GET /device/:udid/builds with traceability to DEVI-11 and D-08, and is committed alongside DeviceDetail.vue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route param `:udid` → loadDevice() | UDID comes from the URL — used only to look up an already-fetched device from the store (no SQL/templating); same trust level as the existing DeviceDetail page |
| transferForm.to (user email input) → POST /transfer/request | User-typed email forwarded to backend by existing `devices/transferDevices` action; backend validates and resolves the target owner |
| editForm.transformers (multi-select selection) → POST /device | List of UTIDs chosen from the bounded set returned by `transformers/getItems`; backend validates UTID ownership |
| build log items rendered as `<pre>{{ entry.log.join('\n') }}</pre>` | Build log strings come from backend; Vue `{{ }}` interpolation auto-escapes, no `v-html` used |
| `<code>{{ val }}</code>` (environment variable values) | Values are masked server-side; Vue `{{ }}` interpolation auto-escapes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | Transfer button + transferDevice() | mitigate | The modal pattern requires an explicit `OK` click and a non-empty target email (`transferDevice` returns early on empty `to`). Reuses the existing `devices/transferDevices` action — same control already accepted for the Devices.vue bulk path. |
| T-04-02-02 | Tampering | saveTransformers() — payload shape mismatch | mitigate | Payload uses the documented signature `updateDevice({ udid, changes: { transformers: [...utids] } })` verified against `vue/src/store/devices.js:63-66`. Acceptance criterion greps for the exact string. UTIDs originate from the bounded `transformerOptions` derived from `transformers/getItems` — no free-form input. |
| T-04-02-03 | Information Disclosure | Build log `<pre>` rendering with `entry.log.join('\n')` | mitigate | `entry.log` is an array of strings produced by `normalizeBuildItems` (see `buildlog.js:75-77` — `.filter(Boolean)` removes falsy entries). Vue `{{ }}` interpolation auto-escapes HTML; no `v-html` is used. CSS `max-height:300px;overflow-y:auto` confines content to a scrollable region. |
| T-04-02-04 | Information Disclosure | `device.environment` masked values rendered in `<code>` | accept | Values are masked server-side per CONTEXT.md ("masked env vars"). The card title explicitly reads "Environment Variables (masked)" to signal this to the user. |
| T-04-02-05 | Denial of Service | `getBuildItems()` returns the full account-wide build log (could be large) | accept | Same cost already incurred by `vue/src/pages/History/History.vue` which uses the identical dispatch. Two client-side filters are O(n) over the same array — no additional cost. The future `GET /device/:udid/builds` endpoint (tracked in IMPROVEMENTS.md per D-08) is the long-term fix. |
| T-04-02-06 | Tampering | Transfer modal accepts any email string | mitigate | Empty-string check (`!this.transferForm.to.trim()`) blocks empty submissions. Backend `/transfer/request` validates that the target owner exists and is reachable. This mirrors the existing Devices.vue transfer flow with no relaxation. |
| T-04-02-07 | Tampering | Promise.all([fetchItems, fetchBuildLog, fetchTransformers]) — partial failure | accept | Each fetch action returns the store state regardless of HTTP success (the existing pattern from Devices.vue loadData). Partial failures result in empty buildHistory / deviceLogs / transformerOptions — the cards display their empty-state fallbacks. No exception leaks to the user; same UX as the existing single-fetch loadDevice. |
| T-04-02-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NO new npm packages. RESEARCH.md Package Legitimacy Audit confirms zero new dependencies for the entire phase. All used libraries (Vue 2.6.14, BootstrapVue 2.21.2, Vuex 3.6.2) are already in package.json. |
</threat_model>

<verification>
1. Run dev server: `yarn --cwd vue serve` and navigate to `/app/device/:udid` for an existing device.
2. Confirm the right column renders these cards in order: Edit Device, Actions (now with Build / Revoke / Transfer buttons), Linked Repository (if device.source), Environment Variables, Transformer Assignment, Build History, Device Logs (only if last_build_id).
3. Environment Variables: for a device with env vars, key-value rows render. For a device with `environment: null`, the "No environment variables." fallback renders without browser errors.
4. Transformer Assignment: the multi-select shows transformer aliases as option text; pre-existing assignments are highlighted from `device.transformers`. Selecting additional transformers and clicking Save Transformers dispatches `devices/updateDevice` with `{ udid, changes: { transformers: [...] } }` and the success message appears. Reloading the page shows the new assignments persisted.
5. Build History: for a device with builds, the table shows date | build_id | status badge. For a device with no builds, the "No build history." fallback renders.
6. Device Logs: for a device with `last_build_id` set, the scrollable `<pre>` block(s) render with the build's log lines. For a device without last_build_id, the entire Device Logs card is not rendered (outer `v-if`).
7. Transfer Device button: clicking opens the `transfer-modal`. Submitting with empty email is a no-op. Submitting with a valid email dispatches `devices/transferDevices({ udids: [device.udid], to, mig_sources, mig_apikeys })`; success message appears and modal closes.
8. The existing Edit Device, Build Firmware, and Revoke Device buttons continue to work unchanged.
9. Cypress sanity: `yarn --cwd vue cy:open` loads `device-detail.spec.js` (created by Plan 04-00); the 7 stub tests still load and can later assert against the new cards.
10. IMPROVEMENTS.md exists at `services/console/IMPROVEMENTS.md` and contains the GET /device/:udid/builds note with DEVI-11 + D-08 traceability.
</verification>

<success_criteria>
- DEVI-10: Detail page reachable at `/app/device/:udid` (preexisting; unchanged).
- DEVI-11: Detail page renders metadata (existing) + assigned repository (existing) + build/deploy history (new) + device enviros (new) + transformer assignment (new + editable + saved via updateDevice) + device logs (new) + revoke (existing) + transfer (new modal + dispatch).
- D-08: Build History uses `buildlog/fetchBuildLog` filtered client-side by `device.udid`; IMPROVEMENTS.md records the future dedicated endpoint.
- D-09: Environment variables card renders read-only key-value table from `device.environment` with null guard.
- D-10: Transformer multi-select bound to `editForm.transformers`; save dispatches `devices/updateDevice({ udid, changes: { transformers: [...utids] } })`.
- D-11: Device Logs filtered by `device.last_build_id`; rendered as scrollable `<pre>` blocks.
- D-12: Transfer button added to Actions card; dispatches `devices/transferDevices({ udids: [device.udid], to, mig_sources, mig_apikeys })`.
- All acceptance criteria grep gates from Tasks 1, 2, and 3 pass.
- No regressions in DeviceDetail.vue rendering for existing sections.
- `mapGetters` remains in `methods` (project convention preserved).
- buildlog dispatch path is `buildlog/fetchBuildLog` (never `buildlog/fetchItems`).
</success_criteria>

<output>
Create /Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-4/04-02-SUMMARY.md when done
</output>
