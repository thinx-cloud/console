---
phase: 04-device-management
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - vue/cypress/integration/devices.spec.js
  - vue/cypress/integration/device-detail.spec.js
autonomous: true
requirements:
  - DEVI-01
  - DEVI-02
  - DEVI-03
  - DEVI-04
  - DEVI-05
  - DEVI-06
  - DEVI-07
  - DEVI-08
  - DEVI-09
  - DEVI-10
  - DEVI-11
tags:
  - cypress
  - e2e
  - test-stub
  - vue

must_haves:
  truths:
    - "vue/cypress/integration/devices.spec.js exists and is loaded by `yarn cy:open`"
    - "vue/cypress/integration/device-detail.spec.js exists and is loaded by `yarn cy:open`"
    - "Both specs use the same describe/beforeEach/cy.login()/cy.visit pattern as login.spec.js"
    - "devices.spec.js has at least 6 `it(...)` blocks each tagged with a DEVI-XX TODO covering DEVI-01 through DEVI-09 smoke"
    - "device-detail.spec.js has at least 6 `it(...)` blocks covering DEVI-10 navigation and DEVI-11 detail sections"
    - "Specs do not block — empty bodies or TODO-only assertions pass under `yarn cy:open` without throwing"
  artifacts:
    - path: "vue/cypress/integration/devices.spec.js"
      provides: "Wave 0 e2e stub for Devices list (DEVI-01–09 coverage)"
      contains: "Devices feature"
    - path: "vue/cypress/integration/device-detail.spec.js"
      provides: "Wave 0 e2e stub for Device Detail page (DEVI-10/11 coverage)"
      contains: "Device Detail feature"
  key_links:
    - from: "devices.spec.js beforeEach"
      to: "cy.login() + cy.visit('http://localhost:3000/#/app/devices')"
      via: "Cypress custom command (defined in vue/cypress/support/commands.ts)"
      pattern: "cy\\.login\\(\\)"
    - from: "device-detail.spec.js beforeEach"
      to: "cy.login() then cy.visit('/app/devices') (navigation to /device/:udid happens inside `it` blocks)"
      via: "Cypress custom command"
      pattern: "cy\\.login\\(\\)"
---

<objective>
Create Wave 0 Cypress spec stubs for the Devices list and Device Detail pages so all Wave 1 implementation tasks have an automated verification target (Nyquist compliance for VALIDATION.md).

Purpose: VALIDATION.md flags both spec files as Wave 0 gaps. Without these stubs, every implementation task in Wave 1 would have to claim MISSING for `<automated>` verify, violating the Nyquist rule. The stubs are minimal — `describe`/`beforeEach`/`cy.login()`/`cy.visit()` plus one `it(...)` per requirement with a `// TODO DEVI-XX` body. Subsequent waves fill in assertions.

Output: Two new Cypress spec files registered with the existing Cypress runner. No production code is touched.
</objective>

<execution_context>
@/Users/igraczech/Repositories/thinx-device-api/services/console/.claude/get-shit-done/workflows/execute-plan.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/ROADMAP.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/REQUIREMENTS.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-4/04-CONTEXT.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-4/04-PATTERNS.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-4/04-VALIDATION.md
@/Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/integration/login.spec.js

<interfaces>
From vue/cypress/integration/login.spec.js (reference structure — copy describe/beforeEach pattern):

```
describe('Login feature', function() {
  beforeEach(() => {
    cy.viewport(1536, 754);
    cy.visit('http://localhost:3000/#/login');
  });
  it.only('Should log in with static test account', function() {
    cy.login();
  });
});
```

Notes:
- `cy.login()` is a custom command defined in vue/cypress/support/commands.ts — use it in `beforeEach` so each spec lands authenticated.
- Viewport size 1536×754 is the project convention — use the same value.
- Cypress 9.5.4 syntax (mocha-style `describe`/`it`).
- DO NOT use `it.only` in stubs — that locks the runner to a single test. Use plain `it(...)` (run all).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Cypress stub for Devices list page</name>
  <files>vue/cypress/integration/devices.spec.js</files>
  <read_first>
    - vue/cypress/integration/login.spec.js — reference describe/beforeEach/cy.login() pattern, viewport convention 1536×754
    - vue/cypress/support/commands.ts — confirm `cy.login()` custom command exists
    - vue/cypress.json — confirm Cypress 9.5.4 config (no special integration folder override)
    - .planning/phase-4/04-VALIDATION.md — confirm the Per-Task Verification Map column "Test Type" and "Automated Command"
  </read_first>
  <action>
    Create vue/cypress/integration/devices.spec.js with `describe('Devices feature', function() { ... })`. In `beforeEach` set `cy.viewport(1536, 754)`, call `cy.login()`, then `cy.visit('http://localhost:3000/#/app/devices')`. Add the following `it(...)` blocks, each with a single-line TODO comment as the body (no assertions yet — Wave 1 fills them in):
    - `it('Should display category filter pills (DEVI-01)', function() { /* TODO DEVI-01: verify pill buttons visible */ })`
    - `it('Should filter devices by category (DEVI-01)', function() { /* TODO DEVI-01: click pill, verify table rows filtered */ })`
    - `it('Should sort devices by alias (DEVI-02)', function() { /* TODO DEVI-02: select Alias in sort dropdown, verify order */ })`
    - `it('Should search devices by alias substring (DEVI-03)', function() { /* TODO DEVI-03: type in search input, verify matching rows only */ })`
    - `it('Should toggle to grid view (DEVI-04)', function() { /* TODO DEVI-04: click grid icon, verify b-card elements appear */ })`
    - `it('Should revoke a single device with confirmation (DEVI-05)', function() { /* TODO DEVI-05: click row Revoke button, confirm dialog, verify device removed */ })`
    - `it('Should bulk revoke selected devices (DEVI-06)', function() { /* TODO DEVI-06 smoke: bulk revoke already implemented; verify pre-existing flow still works */ })`
    - `it('Should transfer a device (DEVI-07)', function() { /* TODO DEVI-07 smoke: transfer modal opens and dispatches */ })`
    - `it('Should push configuration to selected devices (DEVI-08)', function() { /* TODO DEVI-08 smoke: push-config modal opens */ })`
    - `it('Should trigger firmware build (DEVI-09)', function() { /* TODO DEVI-09 smoke: build button triggers store action */ })`
    Do NOT use `it.only`. Do NOT add any `cy.get` or assertion calls — Wave 1 tasks add real bodies. Make sure the file ends with a single trailing newline and is valid ES2017 JavaScript so Cypress can load it.
  </action>
  <verify>
    <automated>node --check /Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/integration/devices.spec.js && grep -c "^  it(" /Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/integration/devices.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - File vue/cypress/integration/devices.spec.js exists.
    - `node --check` on the file exits 0 (valid JavaScript).
    - `grep -c "^  it("` returns 10 (one per requirement plus smoke entries).
    - `grep -c "it.only"` returns 0 — no `.only` lock-ins.
    - `grep -c "cy.login()"` returns 1 — login command called exactly once (in beforeEach).
    - `grep -c "cy.visit('http://localhost:3000/#/app/devices')"` returns 1.
    - `grep -c "TODO DEVI-"` returns at least 10 (one TODO per `it`).
    - Each `it` description contains a DEVI-XX requirement ID in parentheses.
  </acceptance_criteria>
  <done>devices.spec.js exists, parses, contains 10 `it` stubs covering DEVI-01–09, and is recognised by `yarn cy:open` as a runnable but currently no-assert spec file.</done>
</task>

<task type="auto">
  <name>Task 2: Create Cypress stub for Device Detail page</name>
  <files>vue/cypress/integration/device-detail.spec.js</files>
  <read_first>
    - vue/cypress/integration/login.spec.js — reference structure
    - vue/cypress/integration/devices.spec.js (just created in Task 1) — keep same describe/beforeEach style for consistency
    - vue/cypress/support/commands.ts — confirm cy.login() exists
  </read_first>
  <action>
    Create vue/cypress/integration/device-detail.spec.js with `describe('Device Detail feature', function() { ... })`. In `beforeEach` set `cy.viewport(1536, 754)`, call `cy.login()`, then `cy.visit('http://localhost:3000/#/app/devices')` (Wave 1 spec bodies will navigate to /app/device/:udid by clicking a Detail button or by direct visit with a known UDID). Add these `it(...)` blocks, each with a single TODO comment body:
    - `it('Should navigate to device detail on Detail button click (DEVI-10)', function() { /* TODO DEVI-10: click first Detail button, verify URL changes to /app/device/:udid */ })`
    - `it('Should display device info section (DEVI-11)', function() { /* TODO DEVI-11: verify Device Info card visible */ })`
    - `it('Should display environment variables section (DEVI-11)', function() { /* TODO DEVI-11: verify Environment Variables card present */ })`
    - `it('Should display transformer assignment section (DEVI-11)', function() { /* TODO DEVI-11: verify Transformer Assignment card and b-form-select present */ })`
    - `it('Should display build history section (DEVI-11)', function() { /* TODO DEVI-11: verify Build History card present (empty state allowed) */ })`
    - `it('Should display device logs section (DEVI-11)', function() { /* TODO DEVI-11: verify Device Logs card present when last_build_id is set */ })`
    - `it('Should display Transfer button in Actions card (DEVI-11)', function() { /* TODO DEVI-11/D-12: Transfer button visible in Actions card */ })`
    Do NOT use `it.only`. Do NOT add real assertions. Ensure trailing newline and valid ES2017.
  </action>
  <verify>
    <automated>node --check /Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/integration/device-detail.spec.js && grep -c "^  it(" /Users/igraczech/Repositories/thinx-device-api/services/console/vue/cypress/integration/device-detail.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - File vue/cypress/integration/device-detail.spec.js exists.
    - `node --check` on the file exits 0.
    - `grep -c "^  it("` returns 7.
    - `grep -c "it.only"` returns 0.
    - `grep -c "cy.login()"` returns 1.
    - `grep -c "TODO DEVI-"` returns at least 7.
    - One `it` mentions DEVI-10; at least six `it` blocks mention DEVI-11.
  </acceptance_criteria>
  <done>device-detail.spec.js exists, parses, contains 7 `it` stubs covering DEVI-10 (navigation) and DEVI-11 (six detail sections + Transfer button), recognised by `yarn cy:open` as a runnable no-assert spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cypress runner → dev server | Test spec dispatches HTTP requests against http://localhost:3000 — same origin as real app, no cross-origin concerns |
| cy.login() → /login form | Custom command performs real auth flow against the dev backend with a static test account |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-00-01 | Information Disclosure | static test account credentials in cy.login() | accept | Test creds live in `vue/cypress/support/commands.ts` (pre-existing), are dev-only, never reach production. No change here. |
| T-04-00-02 | Tampering | spec files committed to repo | mitigate | Stubs contain no secrets, no eval, no shell-out; only describe/it/cy.* calls. Verified by grep that no `eval`, `Function(`, or `require(` of arbitrary paths appears. |
| T-04-00-SC | Tampering | npm install (supply chain) | accept | This plan installs NO new npm packages. Cypress 9.5.4 already pinned in package.json. RESEARCH.md confirms zero new dependencies for the entire phase. |
</threat_model>

<verification>
1. `yarn --cwd vue cy:open` opens the Cypress runner without parse errors.
2. The runner sidebar lists `devices.spec.js` and `device-detail.spec.js` alongside the existing `login.spec.js`.
3. Selecting each spec shows the expected number of pending `it` blocks (10 and 7 respectively).
4. Running them all passes (empty bodies are vacuous true) — confirms Wave 1 tasks can reference them with `<automated>yarn cy:open --spec ...</automated>` style verifies.
</verification>

<success_criteria>
- Both spec files exist, parse with `node --check`, and match the grep gates in acceptance_criteria.
- No `it.only` used.
- Each requirement DEVI-01 through DEVI-11 has at least one corresponding `it` stub (DEVI-01: 2 stubs covering filter pills + filter behavior; DEVI-02 through DEVI-11: 1 stub each).
- Wave 1 implementation tasks can now declare a non-MISSING `<automated>` verify referencing these specs.
</success_criteria>

<output>
Create /Users/igraczech/Repositories/thinx-device-api/services/console/.planning/phase-4/04-00-SUMMARY.md when done
</output>
